---
phase: 03-feed-posts
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/NossoVizinho.Api/Models/DTOs/AuthResponse.cs
  - src/NossoVizinho.Api/Services/AuthService.cs
  - packages/shared-types/src/auth.ts
autonomous: true
gap_closure: true
requirements: [FEED-003, FEED-008]
must_haves:
  truths:
    - "Verified user logging in lands on /feed (not /onboarding/cep)"
    - "Verified user sees PostComposer enabled and can publish"
    - "Admin user can access /admin/moderation"
    - "/api/v1/auth/login response JSON includes bairroId, isVerified, isAdmin"
  artifacts:
    - path: "src/NossoVizinho.Api/Models/DTOs/AuthResponse.cs"
      provides: "Extended UserInfo record with BairroId, IsVerified, IsAdmin"
      contains: "IsVerified"
    - path: "src/NossoVizinho.Api/Services/AuthService.cs"
      provides: "All 3 UserInfo projections populate new fields"
    - path: "packages/shared-types/src/auth.ts"
      provides: "UserInfo TS interface with required (non-optional) bairroId/isVerified/isAdmin"
  key_links:
    - from: "AuthService.cs (Login/Register/Refresh)"
      to: "UserInfo DTO"
      via: "constructor projection from User entity"
      pattern: "new UserInfo\\(.*IsVerified.*IsAdmin"
---

<objective>
Fix AuthResponse projection so verified-user / admin flags reach the frontend auth store. Currently JWT claims are correct but the JSON `user` object only exposes (Id, Email, DisplayName, EmailConfirmed), so the frontend treats every logged-in user as unverified non-admin. This blocks FEED-003 (PostComposer publish for verified residents) and FEED-008 (admin moderation access) as documented in 03-VERIFICATION.md.

Purpose: Unblock feed publishing and admin moderation without re-running auth/onboarding work.
Output: Three updated files; verified user reaches /feed and can publish; admin reaches /admin/moderation.
</objective>

<execution_context>
@C:/Users/acq20/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/acq20/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-feed-posts/03-VERIFICATION.md
@src/NossoVizinho.Api/Models/DTOs/AuthResponse.cs
@src/NossoVizinho.Api/Services/AuthService.cs
@src/NossoVizinho.Api/Services/TokenService.cs
@packages/shared-types/src/auth.ts

<interfaces>
Current (broken):
```csharp
public record UserInfo(Guid Id, string Email, string? DisplayName, bool EmailConfirmed);
```

User entity (already has these fields, used by TokenService):
- `user.BairroId` -> int?
- `user.IsVerified` -> bool
- `user.IsAdmin` -> bool

AuthService.cs creates UserInfo at lines 53, 97, 151 (Login, Register, Refresh paths).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend UserInfo DTO and project new fields in AuthService</name>
  <files>src/NossoVizinho.Api/Models/DTOs/AuthResponse.cs, src/NossoVizinho.Api/Services/AuthService.cs</files>
  <read_first>
    - src/NossoVizinho.Api/Models/DTOs/AuthResponse.cs (full)
    - src/NossoVizinho.Api/Services/AuthService.cs lines 40-160 (Login/Register/Refresh paths)
    - src/NossoVizinho.Api/Services/TokenService.cs lines 25-40 (confirm field names on User entity)
  </read_first>
  <action>
    1. In AuthResponse.cs, replace the UserInfo record with:
       ```csharp
       public record UserInfo(
           Guid Id,
           string Email,
           string? DisplayName,
           bool EmailConfirmed,
           int? BairroId,
           bool IsVerified,
           bool IsAdmin);
       ```
       Keep `public record AuthResponse(string AccessToken, UserInfo User);` unchanged.

    2. In AuthService.cs, update ALL THREE `new UserInfo(...)` constructions (currently at lines 53, 97, 151) to:
       ```csharp
       new UserInfo(user.Id, user.Email, user.DisplayName, user.EmailConfirmed, user.BairroId, user.IsVerified, user.IsAdmin)
       ```
       Do not change anything else in those methods (refresh tokens, error handling, etc.).

    3. Do NOT touch TokenService.cs — JWT claims are already correct.
    4. Do NOT add new endpoints. Do NOT touch other DTOs.
  </action>
  <verify>
    <automated>cd src/NossoVizinho.Api && dotnet build -nologo -v q</automated>
  </verify>
  <done>
    - Build succeeds with zero errors
    - `grep -n "IsVerified" src/NossoVizinho.Api/Models/DTOs/AuthResponse.cs` finds the field
    - `grep -c "user.IsVerified, user.IsAdmin" src/NossoVizinho.Api/Services/AuthService.cs` returns 3
  </done>
  <acceptance_criteria>
    - AuthResponse.cs contains positional record params: `BairroId`, `IsVerified`, `IsAdmin`
    - AuthService.cs has exactly 3 occurrences of `new UserInfo(user.Id, user.Email, user.DisplayName, user.EmailConfirmed, user.BairroId, user.IsVerified, user.IsAdmin)`
    - `dotnet build` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Update shared-types UserInfo + smoke test login response</name>
  <files>packages/shared-types/src/auth.ts</files>
  <read_first>
    - packages/shared-types/src/auth.ts (full)
  </read_first>
  <action>
    1. In `packages/shared-types/src/auth.ts`, change the `UserInfo` interface so `bairroId`, `isVerified`, and `isAdmin` are REQUIRED (not optional) — backend now always projects them:
       ```typescript
       export interface UserInfo {
         id: string;
         email: string;
         displayName: string | null;
         emailConfirmed: boolean;
         bairroId: number | null;
         isVerified: boolean;
         isAdmin: boolean;
       }
       ```
       Note: `bairroId` is `number | null` (not optional `?`), matching C# `int?` serialized as null.

    2. Rebuild shared-types if it has a build step: `cd packages/shared-types && npm run build` (skip silently if no build script).

    3. Smoke test the API contract — start the API and POST to /api/v1/auth/login with a known verified user, confirm response JSON contains the new fields. If no test user is available locally, instead inspect the compiled DTO via:
       ```
       grep -A 10 "public record UserInfo" src/NossoVizinho.Api/Models/DTOs/AuthResponse.cs
       ```
       and confirm `BairroId`, `IsVerified`, `IsAdmin` are present.
  </action>
  <verify>
    <automated>grep -E "isVerified: boolean" packages/shared-types/src/auth.ts && grep -E "isAdmin: boolean" packages/shared-types/src/auth.ts && grep -E "bairroId: number \| null" packages/shared-types/src/auth.ts</automated>
  </verify>
  <done>
    - shared-types UserInfo has `bairroId: number | null`, `isVerified: boolean`, `isAdmin: boolean` as REQUIRED fields
    - No TypeScript compile errors in consumers (web/mobile) caused by the field becoming required — if any surface, update the call site to handle the now-guaranteed value (typically removing `?? false` fallbacks)
  </done>
  <acceptance_criteria>
    - Grep finds all three required field declarations
    - `cd packages/shared-types && npx tsc --noEmit` exits 0 (if tsconfig present)
  </acceptance_criteria>
</task>

</tasks>

<verification>
After both tasks:
1. `cd src/NossoVizinho.Api && dotnet build` → 0 errors
2. Grep AuthResponse.cs for `IsVerified` → match
3. Grep AuthService.cs for `user.IsVerified, user.IsAdmin` → 3 matches
4. Grep shared-types/src/auth.ts for `isVerified: boolean` → match
5. Manual smoke (if API runnable): login as verified user → response.user.isVerified === true, response.user.bairroId !== null
</verification>

<success_criteria>
- Backend AuthResponse JSON includes `bairroId`, `isVerified`, `isAdmin` on login/register/refresh
- Frontend shared types reflect required (non-optional) fields
- Verified user no longer redirected to /onboarding/cep after login
- PostComposer publish button enabled for verified users
- /admin/moderation accessible for admin users
- Build/typecheck green
</success_criteria>

<output>
Create `.planning/phases/03-feed-posts/03-03-SUMMARY.md` documenting:
- Files changed (3)
- Field additions
- Verification gap closed (FEED-003, FEED-008 unblocked)
</output>
