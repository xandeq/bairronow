---
phase: 03-feed-posts
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - packages/shared-api-client/src/index.ts
  - packages/shared-api-client/src/feed.ts
  - packages/shared-validators/src/index.ts
  - packages/shared-validators/src/feed.ts
  - frontend/src/lib/feed.ts
  - frontend/src/lib/signalr.ts
  - frontend/src/stores/feed-store.ts
  - frontend/src/stores/notification-store.ts
  - frontend/src/components/features/PostCard.tsx
  - frontend/src/components/features/PostComposer.tsx
  - frontend/src/components/features/CommentThread.tsx
  - frontend/src/components/features/LikeButton.tsx
  - frontend/src/components/features/ReportDialog.tsx
  - frontend/src/components/features/NotificationBell.tsx
  - frontend/src/components/layouts/FeedHeader.tsx
  - frontend/src/app/(main)/feed/page.tsx
  - frontend/src/app/(main)/feed/[id]/page.tsx
  - frontend/src/app/(main)/feed/search/page.tsx
  - frontend/src/app/(main)/admin/moderation/page.tsx
  - frontend/package.json
  - frontend/__tests__/PostComposer.test.tsx
  - frontend/__tests__/PostCard.test.tsx
  - frontend/__tests__/CommentThread.test.tsx
autonomous: true
requirements:
  - FEED-001
  - FEED-002
  - FEED-003
  - FEED-004
  - FEED-005
  - FEED-006
  - FEED-007
  - FEED-008
  - FEED-010

must_haves:
  truths:
    - "Verified user visiting /feed sees reverse-chronological list of posts in their bairro with infinite scroll"
    - "Clicking 'Novo post' opens composer with category select + body textarea + drag-drop for up to 4 images"
    - "Composer compresses images client-side via browser-image-compression before upload and posts multipart to /api/v1/posts"
    - "Like button toggles with optimistic UI and reconciles with server response"
    - "Post detail /feed/{id} shows threaded comments (root + 1-level replies) and allows create/edit/delete own"
    - "/feed/search text input + category/date/author filters calls /api/v1/search and renders sorted results"
    - "/admin/moderation lists pending reports with approve/remove buttons (admin-only route guard)"
    - "Header bell shows unread notification count, dropdown lists last 10, SignalR 'notification' event pushes new items live"
  artifacts:
    - path: packages/shared-api-client/src/feed.ts
      provides: "createFeedClient(axios) with listFeed, getPost, createPost(FormData), updatePost, deletePost, toggleLike, listComments, createComment, updateComment, deleteComment, search, createReport, listPendingReports, resolveReport, listNotifications, markRead, markAllRead"
    - path: packages/shared-validators/src/feed.ts
      provides: "zod schemas createPostSchema, createCommentSchema, createReportSchema, searchSchema"
    - path: frontend/src/app/(main)/feed/page.tsx
      provides: "Bairro feed with infinite scroll + composer entry point"
    - path: frontend/src/app/(main)/feed/[id]/page.tsx
      provides: "Post detail + threaded comments"
    - path: frontend/src/app/(main)/feed/search/page.tsx
      provides: "Search page with filters"
    - path: frontend/src/app/(main)/admin/moderation/page.tsx
      provides: "Admin moderation queue"
    - path: frontend/src/components/features/NotificationBell.tsx
      provides: "Header bell with SignalR live updates"
    - path: frontend/src/lib/signalr.ts
      provides: "createHubConnection(baseUrl, tokenFactory) using @microsoft/signalr HubConnectionBuilder"
  key_links:
    - from: frontend/src/components/features/PostComposer.tsx
      to: packages/shared-api-client/src/feed.ts
      via: "feedClient.createPost(formData) where formData has category, body, images[]"
      pattern: "feedClient\\.createPost|createPost\\(formData"
    - from: frontend/src/components/features/PostComposer.tsx
      to: "browser-image-compression"
      via: "imageCompression(file, { maxSizeMB: 1, maxWidthOrHeight: 1600 }) before append to FormData"
      pattern: "browser-image-compression|imageCompression"
    - from: frontend/src/app/(main)/feed/page.tsx
      to: frontend/src/stores/feed-store.ts
      via: "zustand store with items[], cursor, loading; loadMore() calls feedClient.listFeed({bairroId, cursor})"
      pattern: "useFeedStore|zustand"
    - from: frontend/src/lib/signalr.ts
      to: "@microsoft/signalr"
      via: "new HubConnectionBuilder().withUrl(`${baseUrl}/hubs/notifications`, { accessTokenFactory }).withAutomaticReconnect().build()"
      pattern: "HubConnectionBuilder|@microsoft/signalr"
    - from: frontend/src/components/features/NotificationBell.tsx
      to: frontend/src/lib/signalr.ts
      via: "hub.on('notification', dto => notificationStore.prepend(dto))"
      pattern: "hub\\.on\\(.notification"
    - from: frontend/src/components/features/PostCard.tsx
      to: "date-fns"
      via: "formatDistanceToNow(new Date(createdAt), { locale: ptBR, addSuffix: true })"
      pattern: "date-fns|formatDistanceToNow"
---

<objective>
Build the Next.js web feed experience consuming the 03-01 backend: shared API client + zod validators extensions, feed page with infinite scroll, post composer with client-side image compression, post detail with threaded comments, search page, admin moderation queue, and a live notification bell wired to SignalR.

Purpose: Deliver the daily-engagement loop UI so verified Vila Velha residents can read and interact with their bairro feed.
Output: 4 pages, 7 feature components, 2 stores, SignalR client helper, shared client + validator extensions, Jest + RTL tests.
</objective>

<execution_context>
@C:/Users/acq20/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/acq20/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
@.planning/phases/03-feed-posts/03-01-PLAN.md
@.planning/phases/02-verification-neighborhoods/02-02-SUMMARY.md
@packages/shared-api-client/src/index.ts
@packages/shared-validators/src/index.ts
@packages/shared-types/src/feed.ts
@frontend/src/app/(main)/layout.tsx
@frontend/src/app/(main)/feed/page.tsx
@frontend/src/app/(main)/admin/verifications/page.tsx
@frontend/src/app/(main)/onboarding/cep/page.tsx
@frontend/src/components/VerifiedBadge.tsx
@frontend/src/components/ProofDropzone.tsx
@frontend/src/lib/api.ts
@frontend/src/lib/auth.ts

<interfaces>
Existing shared-api-client (packages/shared-api-client/src/index.ts) exposes `createApiClient({baseURL, tokenStore})` returning an AxiosInstance with JWT interceptors. It also exports domain sub-clients (auth, verification, profile) as functions taking the axios instance. Add new file feed.ts exporting `createFeedClient(client: AxiosInstance)` with methods listed in must_haves, and re-export from packages/shared-api-client/src/index.ts.

Existing shared-validators (packages/shared-validators/src/index.ts) already uses zod v4 style. Follow the same file-per-domain convention — create packages/shared-validators/src/feed.ts.

DTO types are imported from `@bairronow/shared-types` — `PostDto`, `CreatePostRequest`, `CommentDto`, `FeedPageDto`, `ReportDto`, `NotificationDto`, `PostCategory`, `ReportReason`, `SearchRequest` (already produced by 03-01).

SignalR hub URL: `${NEXT_PUBLIC_API_BASE_URL}/hubs/notifications` (existing MapHub path from Phase 1). Access token via tokenFactory that reads from zustand auth store.

Existing auth store (frontend/src/lib/auth.ts or stores/auth) exposes useAuthStore() returning { user, accessToken, bairroId, isVerified, isAdmin }. Mirror how admin/verifications/page.tsx gates access.

Existing admin page pattern (frontend/src/app/(main)/admin/verifications/page.tsx): client component, 'use client', uses feedClient-like calls, renders table, approve/reject buttons. Mirror for /admin/moderation.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: shared-api-client feed module + shared-validators feed schemas + signalr helper + feed+notification zustand stores</name>
  <files>
    packages/shared-api-client/src/feed.ts,
    packages/shared-api-client/src/index.ts,
    packages/shared-validators/src/feed.ts,
    packages/shared-validators/src/index.ts,
    frontend/src/lib/signalr.ts,
    frontend/src/lib/feed.ts,
    frontend/src/stores/feed-store.ts,
    frontend/src/stores/notification-store.ts,
    frontend/package.json
  </files>
  <read_first>
    - packages/shared-api-client/src/index.ts (existing createApiClient + existing domain-client export pattern, e.g. createAuthClient/createVerificationClient)
    - packages/shared-validators/src/index.ts (existing zod schema style)
    - packages/shared-types/src/feed.ts (types produced by 03-01 — do NOT redefine)
    - packages/shared-types/src/verification.ts (sibling to mirror structure)
    - frontend/src/lib/api.ts (how axios instance is built + tokenStore wiring)
    - frontend/src/lib/auth.ts (zustand auth store shape)
    - frontend/src/lib/verification.ts (how a lib/*.ts wrapper instantiates a sub-client)
    - frontend/package.json (current deps — need to add `@microsoft/signalr`, `browser-image-compression`, `react-dropzone`, `date-fns` if missing)
  </read_first>
  <behavior>
    - packages/shared-api-client/src/feed.ts exports `createFeedClient(client: AxiosInstance)` with methods:
      * listFeed(params: { bairroId: number; cursor?: string; take?: number }): Promise<FeedPageDto> → GET /api/v1/posts
      * getPost(id: number): Promise<PostDto & { comments: CommentDto[] }> → GET /api/v1/posts/{id}
      * createPost(form: FormData): Promise<PostDto> → POST /api/v1/posts with Content-Type multipart/form-data
      * updatePost(id: number, body: string): Promise<PostDto> → PUT /api/v1/posts/{id}
      * deletePost(id: number): Promise<void> → DELETE /api/v1/posts/{id}
      * toggleLike(postId: number): Promise<{ liked: boolean; count: number }> → POST /api/v1/posts/{postId}/like
      * listWhoLiked(postId: number) → GET /api/v1/posts/{postId}/like/who
      * listComments(postId: number) → GET /api/v1/comments/by-post/{postId}
      * createComment(req: CreateCommentRequest) → POST /api/v1/comments
      * updateComment(id: number, body: string) → PUT /api/v1/comments/{id}
      * deleteComment(id: number) → DELETE /api/v1/comments/{id}
      * search(req: SearchRequest): Promise<{ items: PostDto[]; total: number }> → GET /api/v1/search with query params
      * createReport(req: CreateReportRequest) → POST /api/v1/reports
      * listPendingReports(skip: number, take: number): Promise<ReportDto[]> → GET /api/v1/admin/moderation/reports
      * resolveReport(id: number, action: "dismiss"|"remove", reason?: string) → POST /api/v1/admin/moderation/reports/{id}/resolve
      * listNotifications(): Promise<NotificationDto[]> → GET /api/v1/notifications
      * markRead(id: number) → POST /api/v1/notifications/{id}/read
      * markAllRead() → POST /api/v1/notifications/read-all
    - Re-export from packages/shared-api-client/src/index.ts.
    - packages/shared-validators/src/feed.ts (zod) exports:
      * postCategorySchema = z.enum(["Dica","Alerta","Pergunta","Evento","Geral"])
      * createPostSchema = z.object({ category: postCategorySchema, body: z.string().min(1).max(2000) })
      * createCommentSchema = z.object({ postId: z.number().int().positive(), parentCommentId: z.number().int().positive().nullable(), body: z.string().min(1).max(500) })
      * reportReasonSchema = z.enum(["spam","offensive","discrimination","misinformation","other"])
      * createReportSchema = z.object({ targetType: z.enum(["post","comment"]), targetId: z.number().int().positive(), reason: reportReasonSchema, note: z.string().max(500).nullable() })
      * searchSchema = z.object({ q: z.string().min(1).max(200), category: postCategorySchema.optional(), from: z.string().datetime().optional(), to: z.string().datetime().optional(), authorId: z.number().int().optional() })
    - Re-export from packages/shared-validators/src/index.ts.
    - frontend/src/lib/signalr.ts exports:
      * `createNotificationHub({ baseUrl, getAccessToken }: { baseUrl: string; getAccessToken: () => string | null }): HubConnection` using `new HubConnectionBuilder().withUrl(`${baseUrl}/hubs/notifications`, { accessTokenFactory: () => getAccessToken() ?? "" }).withAutomaticReconnect().build()`.
    - frontend/src/lib/feed.ts: instantiate `feedClient = createFeedClient(apiClient)` once and export.
    - frontend/src/stores/feed-store.ts: zustand store with state `{ items: PostDto[]; cursor: string | null; loading: boolean; hasMore: boolean }` and actions `loadFirst(bairroId)`, `loadMore(bairroId)`, `prependNew(post)`, `updatePost(id, patch)`, `removePost(id)`, `setLiked(id, liked, count)`.
    - frontend/src/stores/notification-store.ts: zustand store `{ items: NotificationDto[]; unread: number }` with actions `load()`, `prepend(dto)`, `markRead(id)`, `markAllRead()`.
    - Add missing deps to frontend/package.json: `@microsoft/signalr@^8.0.0`, `browser-image-compression@^2.0.2`, `react-dropzone@^14.2.3`, `date-fns@^3.6.0`. Use pnpm.
  </behavior>
  <action>
    1. Read the four files in packages/ + frontend/src/lib/{api,auth,verification}.ts to confirm conventions, export naming, and tokenStore shape.
    2. Create packages/shared-api-client/src/feed.ts and re-export from index.ts.
    3. Create packages/shared-validators/src/feed.ts and re-export from index.ts.
    4. From repo root: `pnpm --filter @bairronow/shared-api-client build` and `pnpm --filter @bairronow/shared-validators build` — both must exit 0.
    5. From repo root: `pnpm --filter frontend add @microsoft/signalr browser-image-compression react-dropzone date-fns`.
    6. Create frontend/src/lib/signalr.ts, frontend/src/lib/feed.ts.
    7. Create frontend/src/stores/feed-store.ts and notification-store.ts (zustand). If frontend/src/stores/ doesn't exist, create it.
    8. `pnpm --filter frontend typecheck` (or `pnpm --filter frontend exec tsc --noEmit`) must pass.
  </action>
  <verify>
    <automated>cd /d D:\claude-code\nosso-vizinho &amp;&amp; pnpm --filter @bairronow/shared-api-client build 2>&amp;1 | tail -5 &amp;&amp; pnpm --filter @bairronow/shared-validators build 2>&amp;1 | tail -5 &amp;&amp; pnpm --filter frontend exec tsc --noEmit 2>&amp;1 | tail -15 &amp;&amp; grep -l "createFeedClient" packages/shared-api-client/src/feed.ts &amp;&amp; grep -l "createPostSchema" packages/shared-validators/src/feed.ts &amp;&amp; grep -l "HubConnectionBuilder" frontend/src/lib/signalr.ts &amp;&amp; grep -l "zustand" frontend/src/stores/feed-store.ts &amp;&amp; grep -l "@microsoft/signalr" frontend/package.json &amp;&amp; grep -l "browser-image-compression" frontend/package.json &amp;&amp; grep -l "react-dropzone" frontend/package.json &amp;&amp; grep -l "date-fns" frontend/package.json</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @bairronow/shared-api-client build` exits 0
    - `pnpm --filter @bairronow/shared-validators build` exits 0
    - `pnpm --filter frontend exec tsc --noEmit` exits 0
    - packages/shared-api-client/src/feed.ts contains exact strings `createFeedClient`, `listFeed`, `createPost`, `toggleLike`, `listNotifications`, `search`, `createReport`, `resolveReport`
    - packages/shared-api-client/src/index.ts contains exact string `feed`
    - packages/shared-validators/src/feed.ts contains exact strings `createPostSchema`, `createCommentSchema`, `createReportSchema`, `searchSchema`, `z.enum`
    - packages/shared-validators/src/index.ts contains exact string `feed`
    - frontend/src/lib/signalr.ts contains exact strings `HubConnectionBuilder`, `/hubs/notifications`, `accessTokenFactory`, `withAutomaticReconnect`
    - frontend/src/lib/feed.ts contains exact string `createFeedClient`
    - frontend/src/stores/feed-store.ts contains exact strings `zustand`, `loadMore`, `prependNew`, `setLiked`
    - frontend/src/stores/notification-store.ts contains exact strings `zustand`, `prepend`, `unread`
    - frontend/package.json contains exact strings `@microsoft/signalr`, `browser-image-compression`, `react-dropzone`, `date-fns`
  </acceptance_criteria>
  <done>API client + validators + SignalR helper + stores published and typechecking. UI components in Task 2 can wire them directly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Feed UI — pages, components, composer with image compression, search, moderation, notification bell + Jest tests</name>
  <files>
    frontend/src/components/features/PostCard.tsx,
    frontend/src/components/features/PostComposer.tsx,
    frontend/src/components/features/CommentThread.tsx,
    frontend/src/components/features/LikeButton.tsx,
    frontend/src/components/features/ReportDialog.tsx,
    frontend/src/components/features/NotificationBell.tsx,
    frontend/src/components/layouts/FeedHeader.tsx,
    frontend/src/app/(main)/feed/page.tsx,
    frontend/src/app/(main)/feed/[id]/page.tsx,
    frontend/src/app/(main)/feed/search/page.tsx,
    frontend/src/app/(main)/admin/moderation/page.tsx,
    frontend/__tests__/PostComposer.test.tsx,
    frontend/__tests__/PostCard.test.tsx,
    frontend/__tests__/CommentThread.test.tsx
  </files>
  <read_first>
    - frontend/src/app/(main)/layout.tsx (site header + auth gate pattern)
    - frontend/src/app/(main)/feed/page.tsx (CURRENT placeholder — replace)
    - frontend/src/app/(main)/admin/verifications/page.tsx (table + admin guard pattern to mirror for moderation)
    - frontend/src/app/(main)/onboarding/cep/page.tsx (react-hook-form + zod resolver pattern to mirror for composer)
    - frontend/src/components/ProofDropzone.tsx (existing react-dropzone usage pattern)
    - frontend/src/components/VerifiedBadge.tsx (badge component, reuse inside PostCard author)
    - frontend/src/lib/feed.ts and frontend/src/stores/feed-store.ts (created in Task 1)
    - frontend/src/lib/auth.ts (useAuthStore shape — bairroId, isVerified, isAdmin, accessToken)
  </read_first>
  <behavior>
    - All files 'use client' React components using shadcn/tailwind classes already used in 02-02 (buttons `bg-green-700 hover:bg-green-800 text-white rounded-md px-4 py-2`, cards `bg-white rounded-lg shadow p-4`, inputs `border border-gray-300 rounded-md px-3 py-2 w-full`).
    - PostCard.tsx props: `{ post: PostDto; onLikeChange?: (liked, count) => void; onDelete?: (id) => void }`. Renders: author row (photo + displayName + VerifiedBadge if isVerified + time-ago via `formatDistanceToNow(new Date(post.createdAt), { locale: ptBR, addSuffix: true })` from date-fns), category pill, body (preserve newlines), image grid (1/2/3/4 images → responsive grid), LikeButton, comment count button (links to /feed/{id}), edit/delete menu if user.id === post.author.id, report button (opens ReportDialog). "Editado" indicator if post.isEdited.
    - LikeButton.tsx: optimistic toggle — immediately updates UI then calls feedClient.toggleLike(postId); on error, revert and show toast. Shows heart icon + count.
    - PostComposer.tsx: modal/dialog with react-hook-form + zodResolver(createPostSchema). Fields: category select (5 options), body textarea (max 2000 chars, shows counter), react-dropzone accepting max 4 images (jpeg/png/webp). For each dropped file: run `await imageCompression(file, { maxSizeMB: 1, maxWidthOrHeight: 1600, useWebWorker: true })` then show preview thumbnail. On submit: build FormData with `category`, `body`, and each compressed file appended as `images`; call `feedClient.createPost(form)`; on success: call `useFeedStore.getState().prependNew(post)` and close modal. Block submission if `!useAuthStore.getState().isVerified` (show message "Você precisa estar verificado para postar").
    - CommentThread.tsx: props `{ postId: number; initial: CommentDto[] }`. Renders recursive list (1 level — map comments, each with replies[] rendered indented). Each comment shows author, time-ago, body, like (optional — skip for MVP), reply button (only on root, not on replies), edit/delete own, report. Has a form at top for new root comment (react-hook-form + createCommentSchema); reply form inlines under each root when clicked. Uses feedClient.createComment/updateComment/deleteComment and refetches or mutates local state.
    - ReportDialog.tsx: small modal with reason select (5 options) + note textarea; calls feedClient.createReport on submit.
    - NotificationBell.tsx: header bell icon with unread count badge. On mount: useNotificationStore().load() + createNotificationHub({ baseUrl: NEXT_PUBLIC_API_BASE_URL, getAccessToken: () => useAuthStore.getState().accessToken }).start(); subscribes `hub.on('notification', dto => notificationStore.prepend(dto))`. On unmount: hub.stop(). Clicking bell opens dropdown listing last 10 notifications with "X comentou no seu post" / "X curtiu seu post" / "X mencionou você" — clicking item calls markRead and navigates to /feed/{postId}.
    - FeedHeader.tsx: top bar with "NossoVizinho" brand, bairro name, NotificationBell, link to /feed/search, admin link if user.isAdmin.
    - /feed/page.tsx: 'use client'. Reads bairroId from auth store; if !bairroId redirect to /onboarding/cep. On mount: useFeedStore().loadFirst(bairroId). Renders FeedHeader, "Novo post" button that opens PostComposer, then maps useFeedStore().items → PostCard. IntersectionObserver on sentinel div at list end triggers loadMore(bairroId). Empty state: "Nenhum post ainda no seu bairro".
    - /feed/[id]/page.tsx: fetches post + comments via feedClient.getPost(id). Renders full PostCard (without link to detail) + CommentThread. Handles 404.
    - /feed/search/page.tsx: form with text input, category select (optional), date from/to, "authorId" field (optional numeric). On submit calls feedClient.search(req). Renders results as PostCard list. Shows result count. Uses zod validation from searchSchema.
    - /admin/moderation/page.tsx: admin guard (if !user.isAdmin return "Acesso negado"). Lists pending reports (useEffect → feedClient.listPendingReports(0, 50)); each row shows targetType, targetId (linked to /feed/{id}), reporter email, reason, note, createdAt; two buttons: "Remover conteúdo" → resolveReport(id, "remove"), "Dispensar" → resolveReport(id, "dismiss"). Refreshes after action. Mirrors 02-02 admin/verifications styling.
    - Tests (Jest + React Testing Library — already set up in 02-02):
      * PostComposer.test.tsx: renders; rejects body >2000 chars (zod error); calls feedClient.createPost with FormData containing category+body+file on submit (mock feedClient + browser-image-compression + react-dropzone).
      * PostCard.test.tsx: renders author, body, images, time-ago string matching /há/ (pt-BR); clicking like calls toggleLike; shows "Editado" when isEdited=true.
      * CommentThread.test.tsx: renders root comment and its reply indented; reply button on root visible but hidden on reply (1-level only); submitting new comment calls createComment.
  </behavior>
  <action>
    1. Read the 8 files in read_first.
    2. Create the 7 components in frontend/src/components/features/ (create `features/` if absent — mirror 02-02 structure).
    3. Create FeedHeader in frontend/src/components/layouts/.
    4. Replace frontend/src/app/(main)/feed/page.tsx with the infinite-scroll feed.
    5. Create frontend/src/app/(main)/feed/[id]/page.tsx, feed/search/page.tsx, admin/moderation/page.tsx.
    6. Create the 3 Jest test files under frontend/__tests__/.
    7. Run `pnpm --filter frontend exec tsc --noEmit` then `pnpm --filter frontend test -- --testPathPattern="PostComposer|PostCard|CommentThread"` then `pnpm --filter frontend build`.
  </action>
  <verify>
    <automated>cd /d D:\claude-code\nosso-vizinho &amp;&amp; pnpm --filter frontend exec tsc --noEmit 2>&amp;1 | tail -15 &amp;&amp; pnpm --filter frontend test -- --testPathPattern="PostComposer|PostCard|CommentThread" --watchAll=false 2>&amp;1 | tail -20 &amp;&amp; pnpm --filter frontend build 2>&amp;1 | tail -15 &amp;&amp; grep -l "useFeedStore" frontend/src/app/\(main\)/feed/page.tsx &amp;&amp; grep -l "PostComposer" frontend/src/app/\(main\)/feed/page.tsx &amp;&amp; grep -l "imageCompression" frontend/src/components/features/PostComposer.tsx &amp;&amp; grep -l "react-dropzone" frontend/src/components/features/PostComposer.tsx &amp;&amp; grep -l "createPostSchema" frontend/src/components/features/PostComposer.tsx &amp;&amp; grep -l "feedClient.createPost" frontend/src/components/features/PostComposer.tsx &amp;&amp; grep -l "formatDistanceToNow" frontend/src/components/features/PostCard.tsx &amp;&amp; grep -l "toggleLike" frontend/src/components/features/LikeButton.tsx &amp;&amp; grep -l "createNotificationHub" frontend/src/components/features/NotificationBell.tsx &amp;&amp; grep -l "hub.on" frontend/src/components/features/NotificationBell.tsx &amp;&amp; grep -l "resolveReport" frontend/src/app/\(main\)/admin/moderation/page.tsx &amp;&amp; grep -l "searchSchema" frontend/src/app/\(main\)/feed/search/page.tsx &amp;&amp; grep -l "CommentThread" frontend/src/app/\(main\)/feed/\[id\]/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter frontend exec tsc --noEmit` exits 0
    - `pnpm --filter frontend test -- --testPathPattern="PostComposer|PostCard|CommentThread" --watchAll=false` reports 0 failed, at least 6 tests passed
    - `pnpm --filter frontend build` exits 0 (Next.js static export succeeds)
    - frontend/src/components/features/PostComposer.tsx contains exact strings `imageCompression`, `react-dropzone`, `createPostSchema`, `FormData`, `feedClient.createPost`, `maxWidthOrHeight: 1600`
    - frontend/src/components/features/PostCard.tsx contains exact strings `formatDistanceToNow`, `ptBR` or `pt-BR`, `VerifiedBadge`, `LikeButton`
    - frontend/src/components/features/LikeButton.tsx contains exact strings `toggleLike`, `optimistic` or `setLiked`
    - frontend/src/components/features/CommentThread.tsx contains exact strings `createComment`, `ParentCommentId` or `parentCommentId`, `replies`
    - frontend/src/components/features/ReportDialog.tsx contains exact string `createReport`
    - frontend/src/components/features/NotificationBell.tsx contains exact strings `createNotificationHub`, `hub.on('notification'` or `hub.on("notification"`, `unread`
    - frontend/src/app/(main)/feed/page.tsx contains exact strings `useFeedStore`, `loadMore`, `PostComposer`, `IntersectionObserver` or `sentinel`
    - frontend/src/app/(main)/feed/[id]/page.tsx contains exact strings `getPost`, `CommentThread`
    - frontend/src/app/(main)/feed/search/page.tsx contains exact strings `searchSchema`, `feedClient.search`
    - frontend/src/app/(main)/admin/moderation/page.tsx contains exact strings `listPendingReports`, `resolveReport`, `isAdmin`
    - frontend/__tests__/PostComposer.test.tsx, PostCard.test.tsx, CommentThread.test.tsx exist
  </acceptance_criteria>
  <done>Web feed fully functional: verified user can post/read/comment/like/search in their bairro, admin can moderate, SignalR notification bell lights up on new activity. Build green, tests green.</done>
</task>

</tasks>

<verification>
Manual end-to-end smoke after both plans executed and deployed:
1. Log in as verified user (bairroId=1), visit /feed — see empty state.
2. Click "Novo post", select category "Dica", type body, drop 2 jpg images → submit → post appears at top.
3. Open a second browser (user B, same bairro) — sees the post in their feed.
4. User B likes the post → user A's notification bell badge increments; click bell, see "B curtiu seu post".
5. User B comments "ótima dica!" → user A bell increments again; click comment → navigates to /feed/{id}; user A replies.
6. User A edits own post within 30min → saves, "Editado" appears.
7. Visit /feed/search?q=dica → result appears.
8. User B reports the post → admin visits /admin/moderation, sees pending report, clicks "Dispensar" → list empties.
9. Post with body containing offensive word → backend IsPublished=false, does not appear in /feed for either user.
</verification>

<success_criteria>
- 9 FEED requirements (001,002,003,004,005,006,007,008,010) have working UI
- Infinite scroll feed scoped to caller's bairro
- Post composer compresses images client-side and uploads multipart (max 4)
- Threaded comments (1 level) working
- Optimistic like button
- Search page with filters functional
- Admin moderation queue functional
- SignalR notification bell live-updates
- All typechecks + tests + Next.js build green
</success_criteria>

<output>
After completion, create `.planning/phases/03-feed-posts/03-02-SUMMARY.md` documenting pages, components, store shapes, SignalR wiring, key deviations, and screenshots/paths for manual QA.
</output>
