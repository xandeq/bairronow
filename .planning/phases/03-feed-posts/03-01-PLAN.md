---
phase: 03-feed-posts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/NossoVizinho.Api/Models/Entities/Post.cs
  - src/NossoVizinho.Api/Models/Entities/PostImage.cs
  - src/NossoVizinho.Api/Models/Entities/Comment.cs
  - src/NossoVizinho.Api/Models/Entities/PostLike.cs
  - src/NossoVizinho.Api/Models/Entities/Report.cs
  - src/NossoVizinho.Api/Models/Entities/Notification.cs
  - src/NossoVizinho.Api/Models/Enums/PostCategory.cs
  - src/NossoVizinho.Api/Models/Enums/ReportReason.cs
  - src/NossoVizinho.Api/Data/AppDbContext.cs
  - src/NossoVizinho.Api/Migrations/
  - src/NossoVizinho.Api/Services/IOffensiveWordFilter.cs
  - src/NossoVizinho.Api/Services/OffensiveWordFilter.cs
  - src/NossoVizinho.Api/Services/IFeedQueryService.cs
  - src/NossoVizinho.Api/Services/FeedQueryService.cs
  - src/NossoVizinho.Api/Services/IPostService.cs
  - src/NossoVizinho.Api/Services/PostService.cs
  - src/NossoVizinho.Api/Services/ICommentService.cs
  - src/NossoVizinho.Api/Services/CommentService.cs
  - src/NossoVizinho.Api/Services/ILikeService.cs
  - src/NossoVizinho.Api/Services/LikeService.cs
  - src/NossoVizinho.Api/Services/IModerationService.cs
  - src/NossoVizinho.Api/Services/ModerationService.cs
  - src/NossoVizinho.Api/Services/INotificationService.cs
  - src/NossoVizinho.Api/Services/NotificationService.cs
  - src/NossoVizinho.Api/Services/FileStorageService.cs
  - src/NossoVizinho.Api/Controllers/v1/PostsController.cs
  - src/NossoVizinho.Api/Controllers/v1/CommentsController.cs
  - src/NossoVizinho.Api/Controllers/v1/LikesController.cs
  - src/NossoVizinho.Api/Controllers/v1/SearchController.cs
  - src/NossoVizinho.Api/Controllers/v1/ReportsController.cs
  - src/NossoVizinho.Api/Controllers/v1/ModerationController.cs
  - src/NossoVizinho.Api/Controllers/v1/NotificationsController.cs
  - src/NossoVizinho.Api/Validators/CreatePostRequestValidator.cs
  - src/NossoVizinho.Api/Validators/CreateCommentRequestValidator.cs
  - src/NossoVizinho.Api/Validators/CreateReportRequestValidator.cs
  - src/NossoVizinho.Api/Hubs/NotificationHub.cs
  - src/NossoVizinho.Api/Program.cs
  - packages/shared-types/src/feed.ts
  - packages/shared-types/src/index.ts
  - tests/NossoVizinho.Api.Tests/Services/OffensiveWordFilterTests.cs
  - tests/NossoVizinho.Api.Tests/Services/PostServiceTests.cs
  - tests/NossoVizinho.Api.Tests/Services/CommentServiceTests.cs
autonomous: true
requirements:
  - FEED-001
  - FEED-002
  - FEED-003
  - FEED-004
  - FEED-005
  - FEED-006
  - FEED-008
  - FEED-009
  - FEED-010

must_haves:
  truths:
    - "Verified user can POST /api/v1/posts with up to 4 images and receive {id, category, body, imageUrls[], createdAt}"
    - "GET /api/v1/posts?bairroId&cursor&take returns reverse-chronological list scoped to caller's bairro"
    - "GET /api/v1/posts/{id} returns post with comments tree (1-level threading) and like count"
    - "PUT /api/v1/posts/{id} allowed only for author within 30min window; sets EditedAt"
    - "DELETE /api/v1/posts/{id} soft-deletes (sets DeletedAt) and hides via global query filter"
    - "POST /api/v1/posts/{id}/like toggles like and returns {liked:bool, count:int}"
    - "POST /api/v1/comments creates comment or reply (ParentCommentId for 1-level thread)"
    - "Offensive word in Post.Body sets IsFlagged=true and IsPublished=false before insert"
    - "POST /api/v1/reports enqueues report; GET /api/v1/admin/moderation/reports lists pending"
    - "Creating a comment/like/mention persists a Notification row and calls NotificationHub.Clients.User(authorId).SendAsync('notification', dto)"
    - "GET /api/v1/search?q&category&from&to&authorId returns relevance-ordered posts using SQL LIKE/CONTAINS"
  artifacts:
    - path: src/NossoVizinho.Api/Models/Entities/Post.cs
      provides: "Post entity with Id, AuthorId, BairroId, Category, Body, ImageUrls (via PostImage), IsFlagged, IsPublished, EditedAt, DeletedAt, CreatedAt"
    - path: src/NossoVizinho.Api/Models/Entities/Comment.cs
      provides: "Comment entity (Id, PostId, AuthorId, ParentCommentId nullable, Body, EditedAt, DeletedAt, CreatedAt)"
    - path: src/NossoVizinho.Api/Models/Entities/PostLike.cs
      provides: "Composite key (PostId, UserId) + CreatedAt"
    - path: src/NossoVizinho.Api/Models/Entities/Report.cs
      provides: "Report entity (Id, ReporterId, TargetType enum Post/Comment, TargetId, Reason enum, Note, Status, ResolvedAt)"
    - path: src/NossoVizinho.Api/Models/Entities/Notification.cs
      provides: "Notification entity (Id, UserId, Type, PostId, CommentId, ActorUserId, IsRead, CreatedAt)"
    - path: src/NossoVizinho.Api/Services/OffensiveWordFilter.cs
      provides: "pt-BR wordlist + Contains(text) returning bool, used pre-insert"
    - path: src/NossoVizinho.Api/Controllers/v1/PostsController.cs
      provides: "POST/GET/PUT/DELETE /api/v1/posts and /api/v1/posts/{id}"
    - path: src/NossoVizinho.Api/Controllers/v1/CommentsController.cs
      provides: "POST/PUT/DELETE /api/v1/comments"
    - path: src/NossoVizinho.Api/Controllers/v1/SearchController.cs
      provides: "GET /api/v1/search"
    - path: src/NossoVizinho.Api/Controllers/v1/ModerationController.cs
      provides: "Admin moderation queue endpoints"
    - path: packages/shared-types/src/feed.ts
      provides: "PostDto, CreatePostRequest, CommentDto, FeedPageDto, ReportDto, NotificationDto, PostCategory, ReportReason"
  key_links:
    - from: src/NossoVizinho.Api/Services/PostService.cs
      to: src/NossoVizinho.Api/Services/FileStorageService.cs
      via: "SaveImageAsync(stream) reused from Phase 2 with ImageSharp resize, called once per IFormFile (max 4)"
      pattern: "SaveImageAsync|SixLabors"
    - from: src/NossoVizinho.Api/Services/PostService.cs
      to: src/NossoVizinho.Api/Services/IOffensiveWordFilter.cs
      via: "Contains(body) => sets IsFlagged=true, IsPublished=false"
      pattern: "IOffensiveWordFilter"
    - from: src/NossoVizinho.Api/Services/CommentService.cs
      to: src/NossoVizinho.Api/Services/INotificationService.cs
      via: "After comment created, NotifyCommentAsync(postAuthorId, actorId, postId, commentId)"
      pattern: "NotifyCommentAsync|_notifications"
    - from: src/NossoVizinho.Api/Services/NotificationService.cs
      to: src/NossoVizinho.Api/Hubs/NotificationHub.cs
      via: "IHubContext<NotificationHub>.Clients.User(userId.ToString()).SendAsync(\"notification\", dto)"
      pattern: "IHubContext<NotificationHub>"
    - from: src/NossoVizinho.Api/Services/FeedQueryService.cs
      to: src/NossoVizinho.Api/Data/AppDbContext.cs
      via: "db.Posts.AsNoTracking().Where(p => p.BairroId == caller.BairroId && p.IsPublished).OrderByDescending(p => p.CreatedAt)"
      pattern: "BairroId == .*\\.BairroId"
    - from: src/NossoVizinho.Api/Program.cs
      to: src/NossoVizinho.Api/Services/OffensiveWordFilter.cs
      via: "AddSingleton<IOffensiveWordFilter, OffensiveWordFilter>() and RequireAuthenticatedUser policy 'VerifiedOnly' checking claim is_verified=true"
      pattern: "AddSingleton<IOffensiveWordFilter|VerifiedOnly"
---

<objective>
Build the backend feed stack for Phase 3: Post/Comment/Like/Report/Notification entities, EF migration, PostService with ImageSharp multi-image pipeline (reuse FileStorageService from 02), CommentService with 1-level threading, LikeService, SearchController (SQL LIKE), offensive-word filter gate, moderation queue, in-app notifications via existing NotificationHub, rate limiting, FluentValidation, xUnit tests.

Purpose: Provide all endpoints and real-time hooks the Next.js feed UI (03-02) will consume.
Output: 7 controllers, 6 services, 6 entities, 1 migration, shared TS types, unit tests.
</objective>

<execution_context>
@C:/Users/acq20/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/acq20/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/02-verification-neighborhoods/02-01-SUMMARY.md
@src/NossoVizinho.Api/Program.cs
@src/NossoVizinho.Api/Data/AppDbContext.cs
@src/NossoVizinho.Api/Controllers/v1/VerificationController.cs
@src/NossoVizinho.Api/Controllers/v1/AdminVerificationController.cs
@src/NossoVizinho.Api/Services/FileStorageService.cs
@src/NossoVizinho.Api/Services/VerificationService.cs
@src/NossoVizinho.Api/Hubs/NotificationHub.cs
@src/NossoVizinho.Api/Models/Entities/User.cs
@src/NossoVizinho.Api/Validators/RegisterRequestValidator.cs

<interfaces>
Existing NotificationHub (src/NossoVizinho.Api/Hubs/NotificationHub.cs):
```csharp
[Authorize]
public class NotificationHub : Hub
{
    public async Task JoinBairro(string bairroId) { ... }
}
```
Extend: no new methods required — use IHubContext<NotificationHub>.Clients.User(userId.ToString()).SendAsync("notification", payload).

Existing FileStorageService (from 02-01): already validates images (jpeg/png/webp), resizes via ImageSharp to 1600px JPEG q85, saves to wwwroot/uploads/{folder}/{yyyy}/{MM}/{guid}.jpg, returns (relativePath, sha256). Reuse for post images; new folder key "posts" → wwwroot/uploads/posts/{yyyy}/{MM}/.

Existing User has: Id, BairroId (int?), IsVerified (bool), IsAdmin (bool). JWT already carries is_admin claim. Add is_verified claim issuance in TokenService (or load from DB in authorization handler) so [Authorize(Policy="VerifiedOnly")] can gate FEED writes.

New shared-types (packages/shared-types/src/feed.ts) MUST export:
```ts
export type PostCategory = "Dica" | "Alerta" | "Pergunta" | "Evento" | "Geral";
export type ReportReason = "spam" | "offensive" | "discrimination" | "misinformation" | "other";
export type ReportTargetType = "post" | "comment";
export interface PostImageDto { url: string; order: number; }
export interface PostAuthorDto { id: number; displayName: string | null; photoUrl: string | null; isVerified: boolean; }
export interface PostDto {
  id: number;
  author: PostAuthorDto;
  bairroId: number;
  category: PostCategory;
  body: string;
  images: PostImageDto[];
  likeCount: number;
  commentCount: number;
  likedByMe: boolean;
  isEdited: boolean;
  createdAt: string;
  editedAt: string | null;
}
export interface CreatePostRequest { category: PostCategory; body: string; } // images sent as multipart files[]
export interface FeedPageDto { items: PostDto[]; nextCursor: string | null; }
export interface CommentDto {
  id: number;
  postId: number;
  parentCommentId: number | null;
  author: PostAuthorDto;
  body: string;
  createdAt: string;
  editedAt: string | null;
  replies: CommentDto[];
}
export interface CreateCommentRequest { postId: number; parentCommentId: number | null; body: string; }
export interface CreateReportRequest { targetType: ReportTargetType; targetId: number; reason: ReportReason; note: string | null; }
export interface ReportDto { id: number; targetType: ReportTargetType; targetId: number; reason: ReportReason; note: string | null; reporterEmail: string; createdAt: string; status: "pending" | "resolved" | "dismissed"; }
export type NotificationType = "comment" | "reply" | "like" | "mention";
export interface NotificationDto { id: number; type: NotificationType; postId: number | null; commentId: number | null; actor: PostAuthorDto; isRead: boolean; createdAt: string; }
export interface SearchRequest { q: string; category?: PostCategory; from?: string; to?: string; authorId?: number; skip?: number; take?: number; }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Feed entities, enums, DbContext wiring, EF migration, offensive-word filter service, FileStorageService image reuse helper</name>
  <files>
    src/NossoVizinho.Api/Models/Enums/PostCategory.cs,
    src/NossoVizinho.Api/Models/Enums/ReportReason.cs,
    src/NossoVizinho.Api/Models/Entities/Post.cs,
    src/NossoVizinho.Api/Models/Entities/PostImage.cs,
    src/NossoVizinho.Api/Models/Entities/Comment.cs,
    src/NossoVizinho.Api/Models/Entities/PostLike.cs,
    src/NossoVizinho.Api/Models/Entities/Report.cs,
    src/NossoVizinho.Api/Models/Entities/Notification.cs,
    src/NossoVizinho.Api/Data/AppDbContext.cs,
    src/NossoVizinho.Api/Services/IOffensiveWordFilter.cs,
    src/NossoVizinho.Api/Services/OffensiveWordFilter.cs,
    src/NossoVizinho.Api/Services/FileStorageService.cs,
    src/NossoVizinho.Api/Services/IFileStorageService.cs,
    src/NossoVizinho.Api/Migrations/,
    tests/NossoVizinho.Api.Tests/Services/OffensiveWordFilterTests.cs
  </files>
  <read_first>
    - src/NossoVizinho.Api/Data/AppDbContext.cs (existing DbSets, OnModelCreating patterns, HasQueryFilter soft-delete pattern from Verification)
    - src/NossoVizinho.Api/Models/Entities/Verification.cs (soft-delete + IsDeleted column style to mirror)
    - src/NossoVizinho.Api/Models/Entities/User.cs (FK navigation patterns)
    - src/NossoVizinho.Api/Services/FileStorageService.cs (existing SaveProofAsync — add SaveImageAsync("posts") variant)
    - src/NossoVizinho.Api/Services/IFileStorageService.cs (interface contract to extend)
    - src/NossoVizinho.Api/Migrations/ (migration file naming convention)
    - tests/NossoVizinho.Api.Tests/ (test project structure, existing xUnit conventions)
  </read_first>
  <behavior>
    - PostCategory enum values: Dica, Alerta, Pergunta, Evento, Geral (stored as string via HasConversion<string>()).
    - ReportReason enum: Spam, Offensive, Discrimination, Misinformation, Other.
    - Post entity columns: Id int PK, AuthorId int FK Users, BairroId int FK Bairros, Category PostCategory, Body nvarchar(2000) required, IsFlagged bool default false, IsPublished bool default true, RestrictedToVerified bool default false, EditedAt datetime2 nullable, DeletedAt datetime2 nullable, CreatedAt datetime2 default GETUTCDATE.
    - PostImage entity: Id int PK, PostId int FK Posts, Url nvarchar(500), Order int (0..3). Unique (PostId, Order). Cascade delete.
    - Comment entity: Id int PK, PostId int FK Posts, AuthorId int FK Users, ParentCommentId int? FK Comments (self, Restrict delete), Body nvarchar(500) required, EditedAt datetime2?, DeletedAt datetime2?, CreatedAt datetime2.
    - PostLike entity: composite PK (PostId, UserId), CreatedAt datetime2.
    - Report entity: Id int PK, ReporterId int FK Users, TargetType nvarchar(16) ("post"|"comment"), TargetId int, Reason ReportReason (string), Note nvarchar(500)?, Status nvarchar(16) default "pending", ResolvedByUserId int?, ResolvedAt datetime2?, CreatedAt datetime2.
    - Notification entity: Id int PK, UserId int FK Users (recipient), Type nvarchar(16) ("comment"|"reply"|"like"|"mention"), PostId int?, CommentId int?, ActorUserId int FK Users, IsRead bool default false, CreatedAt datetime2.
    - AppDbContext: add DbSet<Post>, DbSet<PostImage>, DbSet<Comment>, DbSet<PostLike>, DbSet<Report>, DbSet<Notification>. In OnModelCreating: enum-to-string conversions, HasQueryFilter on Post (p => p.DeletedAt == null), on Comment (c => c.DeletedAt == null), composite key on PostLike, index on Post.BairroId+CreatedAt desc, index on Post.Body (no full-text — plain LIKE), index on Comment.PostId, index on Notification.UserId+IsRead.
    - Migration `AddFeedEntities` generated; `db.Database.Migrate()` in Program.cs already applies it at startup (from 02-01).
    - OffensiveWordFilter: singleton, loads pt-BR wordlist from embedded const array (minimum 30 entries covering slurs + common Brazilian insults — use starter list: "idiota","imbecil","burro","otario","lixo","filho da puta","fdp","puta","merda","caralho","porra","cu","viado","retardado","retardada","racista","preto safado","macaco","nigger","nazi","nazista","hitler","bicha","sapatao","gorda nojenta","feia","mongoloide","aborto","vagabunda","corno"). Method `bool Contains(string text)` does case-insensitive whole-word regex match. Method `string[] FindMatches(string text)` returns matched terms. Must be deterministic and pure.
    - FileStorageService: add `Task<string> SaveImageAsync(Stream content, string originalFileName, string contentType, string folder)` where folder ∈ {"proofs","posts"}. Reuse existing ImageSharp pipeline; returns relative URL `/uploads/{folder}/{yyyy}/{MM}/{guid}.jpg`. Enforce 5MB and allowed types ["image/jpeg","image/png","image/webp"]. No PDF for posts.
    - OffensiveWordFilterTests (xUnit): [Fact] Contains_returns_true_for_known_slur; [Fact] Contains_returns_false_for_clean_text("Bom dia vizinhos"); [Theory] with 5 samples.
  </behavior>
  <action>
    1. Create enum files under src/NossoVizinho.Api/Models/Enums/ (create folder).
    2. Create the 6 entity files under src/NossoVizinho.Api/Models/Entities/ mirroring naming/namespace `NossoVizinho.Api.Models.Entities` from Verification.cs.
    3. Edit src/NossoVizinho.Api/Data/AppDbContext.cs: add DbSets, extend OnModelCreating with the relationships, indexes, query filters, and enum conversions listed above. Do NOT touch existing Bairro/Verification config.
    4. Extend IFileStorageService and FileStorageService with SaveImageAsync(stream, fileName, contentType, folder). Refactor the private ImageSharp helper so it takes a folder argument; keep existing SaveProofAsync working (delegate to SaveImageAsync with folder="proofs" for images, keep the PDF branch).
    5. Create IOffensiveWordFilter interface and OffensiveWordFilter singleton with embedded wordlist constant + Regex word-boundary matching.
    6. Run from D:\claude-code\nosso-vizinho: `dotnet ef migrations add AddFeedEntities --project src/NossoVizinho.Api`.
    7. Create tests/NossoVizinho.Api.Tests/Services/OffensiveWordFilterTests.cs with xUnit [Fact]/[Theory] tests covering Contains() and FindMatches().
    8. `dotnet build src/NossoVizinho.Api` and `dotnet test tests/NossoVizinho.Api.Tests --filter FullyQualifiedName~OffensiveWordFilterTests`.
  </action>
  <verify>
    <automated>cd /d D:\claude-code\nosso-vizinho &amp;&amp; dotnet build src/NossoVizinho.Api -c Debug 2>&amp;1 | tail -10 &amp;&amp; dotnet test tests/NossoVizinho.Api.Tests --filter FullyQualifiedName~OffensiveWordFilterTests --nologo 2>&amp;1 | tail -15 &amp;&amp; ls src/NossoVizinho.Api/Migrations/*AddFeedEntities*.cs</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build src/NossoVizinho.Api` exits 0
    - `dotnet test ... OffensiveWordFilterTests` reports 0 failed
    - src/NossoVizinho.Api/Models/Enums/PostCategory.cs contains exact strings `Dica`, `Alerta`, `Pergunta`, `Evento`, `Geral`
    - src/NossoVizinho.Api/Models/Entities/Post.cs contains exact strings `public class Post`, `BairroId`, `Category`, `Body`, `IsFlagged`, `IsPublished`, `EditedAt`, `DeletedAt`, `RestrictedToVerified`
    - src/NossoVizinho.Api/Models/Entities/Comment.cs contains exact strings `public class Comment`, `ParentCommentId`
    - src/NossoVizinho.Api/Models/Entities/PostLike.cs contains exact string `public class PostLike`
    - src/NossoVizinho.Api/Models/Entities/Report.cs contains exact strings `public class Report`, `TargetType`, `Reason`
    - src/NossoVizinho.Api/Models/Entities/Notification.cs contains exact strings `public class Notification`, `ActorUserId`, `IsRead`
    - src/NossoVizinho.Api/Data/AppDbContext.cs contains exact strings `DbSet<Post>`, `DbSet<Comment>`, `DbSet<PostLike>`, `DbSet<Report>`, `DbSet<Notification>`, `HasQueryFilter`
    - src/NossoVizinho.Api/Services/OffensiveWordFilter.cs contains exact strings `IOffensiveWordFilter`, `Contains`, `Regex`
    - src/NossoVizinho.Api/Services/FileStorageService.cs contains exact string `SaveImageAsync` and `wwwroot/uploads/posts`
    - A migration file matching `src/NossoVizinho.Api/Migrations/*AddFeedEntities*.cs` exists
  </acceptance_criteria>
  <done>Schema migrated with feed tables, offensive-word filter unit-tested green, FileStorageService ready to save post images, build green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Feed services + controllers + SignalR notifications + validators + rate limiting + shared types</name>
  <files>
    src/NossoVizinho.Api/Services/IFeedQueryService.cs,
    src/NossoVizinho.Api/Services/FeedQueryService.cs,
    src/NossoVizinho.Api/Services/IPostService.cs,
    src/NossoVizinho.Api/Services/PostService.cs,
    src/NossoVizinho.Api/Services/ICommentService.cs,
    src/NossoVizinho.Api/Services/CommentService.cs,
    src/NossoVizinho.Api/Services/ILikeService.cs,
    src/NossoVizinho.Api/Services/LikeService.cs,
    src/NossoVizinho.Api/Services/IModerationService.cs,
    src/NossoVizinho.Api/Services/ModerationService.cs,
    src/NossoVizinho.Api/Services/INotificationService.cs,
    src/NossoVizinho.Api/Services/NotificationService.cs,
    src/NossoVizinho.Api/Controllers/v1/PostsController.cs,
    src/NossoVizinho.Api/Controllers/v1/CommentsController.cs,
    src/NossoVizinho.Api/Controllers/v1/LikesController.cs,
    src/NossoVizinho.Api/Controllers/v1/SearchController.cs,
    src/NossoVizinho.Api/Controllers/v1/ReportsController.cs,
    src/NossoVizinho.Api/Controllers/v1/ModerationController.cs,
    src/NossoVizinho.Api/Controllers/v1/NotificationsController.cs,
    src/NossoVizinho.Api/Validators/CreatePostRequestValidator.cs,
    src/NossoVizinho.Api/Validators/CreateCommentRequestValidator.cs,
    src/NossoVizinho.Api/Validators/CreateReportRequestValidator.cs,
    src/NossoVizinho.Api/Program.cs,
    packages/shared-types/src/feed.ts,
    packages/shared-types/src/index.ts,
    tests/NossoVizinho.Api.Tests/Services/PostServiceTests.cs,
    tests/NossoVizinho.Api.Tests/Services/CommentServiceTests.cs
  </files>
  <read_first>
    - src/NossoVizinho.Api/Controllers/v1/VerificationController.cs (multipart IFormFile pattern, [Authorize], route prefix /api/v1)
    - src/NossoVizinho.Api/Controllers/v1/AdminVerificationController.cs ([Authorize(Policy="Admin")] pattern, pagination shape)
    - src/NossoVizinho.Api/Services/VerificationService.cs (DI, AppDbContext usage, AsNoTracking, AuditLog write pattern)
    - src/NossoVizinho.Api/Program.cs (rate-limit policies "public"/"auth", AddScoped pattern, JWT policy registration, SignalR MapHub, claim "is_admin" — add "VerifiedOnly" policy checking claim "is_verified" OR DB check)
    - src/NossoVizinho.Api/Services/TokenService.cs (add is_verified claim issuance mirroring is_admin)
    - src/NossoVizinho.Api/Validators/RegisterRequestValidator.cs (FluentValidation style)
    - src/NossoVizinho.Api/Hubs/NotificationHub.cs
    - packages/shared-types/src/index.ts (export pattern)
    - packages/shared-types/src/verification.ts (pattern to mirror for feed.ts)
    - tests/NossoVizinho.Api.Tests/ (existing in-memory DB / Bogus patterns if any)
  </read_first>
  <behavior>
    - TokenService: emit `new Claim("is_verified", user.IsVerified.ToString().ToLower())` alongside existing claims.
    - Program.cs: register `AddSingleton<IOffensiveWordFilter, OffensiveWordFilter>()`, AddScoped for FeedQueryService, PostService, CommentService, LikeService, ModerationService, NotificationService. Add authorization policy `"VerifiedOnly"` requiring claim `is_verified == "true"`. Add rate-limit policy `"feed-write"` SlidingWindowLimiter(permitLimit=10, window=60s) partitioned by user id; apply via [EnableRateLimiting("feed-write")] on create-post/create-comment/like/report endpoints.
    - FeedQueryService.GetBairroFeedAsync(callerUserId, bairroId, cursor, take=20): validates caller.BairroId == bairroId (throw 403 otherwise); queries Posts.AsNoTracking().Include(p => p.Author).Include(p => p.Images).Where(p => p.BairroId == bairroId && p.IsPublished).OrderByDescending(p => p.CreatedAt).ThenByDescending(p => p.Id); cursor = base64({createdAtTicks}:{id}); nextCursor for pagination. Returns FeedPageDto. LikedByMe resolved via subquery over PostLikes.
    - FeedQueryService.GetPostAsync(callerId, postId): returns PostDto + comments tree (1-level: root comments + replies list).
    - FeedQueryService.SearchAsync(callerId, SearchRequest): LIKE match on Body (`EF.Functions.Like(p.Body, "%"+q+"%")` case-insensitive), optional filters on Category, CreatedAt range, AuthorId. Scoped to caller.BairroId. Relevance: ORDER BY (CASE WHEN Body LIKE '% q %' THEN 2 WHEN Body LIKE 'q%' THEN 1 ELSE 0 END) DESC, CreatedAt DESC. Pagination via skip/take.
    - PostService.CreateAsync(authorId, CreatePostRequest dto, IFormFile[] images (max 4)): loads author, guards authorId.IsVerified (return 403 if not) and author.BairroId != null. Validates dto via injected IValidator<CreatePostRequest>. Runs offensiveFilter.Contains(dto.Body) → sets IsFlagged=true, IsPublished=false (else IsPublished=true). Iterates images (max 4, reject 5th): for each, calls _fileStorage.SaveImageAsync(stream, fileName, contentType, "posts") → collects PostImage { Url=relative, Order=i }. Persists Post + Images in one SaveChangesAsync. Returns PostDto mapped from new row.
    - PostService.UpdateAsync(authorId, postId, body): loads tracked post; guard post.AuthorId == authorId (else 403); guard DateTime.UtcNow - post.CreatedAt <= 30min (else 400 "edit window expired"); run offensive filter again; set post.Body, post.EditedAt = UtcNow; save.
    - PostService.DeleteAsync(authorId, postId): load, guard ownership, set post.DeletedAt = UtcNow, save. No hard delete.
    - CommentService.CreateAsync(authorId, CreateCommentRequest): guard verified + same bairro as post; if ParentCommentId != null, load parent and require parent.ParentCommentId == null (1-level only — else 400 "nested replies not allowed"); run offensive filter (if hit → still saved but flagged? For comments, reject with 422 to keep MVP simple); persist comment. After save, call _notifications.NotifyCommentAsync(post.AuthorId, authorId, post.Id, comment.Id) unless authorId == post.AuthorId. If reply, also notify parent comment author with type="reply". Also scan body for @mentions (regex @[A-Za-z0-9_.-]{3,}) → resolve to User.DisplayName exact match → NotifyMentionAsync.
    - CommentService.UpdateAsync / DeleteAsync: same ownership + EditedAt / DeletedAt pattern as PostService.
    - LikeService.ToggleAsync(userId, postId): if row exists in PostLikes → remove; else add. Returns (liked, count). If liked and userId != post.AuthorId → NotifyLikeAsync.
    - NotificationService: `NotifyCommentAsync/NotifyReplyAsync/NotifyLikeAsync/NotifyMentionAsync(recipientId, actorId, postId, commentId?)`. Inserts Notification row, then calls `_hub.Clients.User(recipientId.ToString()).SendAsync("notification", dto)`. Inject IHubContext<NotificationHub>.
    - ModerationService.ListPendingReportsAsync(skip, take): Reports where Status="pending", include reporter email, Post/Comment snippet. Admin only.
    - ModerationService.ResolveAsync(adminId, reportId, action: "dismiss"|"remove"): if remove → set target Post.DeletedAt or Comment.DeletedAt. Always sets Report.Status, ResolvedByUserId, ResolvedAt.
    - Controllers:
      - PostsController `[ApiController] [Route("api/v1/posts")]`: POST "" `[Authorize(Policy="VerifiedOnly")] [EnableRateLimiting("feed-write")]` accepts [FromForm] CreatePostRequest + IFormFileCollection images; GET "" `[Authorize]` query `bairroId`, `cursor`, `take`; GET "{id:int}" `[Authorize]`; PUT "{id:int}" `[Authorize]` body `{body}`; DELETE "{id:int}" `[Authorize]`.
      - CommentsController `[Route("api/v1/comments")]`: POST "", PUT "{id:int}", DELETE "{id:int}" — all `[Authorize(Policy="VerifiedOnly")]` (except DELETE own own allowed for all authors). GET "by-post/{postId:int}" returns tree.
      - LikesController `[Route("api/v1/posts/{postId:int}/like")]`: POST toggles, GET "who" returns list of users who liked.
      - SearchController `[Route("api/v1/search")]`: GET "" `[Authorize]` binding SearchRequest.
      - ReportsController `[Route("api/v1/reports")]`: POST "" `[Authorize] [EnableRateLimiting("feed-write")]` body CreateReportRequest.
      - ModerationController `[Route("api/v1/admin/moderation")]` `[Authorize(Policy="Admin")]`: GET "reports", POST "reports/{id:int}/resolve" body `{action, reason}`.
      - NotificationsController `[Route("api/v1/notifications")] [Authorize]`: GET "" (list last 50), POST "{id:int}/read", POST "read-all".
    - Validators (FluentValidation):
      - CreatePostRequestValidator: Category required ∈ enum values; Body NotEmpty, Length(1,2000).
      - CreateCommentRequestValidator: PostId > 0; Body NotEmpty, Length(1,500); ParentCommentId null or > 0.
      - CreateReportRequestValidator: TargetType ∈ {"post","comment"}; TargetId > 0; Reason ∈ enum; Note MaxLength(500).
      - Registered via existing `AddFluentValidationAutoValidation()` pattern in Program.cs (or add if missing).
    - AuditLog: write audit rows on create/edit/delete post, on moderation resolve, reusing AuditLogs table from 01.
    - shared-types: create packages/shared-types/src/feed.ts with the TS interfaces in the <interfaces> block above. Add `export * from './feed';` to packages/shared-types/src/index.ts.
    - PostServiceTests (xUnit + in-memory SQLite or EF InMemory): [Fact] Create_sets_IsPublished_true_when_body_clean; [Fact] Create_sets_IsFlagged_and_IsPublished_false_when_offensive; [Fact] Update_throws_when_not_author; [Fact] Update_throws_when_outside_30min_window; [Fact] Delete_soft_deletes (DeletedAt set).
    - CommentServiceTests: [Fact] Create_comment_calls_NotifyComment_with_post_author (verify via Moq on INotificationService); [Fact] Reply_to_reply_rejected; [Fact] Reply_to_root_allowed.
  </behavior>
  <action>
    1. Read the files in read_first carefully — especially Program.cs (rate-limit policy names), TokenService.cs (claim issuance site), VerificationController.cs (route + IFormFile pattern).
    2. Extend TokenService to add is_verified claim.
    3. Create all 6 service interface+implementation pairs under src/NossoVizinho.Api/Services/.
    4. Create all 7 controllers under src/NossoVizinho.Api/Controllers/v1/.
    5. Create 3 FluentValidation validator classes under src/NossoVizinho.Api/Validators/.
    6. Edit Program.cs: AddScoped/AddSingleton registrations, AddAuthorization policy "VerifiedOnly" requiring claim "is_verified" == "true", rate-limit policy "feed-write" (SlidingWindowLimiter, 10 permits / 60s partitioned by ClaimTypes.NameIdentifier). Keep existing MapHub&lt;NotificationHub&gt;. Ensure FluentValidation auto-registration covers new validators (should via assembly scan — verify).
    7. Create packages/shared-types/src/feed.ts and export from index.ts.
    8. Create PostServiceTests.cs and CommentServiceTests.cs using EF Core InMemory (`Microsoft.EntityFrameworkCore.InMemory` — add NuGet package ref to test project if missing) + Moq for INotificationService + IFileStorageService + IOffensiveWordFilter.
    9. Build + test + shared-types build:
       `dotnet build src/NossoVizinho.Api`
       `dotnet test tests/NossoVizinho.Api.Tests --filter "FullyQualifiedName~PostServiceTests|FullyQualifiedName~CommentServiceTests|FullyQualifiedName~OffensiveWordFilterTests"`
       `pnpm --filter @bairronow/shared-types build`
  </action>
  <verify>
    <automated>cd /d D:\claude-code\nosso-vizinho &amp;&amp; dotnet build src/NossoVizinho.Api -c Debug 2>&amp;1 | tail -10 &amp;&amp; dotnet test tests/NossoVizinho.Api.Tests --filter "FullyQualifiedName~PostServiceTests|FullyQualifiedName~CommentServiceTests" --nologo 2>&amp;1 | tail -15 &amp;&amp; pnpm --filter @bairronow/shared-types build 2>&amp;1 | tail -5 &amp;&amp; grep -l "/api/v1/posts" src/NossoVizinho.Api/Controllers/v1/PostsController.cs &amp;&amp; grep -l "/api/v1/comments" src/NossoVizinho.Api/Controllers/v1/CommentsController.cs &amp;&amp; grep -l "/api/v1/search" src/NossoVizinho.Api/Controllers/v1/SearchController.cs &amp;&amp; grep -l "/api/v1/admin/moderation" src/NossoVizinho.Api/Controllers/v1/ModerationController.cs &amp;&amp; grep -l "/api/v1/reports" src/NossoVizinho.Api/Controllers/v1/ReportsController.cs &amp;&amp; grep -l "/api/v1/notifications" src/NossoVizinho.Api/Controllers/v1/NotificationsController.cs &amp;&amp; grep -l "VerifiedOnly" src/NossoVizinho.Api/Program.cs &amp;&amp; grep -l "feed-write" src/NossoVizinho.Api/Program.cs &amp;&amp; grep -l "is_verified" src/NossoVizinho.Api/Services/TokenService.cs &amp;&amp; grep -l "PostDto" packages/shared-types/src/feed.ts</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build src/NossoVizinho.Api` exits 0
    - `dotnet test ... PostServiceTests|CommentServiceTests` reports 0 failed and at least 5 passed tests
    - `pnpm --filter @bairronow/shared-types build` exits 0
    - src/NossoVizinho.Api/Controllers/v1/PostsController.cs contains exact strings `api/v1/posts`, `IFormFileCollection`, `[Authorize(Policy = "VerifiedOnly")]` or `Policy="VerifiedOnly"`, `EnableRateLimiting("feed-write")`
    - src/NossoVizinho.Api/Controllers/v1/CommentsController.cs contains exact strings `api/v1/comments`, `ParentCommentId`
    - src/NossoVizinho.Api/Controllers/v1/LikesController.cs contains exact string `like`
    - src/NossoVizinho.Api/Controllers/v1/SearchController.cs contains exact strings `api/v1/search`, `EF.Functions.Like` or `LIKE`
    - src/NossoVizinho.Api/Controllers/v1/ReportsController.cs contains exact string `api/v1/reports`
    - src/NossoVizinho.Api/Controllers/v1/ModerationController.cs contains exact strings `api/v1/admin/moderation`, `Policy = "Admin"` or `Policy="Admin"`
    - src/NossoVizinho.Api/Controllers/v1/NotificationsController.cs contains exact string `api/v1/notifications`
    - src/NossoVizinho.Api/Services/PostService.cs contains exact strings `IOffensiveWordFilter`, `SaveImageAsync`, `IsFlagged`, `IsPublished`, `EditedAt`, `DeletedAt`, `30`
    - src/NossoVizinho.Api/Services/CommentService.cs contains exact strings `ParentCommentId`, `NotifyCommentAsync`, `@`
    - src/NossoVizinho.Api/Services/NotificationService.cs contains exact strings `IHubContext<NotificationHub>`, `Clients.User`, `SendAsync("notification"`
    - src/NossoVizinho.Api/Services/FeedQueryService.cs contains exact strings `BairroId`, `OrderByDescending`, `AsNoTracking`
    - src/NossoVizinho.Api/Validators/CreatePostRequestValidator.cs contains exact strings `AbstractValidator<CreatePostRequest>`, `2000`
    - src/NossoVizinho.Api/Validators/CreateCommentRequestValidator.cs contains exact string `500`
    - src/NossoVizinho.Api/Program.cs contains exact strings `AddPolicy("VerifiedOnly"`, `is_verified`, `feed-write`, `AddScoped<IPostService, PostService>`, `AddSingleton<IOffensiveWordFilter`
    - src/NossoVizinho.Api/Services/TokenService.cs contains exact string `is_verified`
    - packages/shared-types/src/feed.ts contains exact strings `PostDto`, `CreatePostRequest`, `CommentDto`, `FeedPageDto`, `ReportDto`, `NotificationDto`, `PostCategory`
    - packages/shared-types/src/index.ts contains exact string `feed`
  </acceptance_criteria>
  <done>All feed endpoints implemented with validation + rate limiting, SignalR notifications dispatched, tests green, shared types published. Web feed (03-02) can consume.</done>
</task>

</tasks>

<verification>
End-to-end smoke after `dotnet run`:
1. Register + verify (via admin flip) a user with BairroId=1.
2. `curl -X POST http://localhost:5000/api/v1/posts -H "Authorization: Bearer $JWT" -F "category=Dica" -F "body=Teste de post" -F "images=@test.jpg"` → 200 with PostDto.
3. `curl http://localhost:5000/api/v1/posts?bairroId=1` → contains the post.
4. `curl -X POST http://localhost:5000/api/v1/posts/1/like` → `{liked:true,count:1}`.
5. `curl -X POST http://localhost:5000/api/v1/comments -d '{"postId":1,"parentCommentId":null,"body":"bom!"}'` → 200; SignalR client receives "notification" event.
6. POST with body containing an offensive word → Post row exists but IsPublished=false, IsFlagged=true.
7. `curl http://localhost:5000/api/v1/search?q=teste` → returns the post.
</verification>

<success_criteria>
- 9 FEED requirements (001,002,003,004,005,006,008,009,010) backed by endpoints + services
- SignalR notifications fire for comment/reply/like/mention
- Offensive filter auto-flags pre-publish
- Rate limiting active on write endpoints
- Unit tests green (offensive filter + post + comment)
- shared-types exports feed module
- Build green
</success_criteria>

<output>
After completion, create `.planning/phases/03-feed-posts/03-01-SUMMARY.md` documenting entities, endpoints, SignalR contract, rate-limit policy, wordlist source, and any deviations.
</output>
