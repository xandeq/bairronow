---
phase: 04-marketplace-chat
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - mobile/package.json
  - mobile/src/features/marketplace/MarketplaceScreen.tsx
  - mobile/src/features/marketplace/ListingDetailScreen.tsx
  - mobile/src/features/marketplace/CreateListingScreen.tsx
  - mobile/src/features/marketplace/EditListingScreen.tsx
  - mobile/src/features/marketplace/components/ListingCard.tsx
  - mobile/src/features/marketplace/components/FilterChips.tsx
  - mobile/src/features/marketplace/components/CategoryPicker.tsx
  - mobile/src/features/marketplace/components/PhotoPicker.tsx
  - mobile/src/features/marketplace/components/ReportListingSheet.tsx
  - mobile/src/features/marketplace/components/RatingForm.tsx
  - mobile/src/features/chat/ConversationListScreen.tsx
  - mobile/src/features/chat/ChatScreen.tsx
  - mobile/src/features/chat/components/MessageBubble.tsx
  - mobile/src/features/chat/components/MessageComposer.tsx
  - mobile/src/lib/signalr.ts
  - mobile/src/lib/api/marketplace.ts
  - mobile/src/lib/api/chat.ts
  - mobile/src/lib/validators/listing.ts
  - mobile/src/stores/marketplaceStore.ts
  - mobile/src/stores/chatStore.ts
  - mobile/src/navigation/BottomTabs.tsx
  - mobile/src/features/marketplace/__tests__/marketplace-grid.test.tsx
  - mobile/src/features/chat/__tests__/chat-screen.test.tsx
autonomous: true
requirements: [MKT-001, MKT-002, MKT-003, MKT-004, MKT-005, MKT-006, MKT-007, MKT-008, MKT-009, MKT-010, MKT-011]

must_haves:
  truths:
    - "Verified mobile user sees a 2-column marketplace grid scoped to their bairro"
    - "User can create a listing from mobile with 1-6 photos picked via expo-image-picker and compressed via expo-image-manipulator"
    - "User opens listing detail, taps 'Chat com vendedor', enters chat room with forced-WebSocket SignalR transport that works in EAS release builds (not just Expo Go)"
    - "SignalR reconnects automatically when app backgrounds/foregrounds without crashing on iOS"
    - "User can report a listing and rate a seller from mobile"
  artifacts:
    - path: "mobile/src/lib/signalr.ts"
      provides: "Forced-WebSocket HubConnection with skipNegotiation + withAutomaticReconnect, shared across chat + notifications"
      contains: "HttpTransportType.WebSockets"
    - path: "mobile/src/features/chat/ChatScreen.tsx"
      provides: "Chat room using shared SignalR connection"
      contains: "JoinConversation"
    - path: "mobile/src/features/marketplace/components/PhotoPicker.tsx"
      provides: "Expo image picker + manipulator pipeline (reuses Phase 02-03 ProofPicker pattern)"
      contains: "ImageManipulator"
    - path: "mobile/src/stores/chatStore.ts"
      provides: "Zustand store mirroring web chatStore"
      contains: "messagesByConversation"
  key_links:
    - from: "mobile/src/lib/signalr.ts"
      to: "api.bairronow.com.br /hubs/notifications"
      via: "HubConnectionBuilder with transport: HttpTransportType.WebSockets + skipNegotiation: true"
      pattern: "skipNegotiation.*true"
    - from: "mobile/src/features/marketplace/components/PhotoPicker.tsx"
      to: "expo-image-picker + expo-image-manipulator"
      via: "same pipeline as Phase 02-03 ProofPicker"
      pattern: "ImageManipulator\\.manipulateAsync"
    - from: "mobile/src/features/chat/ChatScreen.tsx"
      to: "chatStore"
      via: "useChatStore selector"
      pattern: "chatStore|useChatStore"
---

<objective>
Mobile (Expo / React Native) frontend for Phase 4: marketplace grid + filters, listing create/detail/edit (reusing Phase 02-03 ProofPicker photo pipeline), 1:1 chat with SignalR forced to WebSocket transport (mandatory for EAS release builds), report and rating flows.

Purpose: Parity with web features on mobile with extra care for Expo-specific SignalR gotchas documented in RESEARCH.md Pitfall 1 + 2.

Output: Expo screens + components + tests. Release build smoke test (manual) documented in VALIDATION.md.
</objective>

<execution_context>
@C:/Users/acq20/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/acq20/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-marketplace-chat/04-CONTEXT.md
@.planning/phases/04-marketplace-chat/04-RESEARCH.md
@.planning/phases/04-marketplace-chat/04-01-PLAN.md
@.planning/phases/02-verification-neighborhoods/02-03-PLAN.md
@mobile/src/features/verification/components/ProofPicker.tsx
@mobile/src/lib/api/client.ts
@mobile/src/stores/authStore.ts
@CLAUDE.md

<interfaces>
<!-- Shared with web 04-02 — DTOs and endpoints from 04-01 -->
Same DTO shapes as web/04-02 (Listing, Message, Conversation).
Same REST endpoints (base URL from `EXPO_PUBLIC_API_URL`).

Expo-specific dependency (needs install):
- `@microsoft/signalr@8.0.7` (not yet in mobile/package.json per Phase 3 state)

Already installed from Phase 02-03:
- `expo-image-picker`
- `expo-image-manipulator`
- `zustand`, `react-hook-form`, `zod`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 0 (Wave 0): Install SignalR, test scaffolds, API clients, Zustand stores, validators, forced-WebSocket signalr.ts</name>
  <files>
    mobile/package.json,
    mobile/src/lib/signalr.ts,
    mobile/src/lib/api/marketplace.ts,
    mobile/src/lib/api/chat.ts,
    mobile/src/lib/validators/listing.ts,
    mobile/src/stores/marketplaceStore.ts,
    mobile/src/stores/chatStore.ts,
    mobile/src/features/marketplace/__tests__/marketplace-grid.test.tsx,
    mobile/src/features/chat/__tests__/chat-screen.test.tsx
  </files>
  <read_first>
    - .planning/phases/04-marketplace-chat/04-RESEARCH.md §"Common Pitfalls" Pitfalls 1, 2 (Expo SignalR gotchas — VERBATIM config), §"Code Examples"
    - .planning/phases/04-marketplace-chat/04-01-PLAN.md (DTOs + endpoints)
    - .planning/phases/02-verification-neighborhoods/02-03-PLAN.md (Expo patterns, ProofPicker)
    - mobile/src/lib/api/client.ts (existing axios + JWT refresh)
    - mobile/src/stores/authStore.ts (existing Zustand pattern)
    - CLAUDE.md §"Frontend Libraries" + Expo section
  </read_first>
  <behavior>
    - `jest-expo` test runs show stub suites compile.
    - `mobile/src/lib/signalr.ts` exports a singleton `getHubConnection()` with forced-WebSocket config.
    - Validators + API clients mirror web 04-02 exactly.
  </behavior>
  <action>
    1. Install: `cd mobile && npm install @microsoft/signalr@8.0.7`. Commit package.json + lockfile.
    2. **`mobile/src/lib/signalr.ts`** — singleton module. COPY verbatim from RESEARCH.md Pitfall 1 block:
       ```ts
       import {
         HubConnection,
         HubConnectionBuilder,
         HttpTransportType,
         LogLevel,
       } from "@microsoft/signalr";
       import { authStore } from "@/stores/authStore";

       let connection: HubConnection | null = null;

       export async function getHubConnection(): Promise<HubConnection> {
         if (connection && connection.state === "Connected") return connection;
         if (!connection) {
           connection = new HubConnectionBuilder()
             .withUrl(`${process.env.EXPO_PUBLIC_API_URL}/hubs/notifications`, {
               transport: HttpTransportType.WebSockets,      // force WebSockets — Pitfall 1
               skipNegotiation: true,                         // skip problematic negotiate step
               accessTokenFactory: () => authStore.getState().accessToken ?? "",
             })
             .withAutomaticReconnect([0, 2000, 5000, 10000, 30000])  // Pitfall 2 — never call start() from onclose
             .configureLogging(LogLevel.Warning)
             .build();

           // DO NOT call connection.start() inside onclose — withAutomaticReconnect handles it (Pitfall 2)
           connection.onclose((err) => {
             console.warn("[signalr] closed", err);
             // intentionally empty — automatic reconnect handles recovery
           });
         }
         if (connection.state === "Disconnected") {
           await connection.start();
         }
         return connection;
       }

       export async function disposeHubConnection() {
         if (connection) {
           await connection.stop();
           connection = null;
         }
       }
       ```
    3. **`mobile/src/lib/api/marketplace.ts`** and **`mobile/src/lib/api/chat.ts`** — mirror web 04-02 Task 0 but use `mobile/src/lib/api/client.ts` axios. For file uploads use `FormData` with `{ uri, name, type }` RN shape, not `File`.
    4. **`mobile/src/lib/validators/listing.ts`** — same zod schema as web, but `photos: z.array(z.object({ uri: z.string(), name: z.string(), type: z.string() })).min(1).max(6)` since RN uses asset URIs.
    5. **`mobile/src/stores/marketplaceStore.ts`** and **`mobile/src/stores/chatStore.ts`** — mirror web shapes. `chatStore.connect()` imports `getHubConnection` from `@/lib/signalr`.
    6. Test stubs with `jest-expo` preset and `@testing-library/react-native`. Mock `@microsoft/signalr` module. Stubs marked `.skip` until screens exist.
    7. Update VALIDATION.md per-task map rows for mobile tests.
  </action>
  <verify>
    <automated>cd mobile && npx jest --testPathPattern="marketplace|chat" --passWithNoTests</automated>
  </verify>
  <acceptance_criteria>
    - `npm install @microsoft/signalr@8.0.7` succeeded and package.json updated
    - `grep -n "HttpTransportType.WebSockets" mobile/src/lib/signalr.ts` matches
    - `grep -n "skipNegotiation: true" mobile/src/lib/signalr.ts` matches
    - `grep -n "withAutomaticReconnect" mobile/src/lib/signalr.ts` matches
    - `grep -A2 "onclose" mobile/src/lib/signalr.ts` does NOT contain `.start()` (Pitfall 2)
    - Jest test discovery exits 0
  </acceptance_criteria>
  <done>SignalR configured correctly for Expo, API clients + stores + validators + test stubs ready.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Marketplace mobile screens (grid, detail, create, edit, report, rating) + ProofPicker-pattern PhotoPicker</name>
  <files>
    mobile/src/features/marketplace/MarketplaceScreen.tsx,
    mobile/src/features/marketplace/ListingDetailScreen.tsx,
    mobile/src/features/marketplace/CreateListingScreen.tsx,
    mobile/src/features/marketplace/EditListingScreen.tsx,
    mobile/src/features/marketplace/components/ListingCard.tsx,
    mobile/src/features/marketplace/components/FilterChips.tsx,
    mobile/src/features/marketplace/components/CategoryPicker.tsx,
    mobile/src/features/marketplace/components/PhotoPicker.tsx,
    mobile/src/features/marketplace/components/ReportListingSheet.tsx,
    mobile/src/features/marketplace/components/RatingForm.tsx,
    mobile/src/navigation/BottomTabs.tsx
  </files>
  <read_first>
    - .planning/phases/04-marketplace-chat/04-CONTEXT.md §decisions (D-01..D-11, D-17..D-20, D-22, D-23, D-24, D-25)
    - mobile/src/features/verification/components/ProofPicker.tsx (Phase 02-03 — REPLICATE pattern for PhotoPicker)
    - mobile/src/features/verification/ (Phase 02-03 screens for visual consistency)
    - .planning/phases/02-verification-neighborhoods/02-03-PLAN.md
    - .planning/phases/04-marketplace-chat/04-02-PLAN.md (web Task 1 — match feature parity)
    - mobile/src/components/ui/VerifiedBadge.tsx (Phase 02-03 — reuse)
  </read_first>
  <behavior>
    - marketplace-grid.test.tsx: renders listings from mocked API, scrolling triggers pagination.
    - CreateListing flow: PhotoPicker returns 1-6 compressed assets, zod validates, submit calls createListing with multipart.
  </behavior>
  <action>
    1. **`PhotoPicker.tsx`** — REPLICATE Phase 02-03 ProofPicker pattern:
       - `ImagePicker.launchImageLibraryAsync({ mediaTypes: Images, allowsMultipleSelection: true, selectionLimit: 6 - existing.length, quality: 1 })`
       - For each asset: `ImageManipulator.manipulateAsync(asset.uri, [{ resize: { width: 1920 } }], { compress: 0.85, format: SaveFormat.JPEG })`
       - Return `{ uri, name: 'listing-{guid}.jpg', type: 'image/jpeg' }` shape for FormData
       - UI: grid of thumbnails, first marked "Capa" per D-01, tap to remove, reorder via long-press drag or arrow buttons
       - Enforce min 1, max 6
    2. **`ListingCard.tsx`** — 2-col grid card (D-05): `Image` with aspect-ratio 1, title `numberOfLines={2}`, price formatted pt-BR BRL via `Intl.NumberFormat`, VerifiedBadge, "VENDIDO" overlay if Sold.
    3. **`MarketplaceScreen.tsx`** — FlatList `numColumns={2}`, header with search + FilterChips, FAB "Novo anúncio" if user verified. Pull to refresh. Infinite scroll via `onEndReached`.
    4. **`FilterChips.tsx`** — horizontal ScrollView of chips (category, verified default ON per D-10, price range opens bottom sheet).
    5. **`CategoryPicker.tsx`** — 2-step chip grid modal per D-03, NO pickers/dropdowns.
    6. **`CreateListingScreen.tsx`** — react-hook-form + zodResolver. Verified-only guard. Submit via `createListing` with multipart FormData.
    7. **`EditListingScreen.tsx`** — same form, edit mode, seller-only.
    8. **`ListingDetailScreen.tsx`** — photo carousel (`react-native-pager-view` or FlatList horizontal), title, price, description, seller row with VerifiedBadge + rating, favorite heart, "Chat com vendedor" button → calls `createConversation(listingId)` then `navigation.navigate('ChatScreen', { conversationId })`. Owner actions: Edit, Mark sold, Remove. Buyer post-sale: RatingForm. Report via ReportListingSheet.
    9. **`ReportListingSheet.tsx`** — bottom sheet with 4 reasons from D-20 + optional details.
    10. **`RatingForm.tsx`** — 1-5 star tappable row + comment TextInput.
    11. Add Marketplace tab to `BottomTabs.tsx`.
    12. Un-skip marketplace-grid.test.tsx; simulate FlatList rendering with mocked API response.
  </action>
  <verify>
    <automated>cd mobile && npx jest --testPathPattern="marketplace"</automated>
  </verify>
  <acceptance_criteria>
    - Marketplace test passes
    - `grep -n "ImageManipulator.manipulateAsync" mobile/src/features/marketplace/components/PhotoPicker.tsx` matches
    - `grep -n "selectionLimit" mobile/src/features/marketplace/components/PhotoPicker.tsx` matches
    - `grep -n "numColumns={2}" mobile/src/features/marketplace/MarketplaceScreen.tsx` matches (D-05)
    - `grep -n "Intl.NumberFormat.*pt-BR.*BRL" mobile/src/features/marketplace/components/ListingCard.tsx` matches
    - `grep -rn "a combinar" mobile/src/features/marketplace/` returns nothing (D-02)
    - Marketplace tab registered in BottomTabs
  </acceptance_criteria>
  <done>Mobile marketplace screens complete, photo picker reuses Phase 02-03 pattern, tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Chat screens + SignalR hook + release-build verification scaffolding</name>
  <files>
    mobile/src/features/chat/ConversationListScreen.tsx,
    mobile/src/features/chat/ChatScreen.tsx,
    mobile/src/features/chat/components/MessageBubble.tsx,
    mobile/src/features/chat/components/MessageComposer.tsx
  </files>
  <read_first>
    - .planning/phases/04-marketplace-chat/04-RESEARCH.md §Pitfall 1, §Pitfall 2 (Expo SignalR release-build + iOS onclose)
    - .planning/phases/04-marketplace-chat/04-CONTEXT.md §decisions (D-12..D-16)
    - mobile/src/lib/signalr.ts (Task 0 — already configured correctly)
    - mobile/src/stores/chatStore.ts (Task 0)
    - .planning/phases/04-marketplace-chat/04-01-PLAN.md (hub methods + broadcast events)
    - .planning/phases/04-marketplace-chat/04-VALIDATION.md §"Manual-Only Verifications" (document EAS release-build smoke test)
  </read_first>
  <behavior>
    - chat-screen.test.tsx: mounts ChatScreen with mocked hub, injects a MessageReceived event, new bubble renders.
    - On unmount, LeaveConversation invoked.
    - AppState change (background → foreground) does NOT crash and does not manually call `connection.start()` anywhere (Pitfall 2).
  </behavior>
  <action>
    1. **`ChatScreen.tsx`**:
       - On focus: `const conn = await getHubConnection(); await conn.invoke("JoinConversation", conversationId); chatStore.setActiveConversationId(conversationId); chatStore.markRead(conversationId)`
       - On blur/unmount: `await conn.invoke("LeaveConversation", conversationId); chatStore.setActiveConversationId(null)`
       - Register `conn.on("MessageReceived", msg => chatStore.appendMessage(msg))` once (guard double-registration)
       - Register `conn.on("UnreadChanged", ({ total }) => chatStore.setUnreadTotal(total))`
       - **DO NOT** call `conn.start()` in an `onclose` or `AppState` handler (Pitfall 2). Let `withAutomaticReconnect` handle it.
       - Render: inverted FlatList of messages (load more on scroll-to-top via getMessageHistory cursor), KeyboardAvoidingView + MessageComposer at bottom.
    2. **`MessageBubble.tsx`** — own messages right-aligned green-700, others left gray. Text OR image (with `expo-image` for caching). Timestamp via date-fns pt-BR.
    3. **`MessageComposer.tsx`** — TextInput + attach photo button (ImagePicker single asset, manipulated to 1920 width 85% quality). Send calls `chatStore.sendMessage(conversationId, text, image)`. Optimistic append.
    4. **`ConversationListScreen.tsx`** — FlatList of conversations sorted by lastMessageAt DESC with unread badge. Tap → ChatScreen.
    5. Register chat tab in BottomTabs with unread badge bound to `chatStore.unreadTotal`.
    6. Un-skip chat-screen.test.tsx: mock `@microsoft/signalr`, fire MessageReceived handler, assert Zustand append + new message renders.
    7. Add entries to VALIDATION.md §"Manual-Only Verifications" with the EXACT EAS smoke steps:
       - `eas build --profile preview --platform ios`
       - Install on physical device
       - Open chat, send message, background app 60s, foreground, send another message → must deliver without crash and without manual reconnect
  </action>
  <verify>
    <automated>cd mobile && npx jest --testPathPattern="chat"</automated>
  </verify>
  <acceptance_criteria>
    - chat-screen.test.tsx passes
    - `grep -rn "connection.start()" mobile/src/features/chat/ mobile/src/lib/signalr.ts` shows start() ONLY inside getHubConnection initial bootstrap (not inside onclose, not inside AppState listener)
    - `grep -n "JoinConversation" mobile/src/features/chat/ChatScreen.tsx` matches
    - `grep -n "LeaveConversation" mobile/src/features/chat/ChatScreen.tsx` matches
    - `grep -rn "new HubConnectionBuilder" mobile/src/features/chat/` returns nothing (uses shared signalr.ts singleton)
    - VALIDATION.md manual-only section updated with EAS smoke steps
  </acceptance_criteria>
  <done>Mobile chat works against forced-WebSocket SignalR, tests green, EAS release-build smoke test documented.</done>
</task>

</tasks>

<verification>
- `npx jest` exits 0 across all mobile Phase 4 tests
- Manual EAS release-build smoke test documented in VALIDATION.md and ready to run once EAS credentials available
- No manual `connection.start()` inside onclose or AppState handlers anywhere
- Single HubConnection singleton reused across features
</verification>

<success_criteria>
- Mobile user can browse marketplace grid, create listing (with photo picker), open detail, mark sold, edit, delete, report, rate
- Mobile chat delivers live messages in Expo Go AND (per manual verify) EAS release build
- SignalR auto-reconnects after backgrounding without crashing
- Visual parity with web 04-02 and Phase 02-03 mobile screens
</success_criteria>

<output>
After completion, create `.planning/phases/04-marketplace-chat/04-03-SUMMARY.md` covering: screens created, SignalR config confirmation (forced WebSocket + skipNegotiation + autoReconnect + no manual restart), EAS smoke test result (or pending flag), any deviations.
</output>
