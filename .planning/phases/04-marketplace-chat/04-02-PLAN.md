---
phase: 04-marketplace-chat
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - web/src/app/marketplace/page.tsx
  - web/src/app/marketplace/new/page.tsx
  - web/src/app/marketplace/[id]/page.tsx
  - web/src/app/marketplace/[id]/edit/page.tsx
  - web/src/app/chat/page.tsx
  - web/src/app/chat/[conversationId]/page.tsx
  - web/src/app/admin/moderation/page.tsx
  - web/src/app/admin/categories/page.tsx
  - web/src/components/marketplace/ListingCard.tsx
  - web/src/components/marketplace/ListingGrid.tsx
  - web/src/components/marketplace/FilterChips.tsx
  - web/src/components/marketplace/CategoryPicker.tsx
  - web/src/components/marketplace/PhotoDropzone.tsx
  - web/src/components/marketplace/ListingForm.tsx
  - web/src/components/marketplace/ListingDetailGallery.tsx
  - web/src/components/marketplace/ReportListingDialog.tsx
  - web/src/components/marketplace/RatingForm.tsx
  - web/src/components/chat/ConversationList.tsx
  - web/src/components/chat/ChatRoom.tsx
  - web/src/components/chat/MessageBubble.tsx
  - web/src/components/chat/MessageComposer.tsx
  - web/src/lib/api/marketplace.ts
  - web/src/lib/api/chat.ts
  - web/src/lib/validators/listing.ts
  - web/src/lib/validators/rating.ts
  - web/src/stores/marketplaceStore.ts
  - web/src/stores/chatStore.ts
  - web/src/hooks/useSignalRChat.ts
  - web/src/app/marketplace/__tests__/grid.test.tsx
  - web/src/app/marketplace/__tests__/listing-detail.test.tsx
  - web/src/app/marketplace/__tests__/create-listing.test.tsx
  - web/src/app/chat/__tests__/chat-room.test.tsx
autonomous: true
requirements: [MKT-001, MKT-002, MKT-003, MKT-004, MKT-005, MKT-006, MKT-007, MKT-008, MKT-009, MKT-010, MKT-011]

must_haves:
  truths:
    - "Verified user sees a 2-column marketplace grid scoped to their bairro with top filter chips (category, verified, price)"
    - "User can create a listing via dropzone (1-6 photos, reorder, first = cover), category chip picker, zod-validated form, and see it appear in the grid"
    - "User opens listing detail, clicks 'Chat com vendedor', lands in a chat room with real-time messages via the existing SignalR connection"
    - "User can search listings by text and filter by category/price/verified"
    - "User can report a listing; report lands in the admin moderation queue with type badge"
    - "Seller can mark sold, edit, or delete their own listings"
    - "Buyer can rate seller 1-5 stars after transaction; rating editable 7 days"
    - "Admin can toggle categories ON/OFF and see unified moderation queue (posts + listings)"
  artifacts:
    - path: "web/src/app/marketplace/page.tsx"
      provides: "Grid page with infinite scroll + filter chips"
      contains: "ListingGrid"
    - path: "web/src/app/marketplace/new/page.tsx"
      provides: "Create listing form with dropzone + category picker"
      contains: "ListingForm"
    - path: "web/src/app/chat/[conversationId]/page.tsx"
      provides: "Chat room with SignalR hook"
      contains: "useSignalRChat"
    - path: "web/src/hooks/useSignalRChat.ts"
      provides: "Extends existing SignalR connection with JoinConversation/MessageReceived"
      contains: "JoinConversation"
    - path: "web/src/stores/chatStore.ts"
      provides: "Zustand store for conversations + unread + messagesByConversation"
      contains: "chatStore"
  key_links:
    - from: "web/src/hooks/useSignalRChat.ts"
      to: "existing SignalR connection (from Phase 3 notifications)"
      via: "shared HubConnection instance, not a new connection"
      pattern: "JoinConversation"
    - from: "web/src/components/marketplace/ListingForm.tsx"
      to: "POST /api/v1/listings"
      via: "multipart FormData via axios with browser-image-compression pre-compression"
      pattern: "multipart|FormData"
    - from: "web/src/app/marketplace/page.tsx"
      to: "GET /api/v1/listings"
      via: "useQuery cursor pagination"
      pattern: "/listings"
    - from: "web/src/components/chat/ChatRoom.tsx"
      to: "chatStore"
      via: "Zustand subscription for messagesByConversation[id]"
      pattern: "chatStore|useChatStore"
---

<objective>
Web (Next.js 15) frontend for Phase 4: marketplace grid + filters, listing create/edit with photo dropzone, listing detail with chat entry, 1:1 chat UI wired to the EXISTING SignalR connection (from Phase 3 notifications — do not create a second connection), report flow, admin moderation queue extension, admin category toggles.

Purpose: Expose every backend endpoint from 04-01 through polished UI that matches Phase 1-3 conventions (Inter font, green-700, Tailwind, Zustand, react-hook-form + zod, axios with JWT refresh).

Output: Working web pages + components + tests. Runs against deployed api.bairronow.com.br.
</objective>

<execution_context>
@C:/Users/acq20/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/acq20/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-marketplace-chat/04-CONTEXT.md
@.planning/phases/04-marketplace-chat/04-RESEARCH.md
@.planning/phases/04-marketplace-chat/04-01-PLAN.md
@.planning/phases/02-verification-neighborhoods/02-02-SUMMARY.md
@.planning/phases/03-feed-posts/03-02-SUMMARY.md
@web/src/lib/api/client.ts
@web/src/stores/authStore.ts
@CLAUDE.md

<interfaces>
<!-- Types executor consumes from 04-01 + shared conventions -->

API base URL: `process.env.NEXT_PUBLIC_API_URL` → https://api.bairronow.com.br/api/v1

DTOs (mirror backend 04-01):
```ts
export interface Listing {
  id: string; sellerId: string; sellerDisplayName: string; sellerIsVerified: boolean;
  title: string; description: string; price: number;
  categoryCode: string; subcategoryCode: string;
  status: 'Active' | 'Sold' | 'Removed';
  createdAt: string; soldAt?: string | null;
  photos: { id: string; orderIndex: number; url: string; thumbnailUrl: string }[];
}
export interface Message {
  id: string; conversationId: string; senderId: string;
  text?: string; imageUrl?: string; sentAt: string;
}
export interface Conversation {
  id: string; listingId: string; listingTitle: string; listingThumbnailUrl: string;
  otherUserId: string; otherUserDisplayName: string; otherUserIsVerified: boolean;
  lastMessageAt: string; unreadCount: number;
}
```

Existing Phase 3 SignalR connection (reuse):
- `web/src/lib/signalr.ts` exports a singleton `getHubConnection(): Promise<HubConnection>` that handles auth via `accessTokenFactory: () => authStore.getState().accessToken`
- Existing handlers: `NotificationReceived`. Add: `MessageReceived`, `UnreadChanged`, `ConversationRead`.

Existing patterns:
- `web/src/lib/api/client.ts` — axios instance with JWT refresh interceptor (reuse)
- Form stack: react-hook-form + zod + `@hookform/resolvers/zod`
- zod v4 error syntax (project decision from Phase 1-2)
- Bottom nav is in `web/src/components/layout/BottomNav.tsx` — add Marketplace + Chat tabs
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 0 (Wave 0): Web test scaffolds + zod validators + API clients + Zustand stores</name>
  <files>
    web/src/app/marketplace/__tests__/grid.test.tsx,
    web/src/app/marketplace/__tests__/listing-detail.test.tsx,
    web/src/app/marketplace/__tests__/create-listing.test.tsx,
    web/src/app/chat/__tests__/chat-room.test.tsx,
    web/src/lib/validators/listing.ts,
    web/src/lib/validators/rating.ts,
    web/src/lib/api/marketplace.ts,
    web/src/lib/api/chat.ts,
    web/src/stores/marketplaceStore.ts,
    web/src/stores/chatStore.ts
  </files>
  <read_first>
    - .planning/phases/04-marketplace-chat/04-RESEARCH.md §"Code Examples" (listing zod schema + chatStore shape)
    - .planning/phases/04-marketplace-chat/04-01-PLAN.md <interfaces> block (DTO shapes)
    - web/src/lib/api/client.ts (existing axios instance)
    - web/src/stores/authStore.ts (existing Zustand pattern)
    - web/src/lib/signalr.ts (existing hub singleton)
    - .planning/phases/03-feed-posts/03-02-SUMMARY.md (existing feed API client pattern)
    - CLAUDE.md §"Frontend Libraries" (zod v3, browser-image-compression, @microsoft/signalr 8.x)
  </read_first>
  <behavior>
    - `listingSchema.parse({...valid})` succeeds; invalid cases (title < 3, price <= 0, photos = 0, photos = 7) throw.
    - `ratingSchema.parse({ stars: 6 })` throws; `{ stars: 3, comment: "ok" }` succeeds.
    - API client exports one function per endpoint in 04-01 interfaces block.
    - Zustand stores compile and have typed selectors.
    - Test stubs render without crashing, assertions marked `.skip` until components exist.
  </behavior>
  <action>
    1. **`web/src/lib/validators/listing.ts`** — copy from RESEARCH §"Code Examples":
       ```ts
       import { z } from "zod";
       import { CATEGORY_CODES } from "@/lib/categories";
       export const listingSchema = z.object({
         title: z.string().min(3).max(120),
         description: z.string().min(10).max(500),
         price: z.number().positive().max(999999),
         categoryCode: z.enum(CATEGORY_CODES as [string, ...string[]]),
         subcategoryCode: z.string().min(1),
         photos: z.array(z.instanceof(File)).min(1).max(6),
       });
       export type ListingForm = z.infer<typeof listingSchema>;
       ```
       Also export `CATEGORIES` mirroring `Constants/Categories.cs` D-24 (10 categories + flat subcategories). Hardcode once; do NOT fetch from API for the initial render (cache from `/categories` afterward).
    2. **`web/src/lib/validators/rating.ts`**: `z.object({ stars: z.number().int().min(1).max(5), comment: z.string().max(500).optional(), listingId: z.string().uuid() })`.
    3. **`web/src/lib/api/marketplace.ts`** — typed axios wrappers for every listings/favorites/ratings/reports endpoint in 04-01. Use FormData for create/update when photos present (pre-compress via `browser-image-compression` max 1920 width before append). Export functions: `listListings(params)`, `searchListings(params)`, `getListing(id)`, `createListing(data)`, `updateListing(id, data)`, `markSold(id)`, `deleteListing(id)`, `toggleFavorite(id)`, `reportListing(id, body)`, `createRating(sellerId, body)`, `editRating(sellerId, id, body)`, `getSellerRatings(sellerId)`, `getCategories()`, `adminToggleCategory(code, enabled)`, `adminListReports(filter)`.
    4. **`web/src/lib/api/chat.ts`**: `listConversations()`, `createConversation(listingId)`, `getMessageHistory(conversationId, before?)`, `sendMessage(conversationId, { text?, image? })`, `markRead(conversationId)`, `getUnreadCount()`. Use multipart when image present.
    5. **`web/src/stores/marketplaceStore.ts`** — Zustand: `{ gridCursor, gridItems, filters: { category, verifiedOnly: true /* D-10 */, minPrice, maxPrice, sort }, setFilters, append, reset }`.
    6. **`web/src/stores/chatStore.ts`** — shape from RESEARCH §"Code Examples":
       ```ts
       interface ChatState {
         conversations: Conversation[];
         messagesByConversation: Record<string, Message[]>;
         unreadTotal: number;
         activeConversationId: string | null;
         connect: () => Promise<void>;
         sendMessage: (conversationId: string, text?: string, image?: File) => Promise<void>;
         markRead: (conversationId: string) => Promise<void>;
       }
       ```
       `connect()` gets the EXISTING hub connection via `getHubConnection()`, registers `on("MessageReceived")`, `on("UnreadChanged")`, `on("ConversationRead")`. Do NOT create a new HubConnection.
    7. **Test scaffolds** with `describe.skip`/`it.skip` placeholders referencing MKT-001..011 web assertions. Real assertions added in Task 1-3.
    8. Update VALIDATION.md per-task map with rows for the 4 web test files.
  </action>
  <verify>
    <automated>cd web && pnpm test -- --testPathPattern="marketplace|chat|validators" --passWithNoTests</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test` exits 0 with stub tests (skipped OK)
    - `grep -n "z.array(z.instanceof(File)).min(1).max(6)" web/src/lib/validators/listing.ts` matches
    - `grep -n "verifiedOnly: true" web/src/stores/marketplaceStore.ts` matches (D-10 default)
    - `grep -n "getHubConnection" web/src/stores/chatStore.ts` matches (reuse existing hub)
    - `grep -n "new HubConnectionBuilder" web/src/stores/chatStore.ts` returns nothing (no parallel connection)
    - API client exports cover every endpoint in 04-01 interfaces block
  </acceptance_criteria>
  <done>Validators, API clients, Zustand stores, and test stubs ready for component work.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Marketplace grid + filters + listing detail + create/edit forms + report + rating</name>
  <files>
    web/src/app/marketplace/page.tsx,
    web/src/app/marketplace/new/page.tsx,
    web/src/app/marketplace/[id]/page.tsx,
    web/src/app/marketplace/[id]/edit/page.tsx,
    web/src/components/marketplace/ListingCard.tsx,
    web/src/components/marketplace/ListingGrid.tsx,
    web/src/components/marketplace/FilterChips.tsx,
    web/src/components/marketplace/CategoryPicker.tsx,
    web/src/components/marketplace/PhotoDropzone.tsx,
    web/src/components/marketplace/ListingForm.tsx,
    web/src/components/marketplace/ListingDetailGallery.tsx,
    web/src/components/marketplace/ReportListingDialog.tsx,
    web/src/components/marketplace/RatingForm.tsx
  </files>
  <read_first>
    - .planning/phases/04-marketplace-chat/04-CONTEXT.md §decisions (D-01..D-11, D-17..D-20, D-22, D-23, D-24, D-25)
    - .planning/phases/04-marketplace-chat/04-01-PLAN.md (backend interfaces)
    - web/src/components/posts/ (Phase 3 feed components to match visual style)
    - web/src/components/ui/VerifiedBadge.tsx (Phase 2 — reuse)
    - web/src/lib/validators/listing.ts (Task 0)
    - web/src/lib/api/marketplace.ts (Task 0)
    - CLAUDE.md §"Technology Stack" (Tailwind, react-hook-form + zod, Inter font, green-700 brand)
  </read_first>
  <behavior>
    - grid.test.tsx: renders listings from mocked API, filter chip click updates query, empty state shows CTA.
    - listing-detail.test.tsx: renders seller badge, photo gallery, "Chat com vendedor" button present.
    - create-listing.test.tsx: dropzone accepts 1-6 images, zod validation errors shown inline, submit calls API with FormData.
  </behavior>
  <action>
    1. **`ListingCard.tsx`** — 2-col grid card per D-05: thumbnail (aspect-square, lazy load), title (line-clamp-2), price (R$ formatted via `Intl.NumberFormat('pt-BR', { currency: 'BRL' })`), VerifiedBadge if sellerIsVerified. If `status === 'Sold'`, overlay "VENDIDO" badge (D-17).
    2. **`ListingGrid.tsx`** — 2-col on mobile, 3-col md, 4-col lg (keep 2-col primary per D-05, but allow up-scale on wide screens per Claude discretion). Infinite scroll via existing feed pattern (intersection observer).
    3. **`FilterChips.tsx`** — top chips (D-08): category dropdown (via CategoryPicker), verified toggle (default ON per D-10), price range (min/max numeric). Warn unverified listings with "⚠️ Vendedor não verificado" label when filter is OFF.
    4. **`CategoryPicker.tsx`** — 2-step chip grid picker per D-03: step 1 = 10 category chips, step 2 = subcategory chips for selected category. NO dropdowns.
    5. **`PhotoDropzone.tsx`** — react-dropzone, accepts image/jpeg and image/png, min 1, max 6 (D-01). Drag-to-reorder via `@dnd-kit/sortable` (already in project) OR simple up/down arrows if not. First photo automatically labeled "Capa" per D-01. Pre-compress with `browser-image-compression` to max 1920 width before upload. Show 5MB per-file cap warning.
    6. **`ListingForm.tsx`** — react-hook-form + zodResolver(listingSchema). Fields: title, description (char counter max 500), price (numeric required — NO "a combinar" per D-02), CategoryPicker, PhotoDropzone. Submit calls `createListing` or `updateListing`. Shows zod error messages inline with Phase 1-2 error style (zod v4 syntax).
    7. **`marketplace/page.tsx`** — Grid page. Reads from marketplaceStore, fetches via `listListings` or `searchListings` when query present. Top search bar + FilterChips. FAB "Novo anúncio" → /marketplace/new (only if user is verified, else show "Verifique seu endereço" CTA).
    8. **`marketplace/new/page.tsx`** — Verified-only guard. Renders ListingForm in create mode.
    9. **`marketplace/[id]/page.tsx`** — Listing detail. `ListingDetailGallery` (swipeable photos), title, price, description, seller profile (avatar + name + VerifiedBadge + average rating), posted date, favorite heart button, "Chat com vendedor" button → `createConversation(listingId)` then route to `/chat/{conversationId}`. If caller is seller, show "Editar", "Marcar como vendido", "Remover". If sold and viewer is buyer (had active conv with this listing), show RatingForm. Report button → ReportListingDialog.
    10. **`marketplace/[id]/edit/page.tsx`** — Seller-only. Renders ListingForm in edit mode.
    11. **`ReportListingDialog.tsx`** — radio list with fixed reasons per D-20: "Item proibido", "Suspeita de fraude / golpe", "Preço abusivo", "Descrição enganosa". Optional details textarea. Submits to `reportListing`.
    12. **`RatingForm.tsx`** — 1-5 star picker + optional comment (max 500), submits to `createRating`. If rating already exists within 7 days, show edit mode instead (calls `editRating`).
    13. Add Marketplace tab to bottom nav.
    14. Un-skip and complete grid.test.tsx, listing-detail.test.tsx, create-listing.test.tsx.
  </action>
  <verify>
    <automated>cd web && pnpm test -- --testPathPattern="marketplace" && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - All marketplace test files pass (non-skip)
    - `pnpm build` succeeds (Next.js static export compatible — Phase 1 constraint)
    - `grep -n "Intl.NumberFormat.*pt-BR.*BRL" web/src/components/marketplace/ListingCard.tsx` matches
    - `grep -n "max(6)" web/src/lib/validators/listing.ts` matches
    - `grep -n "Vendedor não verificado" web/src/components/marketplace/FilterChips.tsx` matches (D-10)
    - `grep -rn "a combinar" web/src/components/marketplace/` returns nothing (D-02 no placeholder price)
    - `grep -n "VerifiedBadge" web/src/components/marketplace/ListingCard.tsx` matches (reuse Phase 2)
  </acceptance_criteria>
  <done>Marketplace grid, detail, create/edit, report, rating UI complete, tests green, build passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Chat UI (list + room + composer) + SignalR hook + admin moderation/categories</name>
  <files>
    web/src/hooks/useSignalRChat.ts,
    web/src/app/chat/page.tsx,
    web/src/app/chat/[conversationId]/page.tsx,
    web/src/components/chat/ConversationList.tsx,
    web/src/components/chat/ChatRoom.tsx,
    web/src/components/chat/MessageBubble.tsx,
    web/src/components/chat/MessageComposer.tsx,
    web/src/app/admin/moderation/page.tsx,
    web/src/app/admin/categories/page.tsx
  </files>
  <read_first>
    - .planning/phases/04-marketplace-chat/04-CONTEXT.md §decisions (D-12..D-16, D-21, D-26)
    - .planning/phases/04-marketplace-chat/04-RESEARCH.md §"Pattern 4", §"Code Examples" (chatStore + hub extensions)
    - .planning/phases/04-marketplace-chat/04-01-PLAN.md (hub method signatures + DTOs)
    - web/src/lib/signalr.ts (existing singleton connection from Phase 3 notifications — REUSE)
    - web/src/stores/chatStore.ts (Task 0)
    - web/src/lib/api/chat.ts (Task 0)
    - .planning/phases/03-feed-posts/03-02-SUMMARY.md (admin moderation queue UI to extend, notification bell with unread counter pattern)
    - web/src/app/admin/moderation/ (existing Phase 3 admin queue — EXTEND, do not duplicate)
  </read_first>
  <behavior>
    - chat-room.test.tsx: renders historical messages, simulates incoming MessageReceived event via mocked hub, new bubble appears.
    - Unread badge on bottom nav chat tab updates when UnreadChanged fires.
    - Admin moderation queue shows posts + comments + listings in a single list with TargetType badge column.
  </behavior>
  <action>
    1. **`useSignalRChat.ts`** — hook that on mount calls `chatStore.getState().connect()` which invokes `await (await getHubConnection()).invoke("JoinConversation", conversationId)`. On unmount: `invoke("LeaveConversation", conversationId)`. Does NOT construct a new HubConnection. Returns `{ messages, sendMessage, markRead }`.
    2. **`ConversationList.tsx`** — list of conversations sorted by lastMessageAt DESC. Each row: listing thumbnail + title, other user display name + VerifiedBadge, last message preview, unread badge.
    3. **`ChatRoom.tsx`** — renders listing header (thumbnail + title link to /marketplace/{id}), messages list (reverse scroll with cursor-based load-more via `getMessageHistory(before=oldestMessageSentAt)`), MessageComposer at bottom. On mount: `markRead(conversationId)`. On each new inbound message: also `markRead`. On scroll to top: load more history.
    4. **`MessageBubble.tsx`** — sender-aligned right (green-700 bg), receiver left (gray-100). Shows text or image. Timestamp via date-fns `formatDistanceToNow` with `locale: ptBR`.
    5. **`MessageComposer.tsx`** — textarea + image picker (react-dropzone single file, 5MB max). Submit calls `chatStore.sendMessage(conversationId, text, image)`. Disabled if both empty. Optimistic append to chatStore (revert on failure).
    6. **`app/chat/page.tsx`** — conversation list.
    7. **`app/chat/[conversationId]/page.tsx`** — uses `useSignalRChat`, renders ChatRoom.
    8. Hook unread counter to bottom nav chat icon badge — read from `chatStore.unreadTotal`, updates from `UnreadChanged` hub event.
    9. **`app/admin/moderation/page.tsx`** — EXTEND existing Phase 3 admin queue (do not duplicate). Add TargetType filter chips (All / Post / Comment / Listing). Each row: TargetType badge, reason, reporter, target preview (post snippet OR listing thumbnail + title). Same approve/reject actions.
    10. **`app/admin/categories/page.tsx`** — NEW admin page. Lists 10 categories with ON/OFF toggle each. PATCH `/admin/categories/{code}` on toggle. No create/delete per D-26.
    11. Un-skip chat-room.test.tsx. Mock hub connection to simulate `MessageReceived` and assert Zustand store updates and DOM re-render.
  </action>
  <verify>
    <automated>cd web && pnpm test -- --testPathPattern="chat|admin" && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - All chat + admin tests pass
    - `pnpm build` succeeds
    - `grep -rn "new HubConnectionBuilder" web/src/hooks/ web/src/stores/chatStore.ts web/src/components/chat/` returns nothing (no parallel connection)
    - `grep -n "getHubConnection" web/src/hooks/useSignalRChat.ts` matches
    - `grep -n "JoinConversation" web/src/hooks/useSignalRChat.ts` matches
    - Admin moderation page file does NOT create a new list — grep confirms it imports existing Phase 3 moderation queue component
    - Bottom nav badge reads from `chatStore.unreadTotal`
  </acceptance_criteria>
  <done>Chat UI complete and real-time via existing hub, admin moderation extended for listings, admin category toggles live.</done>
</task>

</tasks>

<verification>
- `pnpm test` and `pnpm build` both exit 0
- Manual: verified user creates listing → appears in own grid → buyer (second account) sees it → clicks chat → sends message → seller receives it live → seller marks sold → buyer sees rating prompt
- Admin sees unified moderation queue with type badges
- No second SignalR HubConnection exists in the bundle: `grep -rn "new HubConnectionBuilder" web/src/` returns ONLY the existing Phase 3 signalr.ts file
- Cloudflare manual verify from VALIDATION.md: `/uploads/listings/*` returns `cf-cache-status: HIT` on second request
</verification>

<success_criteria>
- Grid, search, filters, create, detail, edit, mark-sold, report, rate, favorite all functional end-to-end against 04-01 backend
- Chat room delivers live messages via the existing SignalR connection
- Unread badge accurate across refresh
- Admin queue unified, category toggle works
- Visual parity with Phase 1-3 design system (Inter, green-700, Tailwind)
</success_criteria>

<output>
After completion, create `.planning/phases/04-marketplace-chat/04-02-SUMMARY.md` with: pages/components created, hub hook approach confirmation (single connection), test coverage, any deviations.
</output>
