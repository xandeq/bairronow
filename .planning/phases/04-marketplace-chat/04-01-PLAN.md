---
phase: 04-marketplace-chat
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/Api/Features/Marketplace/Listings/ListingEntity.cs
  - backend/src/Api/Features/Marketplace/Listings/ListingPhoto.cs
  - backend/src/Api/Features/Marketplace/Listings/ListingStatus.cs
  - backend/src/Api/Features/Marketplace/Listings/Commands/CreateListingCommand.cs
  - backend/src/Api/Features/Marketplace/Listings/Commands/UpdateListingCommand.cs
  - backend/src/Api/Features/Marketplace/Listings/Commands/MarkSoldCommand.cs
  - backend/src/Api/Features/Marketplace/Listings/Commands/DeleteListingCommand.cs
  - backend/src/Api/Features/Marketplace/Listings/Queries/GetBairroGridQuery.cs
  - backend/src/Api/Features/Marketplace/Listings/Queries/SearchListingsQuery.cs
  - backend/src/Api/Features/Marketplace/Listings/Queries/GetListingDetailQuery.cs
  - backend/src/Api/Features/Marketplace/Listings/Validators/CreateListingValidator.cs
  - backend/src/Api/Features/Marketplace/Listings/Validators/UpdateListingValidator.cs
  - backend/src/Api/Features/Marketplace/Listings/ListingsController.cs
  - backend/src/Api/Features/Marketplace/Listings/ListingPhotoService.cs
  - backend/src/Api/Features/Marketplace/Favorites/FavoriteEntity.cs
  - backend/src/Api/Features/Marketplace/Favorites/FavoritesController.cs
  - backend/src/Api/Features/Marketplace/Ratings/SellerRatingEntity.cs
  - backend/src/Api/Features/Marketplace/Ratings/RatingsController.cs
  - backend/src/Api/Features/Marketplace/Ratings/Validators/CreateRatingValidator.cs
  - backend/src/Api/Features/Marketplace/Reports/ReportListingCommand.cs
  - backend/src/Api/Features/Moderation/ReportEntity.cs
  - backend/src/Api/Features/Moderation/ReportTargetType.cs
  - backend/src/Api/Features/Chat/ConversationEntity.cs
  - backend/src/Api/Features/Chat/ConversationParticipant.cs
  - backend/src/Api/Features/Chat/MessageEntity.cs
  - backend/src/Api/Features/Chat/ChatController.cs
  - backend/src/Api/Features/Chat/Commands/SendMessageCommand.cs
  - backend/src/Api/Features/Chat/Commands/MarkReadCommand.cs
  - backend/src/Api/Features/Chat/Queries/GetConversationsQuery.cs
  - backend/src/Api/Features/Chat/Queries/GetMessageHistoryQuery.cs
  - backend/src/Api/Features/Chat/ChatHubExtensions.cs
  - backend/src/Api/Infrastructure/Realtime/NotificationHub.cs
  - backend/src/Api/Constants/Categories.cs
  - backend/src/Api/Persistence/AppDbContext.cs
  - backend/src/Api/Persistence/Migrations/20260408_Phase4Marketplace.cs
  - backend/tests/Api.Tests/Marketplace/ListingServiceTests.cs
  - backend/tests/Api.Tests/Marketplace/MarketplaceSearchTests.cs
  - backend/tests/Api.Tests/Chat/ChatHubTests.cs
  - backend/tests/Api.Tests/Chat/ConversationRepositoryTests.cs
  - backend/tests/Api.Tests/Chat/UnreadCountTests.cs
  - backend/tests/Api.Tests/Moderation/ListingReportTests.cs
  - backend/tests/Api.Tests/Ratings/RatingServiceTests.cs
autonomous: true
requirements: [MKT-001, MKT-003, MKT-004, MKT-005, MKT-006, MKT-007, MKT-008, MKT-009, MKT-010, MKT-011, MKT-012, MKT-013]

must_haves:
  truths:
    - "Verified user can POST /api/v1/listings with 1-6 photos and receive a Listing row with bairro scoping + photos on disk under wwwroot/uploads/listings/"
    - "GET /api/v1/listings returns only active, non-deleted listings in the caller's bairro sorted by CreatedAt DESC"
    - "Full-text search via CONTAINS() returns relevance-ordered listings matching title+description"
    - "SendMessage persists a Message row, updates Conversation.LastMessageAt, and pushes MessageReceived over the existing NotificationHub group conv:{id}"
    - "Unread count query returns correct total across all conversations filtering DeletedAt IS NULL"
    - "Report with TargetType=Listing lands in the SAME moderation queue table as Phase 3 post reports"
    - "MarkSold sets SoldAt, listing visible in grid for 7 days then filtered out, remains in seller Vendidos history"
    - "Seller rating 1-5 stars persists with 7-day edit window enforced"
  artifacts:
    - path: "backend/src/Api/Features/Marketplace/Listings/ListingEntity.cs"
      provides: "Listing aggregate with Status, SoldAt, DeletedAt, BairroId, Photos"
      contains: "class Listing"
    - path: "backend/src/Api/Features/Chat/ConversationEntity.cs"
      provides: "Conversation + ConversationParticipant with LastReadAt"
      contains: "class Conversation"
    - path: "backend/src/Api/Features/Chat/MessageEntity.cs"
      provides: "Message with Text/ImagePath/SentAt/DeletedAt"
      contains: "class Message"
    - path: "backend/src/Api/Constants/Categories.cs"
      provides: "Hardcoded 10-category taxonomy per D-24"
      contains: "Categories"
    - path: "backend/src/Api/Persistence/Migrations/20260408_Phase4Marketplace.cs"
      provides: "EF migration for Listings, Photos, Conversations, Messages, Favorites, Ratings, Report.TargetType; creates FT catalog"
      contains: "CREATE FULLTEXT"
    - path: "backend/src/Api/Infrastructure/Realtime/NotificationHub.cs"
      provides: "Extended hub with JoinConversation/LeaveConversation (existing hub, not a new one)"
      contains: "JoinConversation"
  key_links:
    - from: "ChatController.SendMessage"
      to: "NotificationHub group conv:{id}"
      via: "IHubContext<NotificationHub>.Clients.Group($\"conv:{id}\").SendAsync(\"MessageReceived\", dto)"
      pattern: "Clients\\.Group\\(.*conv:"
    - from: "ListingPhotoService"
      to: "wwwroot/uploads/listings/"
      via: "ImageSharp Image.LoadAsync -> Resize -> SaveAsJpegAsync (sequential, per Pitfall 8)"
      pattern: "wwwroot.*uploads.*listings"
    - from: "SearchListingsQuery"
      to: "Listings full-text index"
      via: "FromSqlInterpolated CONTAINS((Title, Description), {term})"
      pattern: "CONTAINS\\(\\(Title, Description\\)"
    - from: "ReportListingCommand"
      to: "Reports table (Phase 3 shared)"
      via: "Report row with TargetType=ReportTargetType.Listing"
      pattern: "TargetType\\s*=\\s*ReportTargetType\\.Listing"
---

<objective>
Backend (.NET 8) for Phase 4: listing CRUD + photo pipeline (reusing Phase 2 ImageSharp), full-text search, 1:1 chat persistence + SignalR hub extension, favorites, ratings, shared moderation queue extension, taxonomy seed, and EF migration.

Purpose: Deliver every server-side capability Phase 4 requires so that 04-02 (web) and 04-03 (mobile) can consume stable endpoints in Wave 2. Extend — do not duplicate — Phase 1-3 infrastructure (SignalR hub, ImageSharp pipeline, moderation queue, audit logging, bairro scoping).

Output: Working REST endpoints + hub methods + migration + xUnit tests.

Deferred scope (included in `requirements` frontmatter for coverage, but NOT implemented):
- MKT-012 Paid spotlight / Verified Business badge → Phase 6 monetization. Acknowledged in CONTEXT.md §deferred.
- MKT-013 Service scheduling with time slots → backlog. Serviços exists as a flat category only (D-24).
</objective>

<execution_context>
@C:/Users/acq20/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/acq20/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-marketplace-chat/04-CONTEXT.md
@.planning/phases/04-marketplace-chat/04-RESEARCH.md
@.planning/phases/04-marketplace-chat/04-VALIDATION.md
@.planning/phases/01-infrastructure-auth/01-01-SUMMARY.md
@.planning/phases/03-feed-posts/03-01-SUMMARY.md
@backend/src/Api/Infrastructure/Realtime/NotificationHub.cs
@backend/src/Api/Features/Moderation/ReportEntity.cs
@backend/src/Api/Persistence/AppDbContext.cs
@CLAUDE.md

<interfaces>
<!-- Contracts downstream plans (04-02 web, 04-03 mobile) consume. Executor must define these exactly. -->

REST endpoints produced by this plan:
- POST   /api/v1/listings                                  — create listing (multipart: json + 1..6 photos)
- GET    /api/v1/listings?category=&minPrice=&maxPrice=&verifiedOnly=&sort=&cursor=
- GET    /api/v1/listings/search?q=&category=&minPrice=&maxPrice=&verifiedOnly=
- GET    /api/v1/listings/{id}
- PATCH  /api/v1/listings/{id}
- POST   /api/v1/listings/{id}/mark-sold
- DELETE /api/v1/listings/{id}                             — soft delete
- POST   /api/v1/listings/{id}/favorite                    — toggle
- POST   /api/v1/listings/{id}/report                      — body: { reason, details }
- GET    /api/v1/sellers/{userId}/ratings
- POST   /api/v1/sellers/{userId}/ratings                  — body: { stars, comment, listingId }
- PATCH  /api/v1/sellers/{userId}/ratings/{id}             — enforce 7-day window
- GET    /api/v1/chat/conversations
- POST   /api/v1/chat/conversations                        — body: { listingId }  (dedupes existing)
- GET    /api/v1/chat/conversations/{id}/messages?before=&limit=50
- POST   /api/v1/chat/conversations/{id}/messages          — multipart: text + optional image
- POST   /api/v1/chat/conversations/{id}/read
- GET    /api/v1/chat/unread-count
- GET    /api/v1/categories                                — returns Constants/Categories.cs seed
- PATCH  /api/v1/admin/categories/{code}                   — { enabled: bool } (ON/OFF only per D-26)
- GET    /api/v1/admin/moderation/reports                  — shared queue, filter by targetType

SignalR NotificationHub methods ADDED (on the existing hub — do not create a new hub):
- JoinConversation(Guid conversationId)
- LeaveConversation(Guid conversationId)

Server-to-client events broadcast:
- "MessageReceived"   payload: MessageDto
- "UnreadChanged"     payload: { total: number }
- "ConversationRead"  payload: { conversationId, readerId, readAt }

DTOs:
```csharp
public record ListingDto(Guid Id, Guid SellerId, string SellerDisplayName, bool SellerIsVerified,
    string Title, string Description, decimal Price, string CategoryCode, string SubcategoryCode,
    string Status, DateTime CreatedAt, DateTime? SoldAt, List<ListingPhotoDto> Photos);
public record ListingPhotoDto(Guid Id, int OrderIndex, string Url, string ThumbnailUrl);
public record MessageDto(Guid Id, Guid ConversationId, Guid SenderId, string? Text, string? ImageUrl, DateTime SentAt);
public record ConversationDto(Guid Id, Guid ListingId, string ListingTitle, string ListingThumbnailUrl,
    Guid OtherUserId, string OtherUserDisplayName, bool OtherUserIsVerified,
    DateTime LastMessageAt, int UnreadCount);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 0 (Wave 0): Test scaffolds + FTS smoke test</name>
  <files>
    backend/tests/Api.Tests/Marketplace/ListingServiceTests.cs,
    backend/tests/Api.Tests/Marketplace/MarketplaceSearchTests.cs,
    backend/tests/Api.Tests/Chat/ChatHubTests.cs,
    backend/tests/Api.Tests/Chat/ConversationRepositoryTests.cs,
    backend/tests/Api.Tests/Chat/UnreadCountTests.cs,
    backend/tests/Api.Tests/Moderation/ListingReportTests.cs,
    backend/tests/Api.Tests/Ratings/RatingServiceTests.cs,
    backend/tests/Api.Tests/Smoke/FullTextSyncLagTests.cs
  </files>
  <read_first>
    - .planning/phases/04-marketplace-chat/04-VALIDATION.md (Wave 0 requirements)
    - .planning/phases/04-marketplace-chat/04-RESEARCH.md §"Validation Architecture"
    - backend/tests/Api.Tests/ (existing xUnit test patterns from Phase 3)
    - .planning/phases/03-feed-posts/03-01-SUMMARY.md (existing test infrastructure)
  </read_first>
  <behavior>
    - All test files compile and contain `[Fact]` stubs marked `Skip = "Wave 0 stub - implemented in Task 1-3"` that reference every MKT-* requirement per the validation map.
    - `FullTextSyncLagTests`: single `[Fact]` that inserts a row into a Listings-like table with an FT index, polls CONTAINS() every 500ms up to 10s, asserts sync lag <5s. Marked `[Trait("Category","Smoke")]`. Test skipped in CI unless `FTS_SMOKE=1` env var set.
  </behavior>
  <action>
    Create xUnit test files listed above. Each file has a `public class` with `[Fact(Skip="Wave 0 stub")]` methods named after the requirements it will cover:
    - ListingServiceTests: CreateListing_With6Photos_PersistsAndWritesToDisk, CreateListing_OnlyVerifiedUsers_Allowed, UpdateListing_WritesAuditLog, MarkSold_SetsSoldAt, SoftDelete_PreservesPhotosForHistory
    - MarketplaceSearchTests: GetBairroGrid_FiltersByBairroAndStatus, GetBairroGrid_SortsByRecency, SearchListings_CONTAINSReturnsMatches, SearchListings_SanitizesSpecialChars (Pitfall 6), SearchListings_WithFilters
    - ChatHubTests: JoinConversation_RejectsNonParticipant, SendMessage_BroadcastsToGroup
    - ConversationRepositoryTests: CreateOrGet_DedupesExistingConversation (Research Open Question 3)
    - UnreadCountTests: Count_IgnoresSoftDeletedMessages (Pitfall 4), Count_HonorsLastReadAt
    - ListingReportTests: Report_CreatesRowWithTargetTypeListing_InSharedQueue
    - RatingServiceTests: Create_Stars1to5, Edit_Within7Days_Succeeds, Edit_After7Days_Rejected
    - FullTextSyncLagTests: Insert_AppearsInContains_Within5Seconds (actual polling test, not stub)

    Update `.planning/phases/04-marketplace-chat/04-VALIDATION.md` per-task map with one row per MKT-* requirement and the automated command.
  </action>
  <verify>
    <automated>cd backend && dotnet test tests/Api.Tests/Api.Tests.csproj --filter "FullyQualifiedName~Marketplace|FullyQualifiedName~Chat|FullyQualifiedName~Moderation|FullyQualifiedName~Ratings" -v quiet</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet test` discovers all stub tests (skipped is OK) and exits 0
    - `grep -r "MKT-" .planning/phases/04-marketplace-chat/04-VALIDATION.md | wc -l` >= 13
    - FullTextSyncLagTests file exists and contains the word "CONTAINS"
    - No test file references a second SignalR hub (grep -r "class.*Hub.*:.*Hub" backend/src/Api/Features/Chat/ returns no new hub class)
  </acceptance_criteria>
  <done>Stub tests compile, VALIDATION.md map filled, FTS smoke test ready to run.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Entities + EF migration + category seed + moderation extension</name>
  <files>
    backend/src/Api/Features/Marketplace/Listings/ListingEntity.cs,
    backend/src/Api/Features/Marketplace/Listings/ListingPhoto.cs,
    backend/src/Api/Features/Marketplace/Listings/ListingStatus.cs,
    backend/src/Api/Features/Marketplace/Favorites/FavoriteEntity.cs,
    backend/src/Api/Features/Marketplace/Ratings/SellerRatingEntity.cs,
    backend/src/Api/Features/Chat/ConversationEntity.cs,
    backend/src/Api/Features/Chat/ConversationParticipant.cs,
    backend/src/Api/Features/Chat/MessageEntity.cs,
    backend/src/Api/Features/Moderation/ReportEntity.cs,
    backend/src/Api/Features/Moderation/ReportTargetType.cs,
    backend/src/Api/Constants/Categories.cs,
    backend/src/Api/Persistence/AppDbContext.cs,
    backend/src/Api/Persistence/Migrations/20260408_Phase4Marketplace.cs
  </files>
  <read_first>
    - .planning/phases/04-marketplace-chat/04-RESEARCH.md §"Pattern 1", §"Pattern 4", §"Pattern 5", §"Pattern 6"
    - .planning/phases/04-marketplace-chat/04-CONTEXT.md §decisions (D-01, D-02, D-17, D-21, D-22, D-23, D-24, D-25, D-26)
    - backend/src/Api/Features/Moderation/ReportEntity.cs (existing — must be EXTENDED, not replaced)
    - backend/src/Api/Persistence/AppDbContext.cs (existing DbContext — add DbSets)
    - backend/src/Api/Persistence/Migrations/ (to match existing migration naming pattern)
    - .planning/phases/02-verification-neighborhoods/02-01-SUMMARY.md (BairroId + IBairroContext)
    - .planning/phases/03-feed-posts/03-01-SUMMARY.md (Report table schema)
  </read_first>
  <behavior>
    - Unskip ListingServiceTests.CreateListing_With6Photos_PersistsAndWritesToDisk once service exists (Task 2).
    - Unskip ListingReportTests.Report_CreatesRowWithTargetTypeListing_InSharedQueue.
    - Migration up/down both run cleanly against LocalDB and include FT catalog creation.
  </behavior>
  <action>
    1. Create `Listing` entity exactly per RESEARCH §Pattern 1:
       ```csharp
       public class Listing {
           public Guid Id { get; set; }
           public Guid SellerId { get; set; }
           public Guid BairroId { get; set; }           // D-07
           public string Title { get; set; } = default!;       // max 120
           public string Description { get; set; } = default!; // max 500 per D-09
           public decimal Price { get; set; }                   // required per D-02
           public string CategoryCode { get; set; } = default!;
           public string SubcategoryCode { get; set; } = default!;
           public ListingStatus Status { get; set; }            // Active | Sold | Removed
           public DateTime CreatedAt { get; set; }
           public DateTime UpdatedAt { get; set; }
           public DateTime? SoldAt { get; set; }
           public DateTime? DeletedAt { get; set; }
           public List<ListingPhoto> Photos { get; set; } = new();
       }
       public enum ListingStatus { Active = 1, Sold = 2, Removed = 3 }
       ```
    2. `ListingPhoto` with Id, ListingId, OrderIndex (0=cover per D-01), StoragePath, ThumbnailPath.
    3. `FavoriteEntity`: { Id, ListingId, UserId, SnapshotPrice, CreatedAt } — SnapshotPrice per Research Open Question 1 (MKT-009 price-change notification).
    4. `SellerRatingEntity`: { Id, SellerId, BuyerId, ListingId, Stars (1-5), Comment (max 500), CreatedAt, UpdatedAt, DeletedByAdminAt }. Enforce 7-day edit via service logic (per D-23).
    5. Chat entities exactly per RESEARCH §Pattern 4: `Conversation`, `ConversationParticipant` (with `LastReadAt`, `SoftDeleted`), `Message` (Text?, ImagePath?, SentAt, DeletedAt?).
    6. Extend `ReportEntity` by ADDING `public ReportTargetType TargetType { get; set; }` + `public Guid TargetId { get; set; }`. Create `ReportTargetType` enum: `Post = 1, Comment = 2, Listing = 3`. Do NOT delete the existing PostId/CommentId columns — migration must backfill TargetType based on which FK is set and then the old columns can be kept nullable (backward compat with Phase 3 moderation UI).
    7. Create `Constants/Categories.cs` — hardcoded 10 categories per D-24 with 2-3 subcategories each per D-25. Each entry: `{ Code, DisplayName, Subcategories: [{ Code, DisplayName }], Enabled (default true) }`. Plus a static `CATEGORY_CODES` string array used by validators.
    8. Update `AppDbContext`: add DbSets for Listing, ListingPhoto, Favorite, SellerRating, Conversation, ConversationParticipant, Message. In `OnModelCreating`:
       - Global query filter on Listing: `HasQueryFilter(l => l.BairroId == _bairroContext.CurrentBairroId && l.DeletedAt == null)`
       - Composite key on ConversationParticipant (ConversationId, UserId)
       - Unique index on (ListingId, UserId) for Favorite
    9. Create EF migration `20260408_Phase4Marketplace` via `dotnet ef migrations add`. In the migration's `Up()`, APPEND raw SQL for the FT catalog per RESEARCH §Pattern 3:
       ```csharp
       migrationBuilder.Sql(@"CREATE FULLTEXT CATALOG ftListings AS DEFAULT;");
       migrationBuilder.Sql(@"CREATE FULLTEXT INDEX ON Listings(Title, Description)
           KEY INDEX PK_Listings ON ftListings WITH CHANGE_TRACKING AUTO;");
       ```
       Also create indexes: `IX_Messages_Conversation_SentAt`, `IX_ConvParticipant_User_LastRead`, `IX_Listings_BairroId_Status_CreatedAt`.
       In `Down()`, drop FT index + catalog BEFORE dropping Listings table.
    10. Do NOT create a separate `ChatHub` class — Task 3 extends the existing `NotificationHub`.
  </action>
  <verify>
    <automated>cd backend && dotnet build && dotnet ef database update --project src/Api --context AppDbContext --connection "Server=(localdb)\MSSQLLocalDB;Database=NossoVizinhoTest_P4;Trusted_Connection=True"</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build` succeeds
    - `grep -n "class Listing" backend/src/Api/Features/Marketplace/Listings/ListingEntity.cs` matches
    - `grep -rn "CREATE FULLTEXT" backend/src/Api/Persistence/Migrations/` returns at least 2 matches (catalog + index)
    - `grep -n "public ReportTargetType TargetType" backend/src/Api/Features/Moderation/ReportEntity.cs` matches
    - `grep -c "class" backend/src/Api/Features/Chat/` for new chat files == 3 (Conversation, ConversationParticipant, Message)
    - No new class extending `Hub` added in backend/src/Api/Features/Chat/ (hub extension lives on NotificationHub in Task 3)
    - `Constants/Categories.cs` contains all 10 category codes from D-24
    - Migration `Up()` runs cleanly on LocalDB
  </acceptance_criteria>
  <done>All entities committed, AppDbContext updated, migration applies + rolls back cleanly, FT catalog created, category seed present, Report extended with TargetType.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Listings + Favorites + Ratings + Reports services, photo pipeline, search, admin taxonomy</name>
  <files>
    backend/src/Api/Features/Marketplace/Listings/ListingPhotoService.cs,
    backend/src/Api/Features/Marketplace/Listings/Commands/CreateListingCommand.cs,
    backend/src/Api/Features/Marketplace/Listings/Commands/UpdateListingCommand.cs,
    backend/src/Api/Features/Marketplace/Listings/Commands/MarkSoldCommand.cs,
    backend/src/Api/Features/Marketplace/Listings/Commands/DeleteListingCommand.cs,
    backend/src/Api/Features/Marketplace/Listings/Queries/GetBairroGridQuery.cs,
    backend/src/Api/Features/Marketplace/Listings/Queries/SearchListingsQuery.cs,
    backend/src/Api/Features/Marketplace/Listings/Queries/GetListingDetailQuery.cs,
    backend/src/Api/Features/Marketplace/Listings/Validators/CreateListingValidator.cs,
    backend/src/Api/Features/Marketplace/Listings/Validators/UpdateListingValidator.cs,
    backend/src/Api/Features/Marketplace/Listings/ListingsController.cs,
    backend/src/Api/Features/Marketplace/Favorites/FavoritesController.cs,
    backend/src/Api/Features/Marketplace/Ratings/RatingsController.cs,
    backend/src/Api/Features/Marketplace/Ratings/Validators/CreateRatingValidator.cs,
    backend/src/Api/Features/Marketplace/Reports/ReportListingCommand.cs,
    backend/src/Api/Features/Admin/CategoriesController.cs,
    backend/src/Api/Features/Admin/ModerationController.cs,
    backend/src/Api/Program.cs
  </files>
  <read_first>
    - .planning/phases/04-marketplace-chat/04-RESEARCH.md §"Pattern 2" (photo pipeline + Pitfall 8 sequential), §"Pattern 3" (FTS with FromSqlInterpolated + Pitfall 6 sanitization + Pitfall 3 fallback), §"Pattern 5" (shared moderation)
    - .planning/phases/04-marketplace-chat/04-CONTEXT.md §decisions (D-01, D-02, D-05..D-11, D-17..D-26)
    - .planning/phases/02-verification-neighborhoods/02-01-SUMMARY.md (ImageSharp pipeline handler to COPY the pattern from)
    - backend/src/Api/Features/Verification/ProofPhotoService.cs (or equivalent Phase 2 ImageSharp handler — REPLICATE, do not rewrite from scratch)
    - backend/src/Api/Features/Posts/Moderation/ReportsController.cs (Phase 3 — extend its admin queue instead of duplicating)
    - backend/src/Api/Program.cs (middleware/static files, rate limiting registration)
  </read_first>
  <behavior>
    - Unskip all Marketplace + Ratings + Moderation tests from Task 0.
    - CreateListing round-trip (request → DB row + photos on disk) passes integration test.
    - Search query with malformed term does not 500 (Pitfall 6).
    - MarkSold keeps listing visible for 7 days (test uses clock abstraction).
  </behavior>
  <action>
    1. **ListingPhotoService** — copy EXACT pipeline from RESEARCH §Pattern 2:
       - Path: `wwwroot/uploads/listings/{yyyy}/{MM}/{listingId}/{photoGuid}.jpg`
       - Main: Resize Max 1920x1080, JPEG quality 85
       - Thumb: Resize Crop 400x400, JPEG quality 80 (`_thumb.jpg` suffix)
       - **Process photos SEQUENTIALLY, not Task.WhenAll** (Pitfall 8). Dispose each `Image` before loading next.
       - Strip EXIF GPS: `image.Metadata.ExifProfile?.RemoveValue(ExifTag.GPSLatitude)` and GPSLongitude (manual verification in VALIDATION.md §Manual-Only).
       - In `Program.cs`, add `Configuration.Default.MemoryAllocator = ArrayPoolMemoryAllocator.CreateWithMinimalPooling()` per Pitfall 8.
       - In `Program.cs`, add static files middleware serving `/uploads/listings/*` with `Cache-Control: public, max-age=31536000, immutable` (Research Open Question 2).
    2. **CreateListingCommand** (MediatR): verify caller `IsVerified == true`, load caller's BairroId from `IBairroContext`, persist Listing with Status=Active, process photos 1..6 via ListingPhotoService, return `ListingDto`. Fail with 403 if not verified.
    3. **UpdateListingCommand**: caller must be seller. Updates title/description/price/category/photos. Writes audit log via existing Serilog sink with structured event `{ ListingId, Before, After, UserId }` per D-18. If price changed AND any Favorites exist with different SnapshotPrice, enqueue notification via Phase 3 notification system ("Preço alterado no anúncio favoritado") — per Research Open Question 1.
    4. **MarkSoldCommand**: caller must be seller. Sets `SoldAt=UtcNow`, Status=Sold. Listing still returned by GetBairroGrid if `SoldAt > UtcNow - 7 days`. Send notification to seller "Avalie esta transação" linking to rating form (D-22).
    5. **DeleteListingCommand**: soft delete (`DeletedAt=UtcNow`, Status=Removed). Photos left on disk per Pitfall 5.
    6. **GetBairroGridQuery** (MediatR): apply bairro scoping via query filter, filter Status=Active OR (Status=Sold AND SoldAt > UtcNow.AddDays(-7)), filter DeletedAt IS NULL, filter by category/price range/verifiedOnly (join Seller.IsVerified), sort by CreatedAt DESC by default or Price ASC/DESC. Cursor pagination on (CreatedAt, Id). Cache first page per-bairro in IMemoryCache with 30s TTL.
    7. **SearchListingsQuery**: use `FromSqlInterpolated` with `CONTAINS((Title, Description), {quotedTerm})` exactly as RESEARCH §Pattern 3 shows. Sanitize: reject if term length < 2 or no word char (Pitfall 6), wrap term in double quotes escaping embedded quotes. For listings created < 20 min ago, UNION with a LIKE query to cover FTS sync lag per Pitfall 3. All filters combinable.
    8. **GetListingDetailQuery**: returns full ListingDto including photos ordered by OrderIndex, seller profile fields (DisplayName, IsVerified badge, BairroName), AsNoTracking. 404 if deleted or cross-bairro.
    9. **FluentValidators**:
       - CreateListingValidator: title 3-120, description 10-500, price > 0 and <= 999999, categoryCode in `CATEGORY_CODES`, subcategoryCode non-empty, photos count 1..6.
       - UpdateListingValidator: same fields optional but validated if present.
       - CreateRatingValidator: stars 1-5, comment max 500, listingId exists and belongs to sellerId and is Sold.
    10. **ListingsController** — REST endpoints from interfaces block. Use `[Authorize]`, rate limit with `.RequireRateLimiting("authenticated")`. Multipart form parsing for create/update (JSON part + photo files).
    11. **FavoritesController**: POST toggles favorite, stores `SnapshotPrice` = current listing price. Seller sees counter via GetListingDetail.
    12. **RatingsController**: create + edit (with 7-day window check), admin delete. GET returns all non-deleted ratings for a seller with average. Enforce: only the buyer who chatted with the seller about a Sold listing can rate.
    13. **ReportListingCommand**: creates `Report` row with `TargetType = ReportTargetType.Listing`, `TargetId = listingId`, reason from fixed list (D-20). Inserts into SAME table Phase 3 uses.
    14. **Admin CategoriesController**: GET returns categories seed with Enabled state (loaded from DB toggle table or in-memory). PATCH `/admin/categories/{code}` toggles Enabled. Cannot create/delete per D-26.
    15. **Admin ModerationController**: EXTEND (or subclass of) Phase 3 reports list endpoint by adding `?targetType=` filter. Dashboard returns mixed Post/Comment/Listing rows with a type badge column, single query, no tab split per RESEARCH §Pattern 5.
  </action>
  <verify>
    <automated>cd backend && dotnet test tests/Api.Tests/Api.Tests.csproj --filter "FullyQualifiedName~Marketplace|FullyQualifiedName~Ratings|FullyQualifiedName~Moderation.Listing" -v quiet</automated>
  </verify>
  <acceptance_criteria>
    - All unskipped tests in Marketplace/, Ratings/, Moderation/ pass
    - `grep -n "ArrayPoolMemoryAllocator.CreateWithMinimalPooling" backend/src/Api/Program.cs` matches (Pitfall 8)
    - `grep -n "Cache-Control.*max-age=31536000" backend/src/Api/Program.cs` matches
    - `grep -rn "FromSqlInterpolated" backend/src/Api/Features/Marketplace/Listings/Queries/SearchListingsQuery.cs` matches
    - `grep -rn "CONTAINS" backend/src/Api/Features/Marketplace/Listings/Queries/SearchListingsQuery.cs` matches
    - Photo pipeline processes sequentially: `grep -n "Task.WhenAll" backend/src/Api/Features/Marketplace/Listings/ListingPhotoService.cs` returns nothing
    - `grep -n "TargetType = ReportTargetType.Listing" backend/src/Api/Features/Marketplace/Reports/ReportListingCommand.cs` matches
    - `grep -rn "class.*: Hub" backend/src/Api/Features/Marketplace/` returns nothing (no parallel hub)
  </acceptance_criteria>
  <done>Listings CRUD + search + favorites + ratings + reports + admin taxonomy endpoints live, all unit/integration tests green, audit log fires on update.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Chat persistence + NotificationHub extension + chat image reuse</name>
  <files>
    backend/src/Api/Infrastructure/Realtime/NotificationHub.cs,
    backend/src/Api/Features/Chat/Commands/SendMessageCommand.cs,
    backend/src/Api/Features/Chat/Commands/MarkReadCommand.cs,
    backend/src/Api/Features/Chat/Commands/CreateConversationCommand.cs,
    backend/src/Api/Features/Chat/Queries/GetConversationsQuery.cs,
    backend/src/Api/Features/Chat/Queries/GetMessageHistoryQuery.cs,
    backend/src/Api/Features/Chat/Queries/GetUnreadCountQuery.cs,
    backend/src/Api/Features/Chat/ChatController.cs,
    backend/src/Api/Features/Chat/ChatImageService.cs
  </files>
  <read_first>
    - .planning/phases/04-marketplace-chat/04-RESEARCH.md §"Pattern 4" (chat persistence), §"Code Examples" (hub extension exact code), §Pitfall 4 (soft-deleted unread), §Pitfall 7 (bairro bypass)
    - .planning/phases/04-marketplace-chat/04-CONTEXT.md §decisions (D-12 extend existing hub, D-13 persist forever, D-14 reuse ImageSharp, D-15 entry points, D-16 1:1 only)
    - backend/src/Api/Infrastructure/Realtime/NotificationHub.cs (EXISTING hub from Phase 1 — MUST be extended, not replaced)
    - .planning/phases/01-infrastructure-auth/01-01-SUMMARY.md (hub auth setup)
    - .planning/phases/03-feed-posts/03-01-SUMMARY.md (notification system to reuse for "new message" and "rate this transaction")
    - backend/src/Api/Features/Marketplace/Listings/ListingPhotoService.cs (Task 2 — pattern for chat images)
  </read_first>
  <behavior>
    - Unskip ChatHubTests, ConversationRepositoryTests, UnreadCountTests from Task 0.
    - SendMessage integration test: persists Message → updates LastMessageAt → pushes to both participants via Clients.Group.
    - Unread count query ignores soft-deleted messages.
    - JoinConversation rejects non-participants with HubException.
    - CreateConversation dedupes when (listingId, buyerId, sellerId) tuple already has an active conversation.
  </behavior>
  <action>
    1. **Extend `NotificationHub`** (existing class — DO NOT create `ChatHub`). Add methods EXACTLY as RESEARCH §"Code Examples":
       ```csharp
       public async Task JoinConversation(Guid conversationId) {
           var userId = Guid.Parse(Context.UserIdentifier!);
           var isParticipant = await _db.ConversationParticipants
               .AnyAsync(p => p.ConversationId == conversationId && p.UserId == userId);
           if (!isParticipant) throw new HubException("Not a participant");
           await Groups.AddToGroupAsync(Context.ConnectionId, $"conv:{conversationId}");
       }
       public async Task LeaveConversation(Guid conversationId) {
           await Groups.RemoveFromGroupAsync(Context.ConnectionId, $"conv:{conversationId}");
       }
       ```
    2. **CreateConversationCommand**: input `{ listingId }`. Looks up existing Active conversation with (ListingId, buyerId=caller, sellerId=listing.SellerId) — if found, returns it (Research Open Question 3 dedupe). Otherwise creates Conversation + 2 ConversationParticipant rows. **GUARD: verify `listing.BairroId == caller.BairroId` explicitly** (Pitfall 7 — don't rely only on global query filter).
    3. **SendMessageCommand** (MediatR): input `{ conversationId, text?, image? }`. Verify caller is a participant. Verify listing.BairroId matches (Pitfall 7). Persist Message (Text or ImagePath via ChatImageService), update `Conversation.LastMessageAt = UtcNow`. After persist, broadcast via `IHubContext<NotificationHub>`:
       ```csharp
       await _hubContext.Clients.Group($"conv:{conversationId}")
           .SendAsync("MessageReceived", messageDto);
       var recipientId = participants.Single(p => p.UserId != senderId).UserId;
       var newUnread = await _mediator.Send(new GetUnreadCountQuery(recipientId));
       await _hubContext.Clients.User(recipientId.ToString())
           .SendAsync("UnreadChanged", new { total = newUnread });
       ```
       Also trigger Phase 3 notification system "Nova mensagem de {senderName}" for the recipient. Rate limit: tag endpoint with SlidingWindowLimiter `chat-send` (30 msg/min per user).
    4. **MarkReadCommand**: sets `LastReadAt = UtcNow` on caller's ConversationParticipant row, broadcasts `ConversationRead` event to the group.
    5. **GetUnreadCountQuery**: implement EXACT query from RESEARCH §Pattern 4 — single query with `m.DeletedAt == null` filter (Pitfall 4):
       ```csharp
       var unreadTotal = await _db.ConversationParticipants
           .Where(p => p.UserId == currentUserId && !p.SoftDeleted)
           .SelectMany(p => _db.Messages
               .Where(m => m.ConversationId == p.ConversationId
                        && m.SenderId != currentUserId
                        && m.DeletedAt == null
                        && (p.LastReadAt == null || m.SentAt > p.LastReadAt)))
           .CountAsync();
       ```
    6. **GetConversationsQuery**: returns list sorted by `LastMessageAt DESC`, joined with Listing (title, first photo thumb) and other user (display name, IsVerified). Includes per-conversation unread count.
    7. **GetMessageHistoryQuery**: cursor-based. Input `{ conversationId, before?, limit=50 }`. `WHERE ConversationId = @id AND (before == null OR SentAt < before) AND DeletedAt IS NULL ORDER BY SentAt DESC LIMIT @limit`. Verify caller is participant.
    8. **ChatImageService**: reuse listing photo pipeline pattern — 1920x1080 max, 85% JPEG, stored at `wwwroot/uploads/chat/{yyyy}/{MM}/{conversationId}/{guid}.jpg`. Size cap 5MB (return 413 otherwise, per VALIDATION.md manual check).
    9. **ChatController** REST endpoints from interfaces block. `[Authorize]`, rate limiting, multipart parsing on SendMessage.
    10. Do NOT call `connection.start()` anywhere server-side logic assumes client reconnect — client handles via `withAutomaticReconnect` (Pitfall 2).
  </action>
  <verify>
    <automated>cd backend && dotnet test tests/Api.Tests/Api.Tests.csproj --filter "FullyQualifiedName~Chat" -v quiet</automated>
  </verify>
  <acceptance_criteria>
    - All Chat tests green (unskipped)
    - `grep -c "class NotificationHub" backend/src/Api/Infrastructure/Realtime/NotificationHub.cs` == 1 (extended, not duplicated)
    - `grep -rn "class.*ChatHub.*:.*Hub" backend/src/Api/Features/Chat/` returns nothing (no parallel hub)
    - `grep -n "JoinConversation" backend/src/Api/Infrastructure/Realtime/NotificationHub.cs` matches
    - `grep -n "conv:" backend/src/Api/Features/Chat/Commands/SendMessageCommand.cs` matches (group naming)
    - `grep -n "DeletedAt == null" backend/src/Api/Features/Chat/Queries/GetUnreadCountQuery.cs` matches (Pitfall 4)
    - `grep -n "listing.BairroId" backend/src/Api/Features/Chat/Commands/SendMessageCommand.cs` matches (Pitfall 7 guard)
    - Build + full backend test suite: `dotnet test` exits 0
  </acceptance_criteria>
  <done>Chat REST + hub extension live, unread count correct, dedupe works, bairro-scoped, audit-logged, all tests green.</done>
</task>

</tasks>

<verification>
- `dotnet build` and `dotnet test` both exit 0
- All 13 MKT-* requirement IDs represented in at least one test (MKT-012/013 deferred, acknowledged in objective)
- Migration applies AND rolls back cleanly on LocalDB
- Manual Wave 0 smoke: `FTS_SMOKE=1 dotnet test --filter FullTextSyncLag` passes against a SmarterASP sandbox DB once credentials available
- No parallel SignalR hub created: `grep -rn "public class.*Hub.*:.*Hub" backend/src/Api/` returns only the existing NotificationHub
- VALIDATION.md per-task map filled with 13 MKT-* rows and sign-off checklist updated
</verification>

<success_criteria>
- Verified user can POST a listing with photos and it persists + photos appear under wwwroot/uploads/listings/
- Bairro-scoped grid query returns only current-bairro active listings sorted by recency
- Full-text search via CONTAINS returns results; malformed terms return 400, not 500
- 1:1 chat: create conversation dedupes, send message persists + broadcasts via existing hub, unread count is correct and ignores soft-deleted
- Report with TargetType=Listing lands in the same Reports table as Phase 3 posts
- Rating 1-5 with 7-day edit window enforced
- All backend tests green, audit log fires on listing edits, no parallel SignalR hub
</success_criteria>

<output>
After completion, create `.planning/phases/04-marketplace-chat/04-01-SUMMARY.md` documenting: endpoints created, hub methods added, entities + migration name, FT catalog confirmation, Wave 0 smoke test result, and any deviations from RESEARCH.md.
</output>
