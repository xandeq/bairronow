---
phase: 02-verification-neighborhoods
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - packages/shared-api-client/src/index.ts
  - packages/shared-validators/src/index.ts
  - frontend/src/lib/api.ts
  - frontend/src/lib/onboarding.ts
  - frontend/src/app/(onboarding)/cep-lookup/page.tsx
  - frontend/src/app/(onboarding)/proof-upload/page.tsx
  - frontend/src/app/(onboarding)/pending/page.tsx
  - frontend/src/app/(main)/profile/page.tsx
  - frontend/src/app/(main)/admin/verifications/page.tsx
  - frontend/src/components/VerifiedBadge.tsx
  - frontend/src/components/ProofDropzone.tsx
  - frontend/src/lib/verification.ts
autonomous: true
requirements:
  - VER-001
  - VER-002
  - VER-003
  - VER-004
  - VER-009
  - AUTH-010
  - AUTH-012

must_haves:
  truths:
    - "Web user completes onboarding: CEP lookup → proof upload → pending → approved badge — all against real API at api.bairronow.com.br"
    - "Admin page at /admin/verifications lists pending verifications and can approve/reject"
    - "Profile page shows DisplayName, photo, bio, bairro, and Verified badge when verified"
    - "Proof upload uses browser-image-compression and react-dropzone, enforces 5MB + image/pdf"
    - "Pending page polls GET /api/v1/verification/me every 5s and redirects to /feed on approved"
  artifacts:
    - path: packages/shared-api-client/src/index.ts
      provides: "createVerificationApi, createProfileApi, createAdminApi, extended createCepApi calling backend"
    - path: frontend/src/app/(onboarding)/cep-lookup/page.tsx
      provides: "CEP form using shared client hitting /api/v1/cep/{cep}"
    - path: frontend/src/app/(onboarding)/proof-upload/page.tsx
      provides: "Dropzone + compress + multipart POST /api/v1/verification"
    - path: frontend/src/app/(onboarding)/pending/page.tsx
      provides: "Status polling + approved/rejected UX"
    - path: frontend/src/app/(main)/profile/page.tsx
      provides: "Profile view + edit (displayName, bio) + verified badge"
    - path: frontend/src/app/(main)/admin/verifications/page.tsx
      provides: "Admin review queue UI"
    - path: frontend/src/components/VerifiedBadge.tsx
      provides: "Reusable verified badge component"
  key_links:
    - from: frontend/src/app/(onboarding)/cep-lookup/page.tsx
      to: packages/shared-api-client/src/index.ts
      via: "cepApi.lookup hitting /api/v1/cep/{cep} (not ViaCEP directly)"
      pattern: "/api/v1/cep"
    - from: frontend/src/app/(onboarding)/proof-upload/page.tsx
      to: packages/shared-api-client/src/index.ts
      via: "verificationApi.submit multipart POST"
      pattern: "FormData|multipart"
    - from: frontend/src/app/(onboarding)/pending/page.tsx
      to: packages/shared-api-client/src/index.ts
      via: "verificationApi.getMyStatus polled via setInterval"
      pattern: "getMyStatus|/verification/me"
    - from: frontend/src/app/(main)/admin/verifications/page.tsx
      to: packages/shared-api-client/src/index.ts
      via: "adminApi.listPending + approve + reject"
      pattern: "/admin/verifications"
---

<objective>
Wire the Next.js web onboarding flow to the real Phase 2 backend: replace the ViaCEP-direct lookup with the backend-proxied CEP endpoint, implement proof upload with compression + dropzone, add polling status page, create admin review queue, and extend profile with edit + verified badge. Depends on 02-01 for API + shared types.

Purpose: Close the web trust loop — a neighbor on the browser can fully verify and get their bairro badge.
Output: Updated shared client, 5 new/updated pages, 2 components.
</objective>

<execution_context>
@C:/Users/acq20/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/acq20/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-verification-neighborhoods/02-01-PLAN.md
@.planning/phases/02-verification-neighborhoods/02-01-SUMMARY.md
@CLAUDE.md
@frontend/CLAUDE.md
@frontend/src/lib/onboarding.ts
@packages/shared-api-client/src/index.ts
@packages/shared-validators/src/index.ts

<interfaces>
New DTOs from packages/shared-types/src/verification.ts (created in 02-01):
```ts
CepLookupResult, VerificationStatusDto, AdminVerificationListItem, ProfileDto, UpdateProfileRequest
```

Existing shared-api-client (packages/shared-api-client/src/index.ts) exports `createApiClient`, `createAuthApi`, `createCepApi`. The existing `createCepApi` hits ViaCEP DIRECTLY — must be REPLACED to hit backend `/api/v1/cep/{cep}` via the authenticated axios client.

Onboarding pages already exist at frontend/src/app/(onboarding)/{cep-lookup,proof-upload,pending}/page.tsx but they consume the stub store. They must be rewritten against real endpoints.

frontend/CLAUDE.md warns Next.js has breaking changes — always read node_modules/next/dist/docs/ before touching app router / server component patterns.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend shared-api-client + shared-validators for verification, replace ViaCEP-direct with backend-proxied cepApi</name>
  <files>
    packages/shared-api-client/src/index.ts,
    packages/shared-validators/src/index.ts
  </files>
  <read_first>
    - packages/shared-api-client/src/index.ts (current createApiClient/createCepApi — createCepApi currently hits viacep.com.br DIRECTLY via its own axios instance. Must change to take the authenticated client and hit /api/v1/cep/{cep})
    - packages/shared-validators/src/index.ts (zod schemas pattern — add verification schemas)
    - packages/shared-types/src/verification.ts (DTOs created in 02-01, imported here)
  </read_first>
  <behavior>
    - Rewrite `createCepApi(client: AxiosInstance)` so it hits `GET /api/v1/cep/{clean}` and returns `CepLookupResult` (not `CepAddress`). Keep name `createCepApi`.
    - Add `createVerificationApi(client)` with methods: `submit(formData: FormData): Promise<VerificationStatusDto>` (POST /api/v1/verification, Content-Type multipart), `getMyStatus(): Promise<VerificationStatusDto>` (GET /api/v1/verification/me).
    - Add `createProfileApi(client)` with: `getMe(): Promise<ProfileDto>`, `updateMe(body: UpdateProfileRequest): Promise<ProfileDto>`.
    - Add `createAdminVerificationApi(client)` with: `listPending(skip, take): Promise<{items: AdminVerificationListItem[]; total: number}>`, `approve(id)`, `reject(id, reason)`, `getProofUrl(id)` returning `${baseURL}/api/v1/admin/verifications/${id}/proof`.
    - shared-validators: add `proofUploadSchema` (zod) validating {cep: cepSchema, numero?: z.string().max(20)}, and `updateProfileSchema` validating {displayName: z.string().min(1).max(80), bio: z.string().max(160)}. Export both.
  </behavior>
  <action>
    1. Read both files fully.
    2. In shared-api-client, import new types from `@bairronow/shared-types`, remove the standalone `axios.create({ baseURL: 'https://viacep.com.br/ws' })` block, rewrite `createCepApi` to take `client: AxiosInstance` and call `client.get<CepLookupResult>('/api/v1/cep/' + clean)`.
    3. Add the three new `create*Api` factories.
    4. In shared-validators add the two schemas + types.
    5. Build both: `pnpm --filter @bairronow/shared-api-client build && pnpm --filter @bairronow/shared-validators build`.
  </action>
  <verify>
    <automated>cd /d D:\claude-code\nosso-vizinho &amp;&amp; grep -l "createVerificationApi" packages/shared-api-client/src/index.ts &amp;&amp; grep -l "createProfileApi" packages/shared-api-client/src/index.ts &amp;&amp; grep -l "createAdminVerificationApi" packages/shared-api-client/src/index.ts &amp;&amp; grep -l "/api/v1/cep" packages/shared-api-client/src/index.ts &amp;&amp; ! grep "viacep.com.br" packages/shared-api-client/src/index.ts &amp;&amp; grep -l "proofUploadSchema" packages/shared-validators/src/index.ts &amp;&amp; grep -l "updateProfileSchema" packages/shared-validators/src/index.ts &amp;&amp; pnpm --filter @bairronow/shared-api-client build 2>&1 | tail -5 &amp;&amp; pnpm --filter @bairronow/shared-validators build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - packages/shared-api-client/src/index.ts contains the exact strings `createVerificationApi`, `createProfileApi`, `createAdminVerificationApi`, `/api/v1/cep/`, `/api/v1/verification`, `/api/v1/profile/me`, `/api/v1/admin/verifications`
    - packages/shared-api-client/src/index.ts does NOT contain the exact string `viacep.com.br` anywhere (the ViaCEP-direct call is removed)
    - packages/shared-validators/src/index.ts contains the exact strings `proofUploadSchema`, `updateProfileSchema`
    - `pnpm --filter @bairronow/shared-api-client build` exits 0
    - `pnpm --filter @bairronow/shared-validators build` exits 0
  </acceptance_criteria>
  <done>Shared packages updated and built; web + mobile can import the new APIs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite onboarding pages + add admin queue + profile + VerifiedBadge hooked to real API</name>
  <files>
    frontend/src/lib/api.ts,
    frontend/src/lib/onboarding.ts,
    frontend/src/lib/verification.ts,
    frontend/src/app/(onboarding)/cep-lookup/page.tsx,
    frontend/src/app/(onboarding)/proof-upload/page.tsx,
    frontend/src/app/(onboarding)/pending/page.tsx,
    frontend/src/app/(main)/profile/page.tsx,
    frontend/src/app/(main)/admin/verifications/page.tsx,
    frontend/src/components/VerifiedBadge.tsx,
    frontend/src/components/ProofDropzone.tsx,
    frontend/package.json
  </files>
  <read_first>
    - frontend/src/lib/api.ts (current client factory — how it wires tokenStore; must export new api factories)
    - frontend/src/lib/onboarding.ts (current stub store to extend with real status polling state)
    - frontend/src/app/(onboarding)/cep-lookup/page.tsx (existing stub to rewrite)
    - frontend/src/app/(onboarding)/proof-upload/page.tsx (existing stub to rewrite)
    - frontend/src/app/(onboarding)/pending/page.tsx (existing stub to rewrite)
    - frontend/src/app/(main)/ (check the (main) route group layout for auth-gated routes)
    - frontend/AGENTS.md (Next.js version caveats)
    - node_modules/next/dist/docs/ (per frontend/CLAUDE.md — before touching app router code)
    - frontend/package.json (verify react-dropzone + browser-image-compression present; add if missing)
  </read_first>
  <behavior>
    - frontend/package.json: ensure `react-dropzone@^14`, `browser-image-compression@^2` are installed; run `pnpm --filter frontend add react-dropzone browser-image-compression` if absent.
    - frontend/src/lib/api.ts: in addition to existing authApi, export `verificationApi`, `profileApi`, `adminVerificationApi`, `cepApi` created via the new factories from @bairronow/shared-api-client, using the existing shared client instance.
    - frontend/src/lib/verification.ts: a small hook `useVerificationPolling(intervalMs=5000)` using `useEffect + setInterval` calling `verificationApi.getMyStatus()`, returns `{status, lastUpdate}`; cleans up on unmount.
    - frontend/src/lib/onboarding.ts: keep existing store but replace `ProofStatus` usage with `VerificationStatusDto`; add `setStatus(dto: VerificationStatusDto)` method.
    - frontend/src/components/VerifiedBadge.tsx: `'use client'` component taking `{verified: boolean; size?: 'sm'|'md'}`, renders a green check pill with "Vizinho verificado" label when `verified=true`, nothing otherwise. Tailwind classes only.
    - frontend/src/components/ProofDropzone.tsx: client component using `useDropzone` from react-dropzone; accepts `image/jpeg, image/png, image/webp, application/pdf`; max 5MB; on image drop runs `imageCompression(file, {maxSizeMB: 1, maxWidthOrHeight: 1600})`; exposes `onFile(file: File)` prop.
    - cep-lookup/page.tsx: client page using `react-hook-form` + `cepSchema`, calls `cepApi.lookup(cep)`, on success stores `CepLookupResult` in onboarding store and pushes to `/proof-upload`. Shows bairro name + "Fora de Vila Velha" warning if `bairroId == null`.
    - proof-upload/page.tsx: client page using ProofDropzone. On submit builds FormData with `cep`, `numero`, `proof` and calls `verificationApi.submit(fd)`. On 200 push to `/pending`.
    - pending/page.tsx: client page using `useVerificationPolling(5000)`. States:
      - pending → spinner + "Documento em análise"
      - approved → success + redirect `/feed` after 2s
      - rejected → show `rejectionReason` + button back to `/proof-upload`
    - profile/page.tsx: client page. `useEffect` loads `profileApi.getMe()`. Form with displayName + bio (validated via `updateProfileSchema`), submit calls `profileApi.updateMe`. Renders `<VerifiedBadge verified={profile.isVerified} />`. Shows bairroNome.
    - admin/verifications/page.tsx: client page that calls `adminVerificationApi.listPending(0, 20)`, renders table with columns [Email, CEP, Bairro, Duplicado?, Proof link, Approve btn, Reject btn (prompt reason)]. Proof link opens `adminVerificationApi.getProofUrl(id)` in new tab. After approve/reject, refetches list. If API returns 403 (non-admin), show "Acesso negado".
    - All new client pages use `'use client'` directive at top.
  </behavior>
  <action>
    1. Read listed files + check node_modules/next/dist/docs/ for current app-router file/route conventions.
    2. Add missing dependencies via pnpm.
    3. Update api.ts and onboarding store.
    4. Build VerifiedBadge + ProofDropzone components.
    5. Rewrite 3 onboarding pages.
    6. Create profile page + admin page.
    7. Run typecheck: `pnpm --filter frontend exec tsc --noEmit`.
    8. Run build: `pnpm --filter frontend build`.
  </action>
  <verify>
    <automated>cd /d D:\claude-code\nosso-vizinho &amp;&amp; grep -l "verificationApi\|profileApi\|adminVerificationApi" frontend/src/lib/api.ts &amp;&amp; grep -l "cepApi.lookup" frontend/src/app/\(onboarding\)/cep-lookup/page.tsx &amp;&amp; grep -l "FormData" frontend/src/app/\(onboarding\)/proof-upload/page.tsx &amp;&amp; grep -l "verificationApi.submit" frontend/src/app/\(onboarding\)/proof-upload/page.tsx &amp;&amp; grep -l "getMyStatus\|useVerificationPolling" frontend/src/app/\(onboarding\)/pending/page.tsx &amp;&amp; grep -l "VerifiedBadge" frontend/src/app/\(main\)/profile/page.tsx &amp;&amp; grep -l "adminVerificationApi.listPending" frontend/src/app/\(main\)/admin/verifications/page.tsx &amp;&amp; grep -l "react-dropzone" frontend/src/components/ProofDropzone.tsx &amp;&amp; grep -l "browser-image-compression" frontend/src/components/ProofDropzone.tsx &amp;&amp; grep -l "Vizinho verificado" frontend/src/components/VerifiedBadge.tsx &amp;&amp; pnpm --filter frontend exec tsc --noEmit 2>&1 | tail -10 &amp;&amp; pnpm --filter frontend build 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/lib/api.ts contains the exact strings `verificationApi`, `profileApi`, `adminVerificationApi`, `cepApi`
    - frontend/src/app/(onboarding)/cep-lookup/page.tsx contains the exact strings `'use client'`, `cepApi.lookup`
    - frontend/src/app/(onboarding)/proof-upload/page.tsx contains the exact strings `'use client'`, `FormData`, `verificationApi.submit`, `ProofDropzone`
    - frontend/src/app/(onboarding)/pending/page.tsx contains the exact strings `'use client'`, `useVerificationPolling` (or `getMyStatus`), `approved`, `rejected`
    - frontend/src/app/(main)/profile/page.tsx contains the exact strings `'use client'`, `profileApi.getMe`, `profileApi.updateMe`, `VerifiedBadge`
    - frontend/src/app/(main)/admin/verifications/page.tsx contains the exact strings `'use client'`, `adminVerificationApi.listPending`, `approve`, `reject`
    - frontend/src/components/VerifiedBadge.tsx contains the exact string `Vizinho verificado`
    - frontend/src/components/ProofDropzone.tsx contains the exact strings `react-dropzone`, `browser-image-compression`, `5` (for 5MB limit)
    - frontend/package.json contains `react-dropzone` and `browser-image-compression`
    - `pnpm --filter frontend exec tsc --noEmit` exits 0
    - `pnpm --filter frontend build` exits 0
  </acceptance_criteria>
  <done>Web onboarding end-to-end works against real API; admin queue and profile shipped; typecheck + build green.</done>
</task>

</tasks>

<verification>
Manual smoke after deploy (human-verify in separate wave):
1. Visit /cep-lookup, enter a Vila Velha CEP (29101-160) → sees bairro, continues.
2. Upload a test JPG on /proof-upload → /pending shows spinner.
3. Flip user is_admin in DB, visit /admin/verifications → see the entry → click Approve.
4. /pending auto-redirects to /feed, profile page shows Verified badge.
</verification>

<success_criteria>
- ViaCEP direct call fully removed from shared-api-client
- All onboarding pages consume backend endpoints only
- Admin page implements list + approve + reject
- Profile page renders verified badge bound to ProfileDto.isVerified
- Build + typecheck clean
</success_criteria>

<output>
After completion, create `.planning/phases/02-verification-neighborhoods/02-02-SUMMARY.md`.
</output>
