---
phase: 02-verification-neighborhoods
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - mobile/src/lib/api.ts
  - mobile/src/lib/onboarding-store.ts
  - mobile/src/lib/verification-polling.ts
  - mobile/src/components/VerifiedBadge.tsx
  - mobile/src/components/ProofPicker.tsx
  - mobile/app/cep-lookup.tsx
  - mobile/app/proof-upload.tsx
  - mobile/app/pending.tsx
  - mobile/app/profile.tsx
  - mobile/app/_layout.tsx
  - mobile/package.json
  - mobile/app.json
autonomous: true
requirements:
  - VER-001
  - VER-002
  - VER-004
  - VER-009
  - AUTH-010

must_haves:
  truths:
    - "Expo mobile app onboarding: CEP → proof picker → pending → approved badge works against the real API"
    - "Mobile CEP screen calls backend /api/v1/cep/{cep} not ViaCEP directly"
    - "Mobile proof upload uses expo-image-picker + expo-document-picker (images OR PDF) and POSTs multipart"
    - "Mobile pending screen polls /api/v1/verification/me every 5s and navigates to /feed on approved"
    - "Mobile profile screen shows display name, bio, bairro, and VerifiedBadge"
  artifacts:
    - path: mobile/app/cep-lookup.tsx
      provides: "CEP screen using backend-proxied cepApi"
    - path: mobile/app/proof-upload.tsx
      provides: "Image/PDF picker + multipart submission"
    - path: mobile/app/pending.tsx
      provides: "Status polling + navigation on approval"
    - path: mobile/app/profile.tsx
      provides: "Profile view + edit + badge"
    - path: mobile/src/components/VerifiedBadge.tsx
      provides: "RN verified badge"
    - path: mobile/src/components/ProofPicker.tsx
      provides: "RN image/pdf picker wrapper"
  key_links:
    - from: mobile/app/cep-lookup.tsx
      to: mobile/src/lib/api.ts
      via: "cepApi.lookup → backend /api/v1/cep"
      pattern: "/api/v1/cep"
    - from: mobile/app/proof-upload.tsx
      to: mobile/src/lib/api.ts
      via: "verificationApi.submit with FormData"
      pattern: "FormData|multipart"
    - from: mobile/app/pending.tsx
      to: mobile/src/lib/verification-polling.ts
      via: "polling hook"
      pattern: "getMyStatus|setInterval"
---

<objective>
Implement the Phase 2 verification flow on Expo/React Native: replace the ViaCEP-direct CEP screen with the backend endpoint, add proof picker (images + PDF) with multipart upload, add polling status screen and profile screen, reuse shared types/validators/client from the monorepo. Depends on 02-01 (API + shared types); runs in parallel with 02-02.

Purpose: Mobile feature parity for verification — neighbors can verify from phone.
Output: 4 updated/new screens, 2 components, polling hook, api wiring.
</objective>

<execution_context>
@C:/Users/acq20/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/acq20/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-verification-neighborhoods/02-01-PLAN.md
@.planning/phases/02-verification-neighborhoods/02-01-SUMMARY.md
@CLAUDE.md
@mobile/app/cep-lookup.tsx
@mobile/app/proof-upload.tsx
@mobile/app/pending.tsx
@mobile/app/_layout.tsx
@mobile/src/lib/api.ts
@packages/shared-api-client/src/index.ts

<interfaces>
Mobile already has:
- expo-router file-based routing in mobile/app/
- mobile/src/lib/api.ts wraps shared-api-client with expo-secure-store token store
- mobile/src/lib/auth-store.ts zustand auth store
- mobile/src/components/{Input,Button}.tsx primitives

Shared DTOs from 02-01: `CepLookupResult, VerificationStatusDto, ProfileDto, UpdateProfileRequest` in `@bairronow/shared-types`.
Shared factories from 02-02 Task 1: `createVerificationApi, createProfileApi, createCepApi (backend-proxied)` in `@bairronow/shared-api-client`.

Existing mobile/app/cep-lookup.tsx calls `cepApi.lookup(cep)` — but that cepApi was ViaCEP-direct. After 02-02 Task 1 lands, the import contract changes: the new `createCepApi(client)` takes the authenticated client. Mobile must update its api.ts to match.

React Native FormData: `fd.append('proof', { uri, name, type } as any)` is the RN pattern — different from web.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update mobile api wiring + onboarding store + VerifiedBadge + ProofPicker + polling hook</name>
  <files>
    mobile/src/lib/api.ts,
    mobile/src/lib/onboarding-store.ts,
    mobile/src/lib/verification-polling.ts,
    mobile/src/components/VerifiedBadge.tsx,
    mobile/src/components/ProofPicker.tsx,
    mobile/package.json,
    mobile/app.json
  </files>
  <read_first>
    - mobile/src/lib/api.ts (current client wiring, how authApi is exported, how shared client is constructed)
    - mobile/src/lib/auth-store.ts (zustand pattern used in mobile to mirror for onboarding store)
    - mobile/package.json (verify expo-image-picker, expo-document-picker, expo-image-manipulator presence; add via `npx expo install` if missing)
    - mobile/app.json (permissions config; need CAMERA + PHOTO_LIBRARY description strings for iOS)
    - packages/shared-api-client/src/index.ts (post-02-02 factories to import)
  </read_first>
  <behavior>
    - mobile/package.json: ensure `expo-image-picker`, `expo-document-picker`, `expo-image-manipulator` installed via `cd mobile && npx expo install expo-image-picker expo-document-picker expo-image-manipulator`.
    - mobile/app.json: add iOS `NSCameraUsageDescription` and `NSPhotoLibraryUsageDescription` strings in PT-BR ("Precisamos acessar sua câmera para enviar comprovante de residência" etc.) if not present.
    - mobile/src/lib/api.ts: export `cepApi`, `verificationApi`, `profileApi` built from the shared factories using the authenticated client instance (same instance already used by authApi). Remove any direct ViaCEP HTTP instance.
    - mobile/src/lib/onboarding-store.ts: zustand store (NO persist middleware or persist with expo-secure-store / AsyncStorage — pick AsyncStorage since secure-store has size limits) tracking `{address: CepLookupResult | null, status: VerificationStatusDto | null, setAddress, setStatus, reset}`.
    - mobile/src/lib/verification-polling.ts: `useVerificationPolling(enabled: boolean, intervalMs=5000)` React hook using useEffect + setInterval calling `verificationApi.getMyStatus()`, stores result in local state, returns `{status, loading, error}`. Cleans up interval on unmount.
    - mobile/src/components/VerifiedBadge.tsx: RN component using View+Text with green background, shows "Verificado" label + checkmark icon (use unicode ✓ or @expo/vector-icons if already installed) when `verified` prop true.
    - mobile/src/components/ProofPicker.tsx: wraps expo-image-picker (`launchImageLibraryAsync` + `launchCameraAsync`) and expo-document-picker (`getDocumentAsync` for PDFs). Exposes three buttons: "Escolher foto", "Tirar foto", "Enviar PDF". On image, runs `ImageManipulator.manipulateAsync(uri, [{resize:{width:1600}}], {compress:0.8, format:'jpeg'})`. Returns picked asset via `onFile({uri, name, type, size})` prop. Enforces 5MB max; alerts if exceeded.
  </behavior>
  <action>
    1. Read current mobile/src/lib/api.ts + auth-store to understand patterns.
    2. Install Expo packages via `npx expo install ...`.
    3. Update app.json permissions (use Edit tool after Read).
    4. Rewrite api.ts to export new API clients.
    5. Create onboarding-store, verification-polling, VerifiedBadge, ProofPicker.
    6. Run `pnpm --filter mobile exec tsc --noEmit` to typecheck.
  </action>
  <verify>
    <automated>cd /d D:\claude-code\nosso-vizinho &amp;&amp; grep -l "verificationApi\|profileApi" mobile/src/lib/api.ts &amp;&amp; ! grep "viacep.com.br" mobile/src/lib/api.ts &amp;&amp; grep -l "useVerificationPolling" mobile/src/lib/verification-polling.ts &amp;&amp; grep -l "Verificado" mobile/src/components/VerifiedBadge.tsx &amp;&amp; grep -l "expo-image-picker" mobile/src/components/ProofPicker.tsx &amp;&amp; grep -l "expo-document-picker" mobile/src/components/ProofPicker.tsx &amp;&amp; grep -l "ImageManipulator\|manipulateAsync" mobile/src/components/ProofPicker.tsx &amp;&amp; grep -l "expo-image-picker" mobile/package.json &amp;&amp; grep -l "NSCameraUsageDescription\|Precisamos acessar" mobile/app.json &amp;&amp; pnpm --filter mobile exec tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - mobile/src/lib/api.ts contains the exact strings `verificationApi`, `profileApi`, `cepApi`
    - mobile/src/lib/api.ts does NOT contain the exact string `viacep.com.br`
    - mobile/src/lib/verification-polling.ts contains the exact strings `useVerificationPolling`, `setInterval`, `getMyStatus`
    - mobile/src/lib/onboarding-store.ts contains the exact strings `zustand`, `setAddress`, `setStatus`
    - mobile/src/components/VerifiedBadge.tsx contains the exact string `Verificado`
    - mobile/src/components/ProofPicker.tsx contains the exact strings `expo-image-picker`, `expo-document-picker`, `ImageManipulator`, `5`
    - mobile/package.json contains `expo-image-picker`, `expo-document-picker`, `expo-image-manipulator`
    - mobile/app.json contains `NSCameraUsageDescription`
    - `pnpm --filter mobile exec tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Mobile infrastructure for verification ready; shared client wired; permissions declared; typecheck clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite cep-lookup/proof-upload/pending screens + add profile screen hitting real API</name>
  <files>
    mobile/app/cep-lookup.tsx,
    mobile/app/proof-upload.tsx,
    mobile/app/pending.tsx,
    mobile/app/profile.tsx,
    mobile/app/_layout.tsx
  </files>
  <read_first>
    - mobile/app/cep-lookup.tsx (current screen — uses the old direct ViaCEP cepApi; rewrite against backend + CepLookupResult)
    - mobile/app/proof-upload.tsx (current stub screen)
    - mobile/app/pending.tsx (current stub screen)
    - mobile/app/_layout.tsx (Stack config — add profile route if missing)
    - mobile/src/components/Input.tsx, mobile/src/components/Button.tsx (primitives in use)
    - mobile/src/lib/onboarding-store.ts (created in Task 1)
  </read_first>
  <behavior>
    - cep-lookup.tsx: on press "Buscar", calls `cepApi.lookup(cep)` (now returns `CepLookupResult` with bairroId). Stores result in onboarding store. Shows card with logradouro + bairro. If `bairroId == null` shows warning "Ainda não atendemos este bairro" but still allows continue. Button "Confirmar e continuar" navigates via `router.push('/proof-upload')`.
    - proof-upload.tsx: uses `ProofPicker` component. Shows selected file name + size. On "Enviar" button: builds `FormData`, appends `cep` (from store), `numero` (optional TextInput), `proof` as `{ uri, name, type } as any` (RN pattern). Calls `verificationApi.submit(fd)`. On success, updates store + `router.replace('/pending')`. Alerts on error.
    - pending.tsx: uses `useVerificationPolling(true, 5000)`. Renders different UI per status: `pending` → ActivityIndicator + "Documento em análise"; `approved` → success view + `setTimeout(() => router.replace('/feed'), 2000)`; `rejected` → shows rejectionReason + button "Enviar novamente" navigating to `/proof-upload`.
    - profile.tsx: new screen. On mount `profileApi.getMe()`, renders editable form (displayName, bio) + `<VerifiedBadge verified={profile.isVerified} />` + bairro name. "Salvar" calls `profileApi.updateMe`.
    - _layout.tsx: ensure `profile` and any missing routes are included in Stack.
  </behavior>
  <action>
    1. Read each current screen to preserve working patterns (styles, imports, router usage).
    2. Rewrite screens one at a time, preserving style objects where sensible.
    3. Add profile.tsx new screen.
    4. Update _layout.tsx Stack to include profile route.
    5. Typecheck: `pnpm --filter mobile exec tsc --noEmit`.
    6. Metro bundle sanity check: `pnpm --filter mobile exec expo export --platform web --output-dir .expo-export-check 2>&1 | tail -20` (or skip if too slow — typecheck is primary gate).
  </action>
  <verify>
    <automated>cd /d D:\claude-code\nosso-vizinho &amp;&amp; grep -l "cepApi.lookup" mobile/app/cep-lookup.tsx &amp;&amp; grep -l "bairroId" mobile/app/cep-lookup.tsx &amp;&amp; grep -l "FormData" mobile/app/proof-upload.tsx &amp;&amp; grep -l "verificationApi.submit" mobile/app/proof-upload.tsx &amp;&amp; grep -l "ProofPicker" mobile/app/proof-upload.tsx &amp;&amp; grep -l "useVerificationPolling" mobile/app/pending.tsx &amp;&amp; grep -l "approved\|rejected" mobile/app/pending.tsx &amp;&amp; grep -l "profileApi.getMe" mobile/app/profile.tsx &amp;&amp; grep -l "VerifiedBadge" mobile/app/profile.tsx &amp;&amp; grep -l "profile" mobile/app/_layout.tsx &amp;&amp; pnpm --filter mobile exec tsc --noEmit 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - mobile/app/cep-lookup.tsx contains the exact strings `cepApi.lookup`, `bairroId`
    - mobile/app/proof-upload.tsx contains the exact strings `FormData`, `verificationApi.submit`, `ProofPicker`
    - mobile/app/pending.tsx contains the exact strings `useVerificationPolling`, `approved`, `rejected`
    - mobile/app/profile.tsx contains the exact strings `profileApi.getMe`, `VerifiedBadge`
    - mobile/app/_layout.tsx contains the exact string `profile` (Stack.Screen or route entry)
    - `pnpm --filter mobile exec tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Mobile onboarding flow fully rewired to real backend; profile with verified badge shipped; typecheck green.</done>
</task>

</tasks>

<verification>
Manual smoke (human-verify in later wave):
1. `cd mobile && npx expo start` → open on device.
2. Register / login. Enter CEP 29101-160 → see bairro.
3. Pick a JPG from library → submit → pending screen polls.
4. Admin approves from web 02-02 admin page.
5. Mobile pending screen auto-navigates to feed. Profile screen shows Verified badge.
</verification>

<success_criteria>
- Mobile consumes only backend endpoints (no ViaCEP direct)
- Image picker + document picker + compression integrated
- Polling + auto-navigation on approval works
- Profile + VerifiedBadge live
- Typecheck clean
</success_criteria>

<output>
After completion, create `.planning/phases/02-verification-neighborhoods/02-03-SUMMARY.md`.
</output>
