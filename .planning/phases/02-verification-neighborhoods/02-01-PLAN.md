---
phase: 02-verification-neighborhoods
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/NossoVizinho.Api/Models/Bairro.cs
  - src/NossoVizinho.Api/Models/Verification.cs
  - src/NossoVizinho.Api/Models/User.cs
  - src/NossoVizinho.Api/Data/AppDbContext.cs
  - src/NossoVizinho.Api/Services/ICepLookupService.cs
  - src/NossoVizinho.Api/Services/CepLookupService.cs
  - src/NossoVizinho.Api/Services/IBairroService.cs
  - src/NossoVizinho.Api/Services/BairroService.cs
  - src/NossoVizinho.Api/Services/IVerificationService.cs
  - src/NossoVizinho.Api/Services/VerificationService.cs
  - src/NossoVizinho.Api/Services/IFileStorageService.cs
  - src/NossoVizinho.Api/Services/FileStorageService.cs
  - src/NossoVizinho.Api/Controllers/v1/CepController.cs
  - src/NossoVizinho.Api/Controllers/v1/VerificationController.cs
  - src/NossoVizinho.Api/Controllers/v1/AdminVerificationController.cs
  - src/NossoVizinho.Api/Controllers/v1/ProfileController.cs
  - src/NossoVizinho.Api/Data/Seed/VilaVelhaBairros.cs
  - src/NossoVizinho.Api/Migrations/
  - src/NossoVizinho.Api/Program.cs
  - src/NossoVizinho.Api/NossoVizinho.Api.csproj
  - packages/shared-types/src/verification.ts
  - packages/shared-types/src/index.ts
autonomous: true
requirements:
  - VER-001
  - VER-002
  - VER-003
  - VER-004
  - VER-005
  - VER-006
  - VER-007
  - VER-008
  - VER-009
  - VER-010
  - AUTH-010
  - AUTH-012

must_haves:
  truths:
    - "GET /api/v1/cep/{cep} returns address + matched bairroId for valid Vila Velha CEPs"
    - "Authenticated user can POST /api/v1/verification with multipart proof file and receive {status: pending}"
    - "GET /api/v1/verification/me returns current user's verification status (none|pending|approved|rejected)"
    - "Admin user can GET /api/v1/admin/verifications?status=pending and POST /api/v1/admin/verifications/{id}/approve|reject"
    - "Approved verification sets User.IsVerified=true, User.BairroId=<id>, User.VerifiedAt=<utc>"
    - "GET /api/v1/profile/me returns {displayName, photoUrl, bio, bairro, isVerified}"
    - "Database has Bairros table seeded with at least 10 Vila Velha bairros (Praia da Costa, Itapuã, Coqueiral de Itaparica, etc.)"
    - "Duplicate proof file SHA256 across users flags Verification.IsSuspectedDuplicate=true"
  artifacts:
    - path: src/NossoVizinho.Api/Models/Bairro.cs
      provides: "Bairro entity (Id, Nome, Cidade, Uf, CepRanges, IsActive)"
    - path: src/NossoVizinho.Api/Models/Verification.cs
      provides: "Verification entity (Id, UserId, Cep, Logradouro, BairroId, ProofFilePath, ProofSha256, Status, ReviewedByUserId, ReviewedAt, RejectionReason, IsSuspectedDuplicate, IsDeleted, SubmittedAt)"
    - path: src/NossoVizinho.Api/Controllers/v1/CepController.cs
      provides: "GET /api/v1/cep/{cep}"
    - path: src/NossoVizinho.Api/Controllers/v1/VerificationController.cs
      provides: "POST /api/v1/verification, GET /api/v1/verification/me"
    - path: src/NossoVizinho.Api/Controllers/v1/AdminVerificationController.cs
      provides: "GET /api/v1/admin/verifications, POST approve/reject"
    - path: src/NossoVizinho.Api/Controllers/v1/ProfileController.cs
      provides: "GET/PUT /api/v1/profile/me (displayName, photoUrl, bio)"
    - path: src/NossoVizinho.Api/Data/Seed/VilaVelhaBairros.cs
      provides: "Static seed list of Vila Velha bairros"
  key_links:
    - from: src/NossoVizinho.Api/Services/CepLookupService.cs
      to: "https://viacep.com.br/ws + https://brasilapi.com.br/api/cep/v2"
      via: "HttpClient with BrasilAPI fallback when ViaCEP fails"
      pattern: "viacep|brasilapi"
    - from: src/NossoVizinho.Api/Services/VerificationService.cs
      to: src/NossoVizinho.Api/Services/IFileStorageService.cs
      via: "SaveProofAsync with ImageSharp resize for images, raw save for PDF"
      pattern: "ImageSharp|SixLabors"
    - from: src/NossoVizinho.Api/Services/VerificationService.cs
      to: src/NossoVizinho.Api/Models/User.cs
      via: "ApproveAsync sets User.IsVerified, BairroId, VerifiedAt"
      pattern: "user.IsVerified\\s*=\\s*true"
    - from: src/NossoVizinho.Api/Program.cs
      to: src/NossoVizinho.Api/Services/CepLookupService.cs
      via: "AddHttpClient + AddScoped registration with IMemoryCache"
      pattern: "AddHttpClient<ICepLookupService"
---

<objective>
Build the entire backend trust layer for Phase 2: bairro entity + Vila Velha seed, CEP lookup with ViaCEP/BrasilAPI fallback, verification submission with proof upload (ImageSharp resize), user profile editing, admin review queue, and the EF Core migration. This is the foundation that web (02-02) and mobile (02-03) consume in wave 2.

Purpose: Establish the verified-neighbor trust mechanism — every downstream feature (feed, marketplace, map, groups) gates on it.
Output: 4 new controllers, 2 new entities, services with caching + fraud detection, EF migration applied, shared TS types exported.
</objective>

<execution_context>
@C:/Users/acq20/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/acq20/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
@src/NossoVizinho.Api/Program.cs
@src/NossoVizinho.Api/Data/AppDbContext.cs
@src/NossoVizinho.Api/Controllers/v1/AuthController.cs
@packages/shared-types/src/onboarding.ts
@packages/shared-types/src/index.ts

<interfaces>
Existing AppDbContext (src/NossoVizinho.Api/Data/AppDbContext.cs):
```csharp
public class AppDbContext : DbContext
{
    public AppDbContext(DbContextOptions<AppDbContext> options) : base(options) { }
    public DbSet<User> Users => Set<User>();
    public DbSet<RefreshToken> RefreshTokens => Set<RefreshToken>();
    public DbSet<AuditLog> AuditLogs => Set<AuditLog>();
}
```
Add: `public DbSet<Bairro> Bairros => Set<Bairro>();` and `public DbSet<Verification> Verifications => Set<Verification>();`

Existing User model needs added columns: `DisplayName`, `PhotoUrl`, `Bio` (max 160), `BairroId` (FK nullable), `IsVerified` (bool), `VerifiedAt` (DateTime?), `IsAdmin` (bool), `AcceptedTermsVersion` (string?), `AcceptedTermsAt` (DateTime?).

Existing shared types (packages/shared-types/src/onboarding.ts):
```ts
export interface CepAddress { cep, logradouro, bairro, localidade, uf, lat?, lng? }
export type ProofStatus = "none" | "uploaded" | "pending" | "approved" | "rejected";
```
New file packages/shared-types/src/verification.ts must export:
```ts
export interface CepLookupResult extends CepAddress { bairroId: number | null; bairroNome: string | null; }
export interface VerificationStatusDto { status: ProofStatus; submittedAt: string | null; reviewedAt: string | null; rejectionReason: string | null; bairroNome: string | null; }
export interface AdminVerificationListItem { id: number; userId: number; userEmail: string; cep: string; logradouro: string; bairroNome: string | null; proofUrl: string; submittedAt: string; isSuspectedDuplicate: boolean; }
export interface ProfileDto { displayName: string | null; photoUrl: string | null; bio: string | null; bairroNome: string | null; isVerified: boolean; }
export interface UpdateProfileRequest { displayName: string; bio: string; }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bairro + Verification entities, User profile fields, EF migration, Vila Velha seed</name>
  <files>
    src/NossoVizinho.Api/Models/Bairro.cs,
    src/NossoVizinho.Api/Models/Verification.cs,
    src/NossoVizinho.Api/Models/User.cs,
    src/NossoVizinho.Api/Data/AppDbContext.cs,
    src/NossoVizinho.Api/Data/Seed/VilaVelhaBairros.cs,
    src/NossoVizinho.Api/NossoVizinho.Api.csproj,
    src/NossoVizinho.Api/Migrations/
  </files>
  <read_first>
    - src/NossoVizinho.Api/Data/AppDbContext.cs (current DbContext, only Users/RefreshTokens/AuditLogs)
    - src/NossoVizinho.Api/Models/User.cs (current User entity to extend)
    - src/NossoVizinho.Api/NossoVizinho.Api.csproj (current package refs — need to add SixLabors.ImageSharp 3.1.x)
    - src/NossoVizinho.Api/Migrations/ (existing migrations to know naming convention)
  </read_first>
  <behavior>
    - Bairro entity: Id (int PK), Nome (nvarchar(120) required), Cidade (nvarchar(120) required), Uf (nvarchar(2) required), Slug (nvarchar(140) unique), IsActive (bool default true).
    - Verification entity: Id (int PK), UserId (FK->User), Cep (nvarchar(9)), Logradouro (nvarchar(200)), Numero (nvarchar(20) nullable), BairroId (FK->Bairro nullable), ProofFilePath (nvarchar(500)), ProofSha256 (nvarchar(64) indexed), Status (nvarchar(20) enum: pending/approved/rejected), ReviewedByUserId (FK->User nullable), ReviewedAt (datetime2 nullable), RejectionReason (nvarchar(500) nullable), IsSuspectedDuplicate (bool), IsDeleted (bool, soft delete per VER-010), SubmittedAt (datetime2 default GETUTCDATE), ReVerifyAfter (datetime2, SubmittedAt + 12 months for VER-007).
    - User entity adds: DisplayName (nvarchar(80) nullable), PhotoUrl (nvarchar(500) nullable), Bio (nvarchar(160) nullable), BairroId (int FK nullable), IsVerified (bool default false), VerifiedAt (datetime2 nullable), IsAdmin (bool default false), AcceptedTermsVersion (nvarchar(20) nullable), AcceptedTermsAt (datetime2 nullable).
    - AppDbContext: register both DbSets, configure relationships in OnModelCreating, add HasIndex on Verification.ProofSha256 and Verification.UserId+Status, add global query filter Verification.IsDeleted == false.
    - VilaVelhaBairros seed file: static List with at least 12 entries — Praia da Costa, Itapuã, Coqueiral de Itaparica, Itaparica, Centro, Glória, Cobilândia, Divino Espírito Santo, Jockey de Itaparica, Praia das Gaivotas, Aribiri, Soteco. Each {Nome, Cidade="Vila Velha", Uf="ES", Slug=kebab-case}.
    - Migration `AddBairrosAndVerifications` generated and applied (or applied at startup via Database.Migrate()).
    - Seeding: in Program.cs at startup, if Bairros table empty, insert VilaVelhaBairros.All.
  </behavior>
  <action>
    1. Add `<PackageReference Include="SixLabors.ImageSharp" Version="3.1.5" />` to src/NossoVizinho.Api/NossoVizinho.Api.csproj.
    2. Create src/NossoVizinho.Api/Models/Bairro.cs with the entity above.
    3. Create src/NossoVizinho.Api/Models/Verification.cs with the entity + a `VerificationStatus` enum or string constants `Pending|Approved|Rejected`.
    4. Edit src/NossoVizinho.Api/Models/User.cs to add the new nullable columns. Do NOT remove existing columns.
    5. Edit src/NossoVizinho.Api/Data/AppDbContext.cs to add `DbSet<Bairro> Bairros` + `DbSet<Verification> Verifications`, override `OnModelCreating` to configure relationships, indexes, and the soft-delete query filter `modelBuilder.Entity<Verification>().HasQueryFilter(v => !v.IsDeleted);`.
    6. Create src/NossoVizinho.Api/Data/Seed/VilaVelhaBairros.cs as `public static class VilaVelhaBairros { public static IReadOnlyList<Bairro> All => new List<Bairro> { ... }; }`.
    7. Run from D:\claude-code\nosso-vizinho: `dotnet ef migrations add AddBairrosAndVerifications --project src/NossoVizinho.Api`.
    8. Edit src/NossoVizinho.Api/Program.cs to add at startup (after `var app = builder.Build();`): scope, get AppDbContext, call `db.Database.Migrate()`, then if `!db.Bairros.Any()` insert `VilaVelhaBairros.All` and SaveChanges.
    9. Build: `dotnet build src/NossoVizinho.Api`.
  </behavior>
  <verify>
    <automated>cd /d D:\claude-code\nosso-vizinho && dotnet build src/NossoVizinho.Api -c Debug 2>&1 | tail -20 &amp;&amp; grep -l "DbSet&lt;Bairro&gt;" src/NossoVizinho.Api/Data/AppDbContext.cs &amp;&amp; grep -l "DbSet&lt;Verification&gt;" src/NossoVizinho.Api/Data/AppDbContext.cs &amp;&amp; grep -l "Praia da Costa" src/NossoVizinho.Api/Data/Seed/VilaVelhaBairros.cs &amp;&amp; grep -l "ProofSha256" src/NossoVizinho.Api/Models/Verification.cs &amp;&amp; grep -l "IsVerified" src/NossoVizinho.Api/Models/User.cs &amp;&amp; ls src/NossoVizinho.Api/Migrations/*AddBairrosAndVerifications*.cs</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build src/NossoVizinho.Api` exits 0 with no errors
    - File src/NossoVizinho.Api/Models/Bairro.cs exists and contains the exact string `public class Bairro`
    - File src/NossoVizinho.Api/Models/Verification.cs exists and contains the exact strings `public class Verification`, `ProofSha256`, `IsSuspectedDuplicate`, `IsDeleted`, `RejectionReason`
    - src/NossoVizinho.Api/Models/User.cs contains the exact strings `IsVerified`, `BairroId`, `DisplayName`, `Bio`, `IsAdmin`
    - src/NossoVizinho.Api/Data/AppDbContext.cs contains the exact strings `DbSet<Bairro> Bairros`, `DbSet<Verification> Verifications`, `HasQueryFilter`
    - src/NossoVizinho.Api/Data/Seed/VilaVelhaBairros.cs exists and contains the exact strings `Praia da Costa`, `Itapuã`, `Coqueiral de Itaparica`, `Vila Velha`
    - A new migration file matching `src/NossoVizinho.Api/Migrations/*AddBairrosAndVerifications*.cs` exists
    - src/NossoVizinho.Api/Program.cs contains the exact string `db.Database.Migrate()` and a seed call referencing `VilaVelhaBairros.All`
    - src/NossoVizinho.Api/NossoVizinho.Api.csproj contains the exact string `SixLabors.ImageSharp`
  </acceptance_criteria>
  <done>Schema migrated, Vila Velha bairros seeded automatically on startup, User has profile + verification fields, build green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CEP lookup service (ViaCEP + BrasilAPI fallback) + file storage + verification + profile + admin services and controllers</name>
  <files>
    src/NossoVizinho.Api/Services/ICepLookupService.cs,
    src/NossoVizinho.Api/Services/CepLookupService.cs,
    src/NossoVizinho.Api/Services/IBairroService.cs,
    src/NossoVizinho.Api/Services/BairroService.cs,
    src/NossoVizinho.Api/Services/IFileStorageService.cs,
    src/NossoVizinho.Api/Services/FileStorageService.cs,
    src/NossoVizinho.Api/Services/IVerificationService.cs,
    src/NossoVizinho.Api/Services/VerificationService.cs,
    src/NossoVizinho.Api/Controllers/v1/CepController.cs,
    src/NossoVizinho.Api/Controllers/v1/VerificationController.cs,
    src/NossoVizinho.Api/Controllers/v1/AdminVerificationController.cs,
    src/NossoVizinho.Api/Controllers/v1/ProfileController.cs,
    src/NossoVizinho.Api/Program.cs,
    src/NossoVizinho.Api/web.config,
    packages/shared-types/src/verification.ts,
    packages/shared-types/src/index.ts
  </files>
  <read_first>
    - src/NossoVizinho.Api/Controllers/v1/AuthController.cs (controller convention, base route, [ApiController], rate limit policy attribute)
    - src/NossoVizinho.Api/Services/AuthService.cs (DI / async patterns to mirror)
    - src/NossoVizinho.Api/Program.cs (where to register HttpClient, scoped services, IMemoryCache)
    - src/NossoVizinho.Api/web.config (current request body size limits — must allow 5MB uploads)
    - packages/shared-types/src/index.ts (export pattern to follow)
    - packages/shared-types/src/onboarding.ts (existing CepAddress, ProofStatus to extend)
  </read_first>
  <behavior>
    - CepLookupService: injected HttpClient + IMemoryCache. `LookupAsync(string cep)` strips non-digits, validates 8 digits, checks IMemoryCache (key `cep:{digits}`, 24h TTL). Calls `https://viacep.com.br/ws/{digits}/json/`. If response.erro==true OR HTTP fails, falls back to `https://brasilapi.com.br/api/cep/v2/{digits}`. Returns `CepLookupResult` with `BairroId` resolved by BairroService. Throws `CepNotFoundException` if both fail.
    - BairroService: `MatchBairroAsync(string bairroNome, string cidade, string uf)` does case-insensitive match against Bairros table where IsActive=true; returns matched Bairro or null. Uses IMemoryCache with key `bairros:{uf}:{cidade}` (1h TTL).
    - FileStorageService: `SaveProofAsync(Stream content, string fileName, string contentType)` validates contentType in [image/jpeg, image/png, image/webp, application/pdf], validates length ≤ 5MB. For images: load with ImageSharp, resize to max 1600px on longest side (preserve aspect), encode JPEG quality 85, write to `wwwroot/uploads/proofs/{yyyy}/{MM}/{guid}.jpg`. For PDFs: write raw to same folder with `.pdf` extension. Returns `(string relativePath, string sha256)` where sha256 is hex of the stored bytes.
    - VerificationService.SubmitAsync(userId, cep, numero, IFormFile proof): looks up cep via ICepLookupService, calls FileStorageService.SaveProofAsync, checks if any other Verification has the same ProofSha256 → set IsSuspectedDuplicate=true, creates Verification row Status=Pending, returns dto.
    - VerificationService.GetMyStatusAsync(userId): returns latest non-deleted Verification for user as VerificationStatusDto. If none → status="none".
    - VerificationService.ListPendingAsync(skip, take): returns admin list items including user email + suspected-duplicate flag.
    - VerificationService.ApproveAsync(verificationId, adminUserId): sets Status=Approved, ReviewedByUserId, ReviewedAt; updates User.IsVerified=true, User.BairroId=verification.BairroId, User.VerifiedAt=UtcNow; logs to AuditLogs.
    - VerificationService.RejectAsync(verificationId, adminUserId, reason): sets Status=Rejected, RejectionReason; logs.
    - ProfileController: GET /api/v1/profile/me returns ProfileDto. PUT /api/v1/profile/me accepts {displayName(1-80), bio(0-160)}, updates User. Both [Authorize].
    - CepController: `[HttpGet("/api/v1/cep/{cep}")] [EnableRateLimiting("public")]` returns CepLookupResult; 404 if not found.
    - VerificationController: `[Authorize]` `[HttpPost("/api/v1/verification")]` accepts multipart form (`cep`, `numero`, `IFormFile proof`); `[HttpGet("/api/v1/verification/me")]` returns status DTO.
    - AdminVerificationController: `[Authorize]` + custom requirement `User.IsAdmin == true` (use a simple `[Authorize(Policy="Admin")]` policy registered in Program.cs that checks claim `is_admin`). Routes: `GET /api/v1/admin/verifications?status=pending&skip=0&take=20`, `POST /api/v1/admin/verifications/{id}/approve`, `POST /api/v1/admin/verifications/{id}/reject` body `{reason}`.
    - Static files: `app.UseStaticFiles()` enabled (proofs are admin-only — actually serve them via authenticated endpoint `GET /api/v1/admin/verifications/{id}/proof` that streams from disk; do NOT expose wwwroot/uploads publicly).
    - Program.cs registrations: `AddHttpClient<ICepLookupService, CepLookupService>()`, `AddMemoryCache()`, AddScoped for BairroService, FileStorageService, VerificationService. Add admin policy. Add `[Authorize(Policy="Admin")]` requires `is_admin` claim — update TokenService later or add via DB lookup in a custom AuthorizationHandler that loads User.IsAdmin. For simplicity: add `AdminAuthorizationHandler : AuthorizationHandler<AdminRequirement>` that resolves IUserRepository / AppDbContext and checks `db.Users.Find(userId).IsAdmin`.
    - JWT claim: update TokenService to include `is_admin` claim from User.IsAdmin so middleware can check via standard policy (cleaner) — verify by reading existing TokenService first.
    - web.config: ensure `<requestLimits maxAllowedContentLength="6291456" />` (6MB) and `<httpRuntime maxRequestLength="6144" />` for 5MB uploads + headroom.
    - shared-types: create packages/shared-types/src/verification.ts with the interfaces in the &lt;interfaces&gt; block above. Export from packages/shared-types/src/index.ts.
  </behavior>
  <action>
    1. Read AuthController.cs and TokenService.cs first to mirror conventions (rate limit attribute names, route prefix, claim issuance).
    2. Create the 4 service interface + implementation pairs.
    3. Create the 4 controllers.
    4. Update Program.cs: AddMemoryCache, AddHttpClient&lt;ICepLookupService,CepLookupService&gt;, scoped registrations, AddAuthorization policy "Admin" requiring claim `is_admin == true`, register UseStaticFiles BEFORE UseAuthentication (only if needed; prefer authenticated proof endpoint).
    5. Update TokenService to add claim `new Claim("is_admin", user.IsAdmin.ToString().ToLower())`.
    6. Update web.config request limits.
    7. Create packages/shared-types/src/verification.ts with the DTOs and re-export from index.ts.
    8. Build backend + run `pnpm --filter @bairronow/shared-types build` (or `tsc -b packages/shared-types`).
  </action>
  <verify>
    <automated>cd /d D:\claude-code\nosso-vizinho && dotnet build src/NossoVizinho.Api -c Debug 2>&1 | tail -10 &amp;&amp; grep -l "ViaCEP\|viacep.com.br" src/NossoVizinho.Api/Services/CepLookupService.cs &amp;&amp; grep -l "brasilapi.com.br" src/NossoVizinho.Api/Services/CepLookupService.cs &amp;&amp; grep -l "SixLabors.ImageSharp" src/NossoVizinho.Api/Services/FileStorageService.cs &amp;&amp; grep -l "ProofSha256" src/NossoVizinho.Api/Services/VerificationService.cs &amp;&amp; grep -l "/api/v1/cep" src/NossoVizinho.Api/Controllers/v1/CepController.cs &amp;&amp; grep -l "/api/v1/verification" src/NossoVizinho.Api/Controllers/v1/VerificationController.cs &amp;&amp; grep -l "/api/v1/admin/verifications" src/NossoVizinho.Api/Controllers/v1/AdminVerificationController.cs &amp;&amp; grep -l "/api/v1/profile/me" src/NossoVizinho.Api/Controllers/v1/ProfileController.cs &amp;&amp; grep -l "is_admin" src/NossoVizinho.Api/Program.cs &amp;&amp; grep -l "VerificationStatusDto" packages/shared-types/src/verification.ts &amp;&amp; grep -l "AdminVerificationListItem" packages/shared-types/src/verification.ts &amp;&amp; pnpm --filter @bairronow/shared-types build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build src/NossoVizinho.Api` exits 0
    - src/NossoVizinho.Api/Services/CepLookupService.cs contains the exact strings `viacep.com.br`, `brasilapi.com.br`, `IMemoryCache`
    - src/NossoVizinho.Api/Services/FileStorageService.cs contains the exact strings `SixLabors.ImageSharp`, `wwwroot/uploads/proofs`, `5 * 1024 * 1024` (or equivalent 5242880)
    - src/NossoVizinho.Api/Services/VerificationService.cs contains the exact strings `IsSuspectedDuplicate`, `ProofSha256`, `User.IsVerified = true` (or equivalent assignment to IsVerified)
    - src/NossoVizinho.Api/Controllers/v1/CepController.cs contains the exact string `/api/v1/cep`
    - src/NossoVizinho.Api/Controllers/v1/VerificationController.cs contains the exact strings `/api/v1/verification`, `IFormFile`, `[Authorize]`
    - src/NossoVizinho.Api/Controllers/v1/AdminVerificationController.cs contains the exact strings `/api/v1/admin/verifications`, `approve`, `reject`, `Policy = "Admin"` (or `Policy="Admin"`)
    - src/NossoVizinho.Api/Controllers/v1/ProfileController.cs contains the exact strings `/api/v1/profile/me`, `[Authorize]`
    - src/NossoVizinho.Api/Program.cs contains the exact strings `AddHttpClient<ICepLookupService`, `AddMemoryCache`, `is_admin`, `AddPolicy("Admin"`
    - src/NossoVizinho.Api/web.config contains the exact string `maxAllowedContentLength="6291456"`
    - packages/shared-types/src/verification.ts contains the exact strings `CepLookupResult`, `VerificationStatusDto`, `AdminVerificationListItem`, `ProfileDto`, `UpdateProfileRequest`
    - packages/shared-types/src/index.ts contains the exact string `verification` (re-export)
    - `pnpm --filter @bairronow/shared-types build` exits 0
  </acceptance_criteria>
  <done>All endpoints implemented, build green, shared TS types published. Web (02-02) and mobile (02-03) can now consume the real API.</done>
</task>

</tasks>

<verification>
End-to-end smoke (manual run after deploy):
1. `dotnet run --project src/NossoVizinho.Api` then `curl -i http://localhost:5000/api/v1/cep/29101-160` returns 200 with bairroId.
2. Register a user, POST multipart to /api/v1/verification with a small JPG → 200 + status pending.
3. GET /api/v1/verification/me → status pending.
4. Manually flip user IsAdmin=true in DB, GET /api/v1/admin/verifications?status=pending → list contains it.
5. POST .../approve → User.IsVerified becomes true.
</verification>

<success_criteria>
- All 12 phase requirements have backend support (endpoints/entities/services exist)
- ViaCEP fallback to BrasilAPI verified by code inspection
- Vila Velha seed populated automatically on first startup
- Soft delete query filter active on Verifications
- Duplicate-proof detection writes IsSuspectedDuplicate
- Admin policy gates admin endpoints via is_admin claim
- shared-types builds and exports new verification module
</success_criteria>

<output>
After completion, create `.planning/phases/02-verification-neighborhoods/02-01-SUMMARY.md` documenting endpoints, entities, services, key decisions, and any deviations.
</output>
